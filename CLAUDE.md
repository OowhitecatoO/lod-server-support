# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **Support branch: `support/mc26.1-v0.10`.** The FRESH v0.10.0 re-port of the full main
> tree (protocol 20, tile cache, perf round, tracer, yield) to **Minecraft 26.1.2** (do NOT
> merge to `main`), cut from main at the v0.10.0 feature level on 2026-08-08 per the XVER
> plan §11.2 / mega-plan R-7. It SUPERSEDES the frozen v0.8.0-era `support/mc26.1` branch
> (kept only as the per-line-flavor reference). Releases tag `v<x.y.z>+mc26.1`.
> Divergences from the primary 26.2 line: `IntegratedServerLanHook` uses the single
> `publishServer(GameType,boolean,int)` overload (26.2's `MultiplayerScope` split does not
> exist here — `LanHookContractTest` carries the per-line pin); toolchain pins fabric-api
> 0.151.0+26.1.2, sodium mc26.1.2-0.8.12, modmenu 18.0.0-beta.1, paperweight dev bundle
> `26.1.2.build.69-stable` (Java 25 unchanged); fabric.mod.json pins `>=26.1 <26.2-`;
> `folia-supported: true` stays correct here because Folia publishes 26.1.2 builds (the
> R-7 direction-flip check was applied, not assumed); the `xray-masked` golden pair is
> regenerated on this line (one registry id differs from 26.2 — the v20-corpus twin and
> the other 13 nbt-corpus goldens are line-invariant, verified byte-identical); the
> 26.2-captured `xver-live-corpus` decodes STRICTLY on this line (zero fallbacks — the
> automated issue-#85 cross-line proof); release.yml/ReleaseWorkflowContractTest carry the
> line-scoped flavors; soak.sh guards base worlds with an mc-version marker. Sections
> below that say "26.2" describe the primary line and apply here with those substitutions.

## Project

LOD Server Support (LSS) — distributes LOD chunk data from servers to clients over a custom networking protocol. Supports Fabric (client + server) and Paper (server only). The Folia code paths (regionized probing, lifecycle mailbox) exist in the one plugin jar. **`folia-supported` is declared on every line again as of 2026-08-01**: Folia published its first MC 26.2 build (`26.2-1`, channel **BETA**, 2026-07-28), removing the reason it was dropped at v0.7.0 (the flag would have auto-loaded release jars onto a platform that did not exist). `PluginYmlContractTest` + `release_check.py` now pin the flag's PRESENCE — the guarded failure is a jar that silently stops loading on Folia. Folia stays **experimental on every line** (26.2 included): single-player soak validated, concurrent multi-region ingress untested. `SOAK_PLATFORM=folia` finally has something to download on 26.2, and **all four Folia scenarios passed on 2026-08-01** against real Folia 26.2-1 — fresh-backfill (41 windows), warm-rejoin (55), dimension-trip (55), paper-dirty-falling-block (27), 0 violations and 0 warnings throughout. The regionized probe (`in_memory` serves), Moonrise generation (2121 submitted/completed, 0 timeouts) and the protocol-19 zstd column path all ran. **This does NOT retire the experimental label**: every one of those scenarios is single-player, and the stated exit criterion is concurrent MULTI-REGION ingress, which no scenario in the harness produces. Clients request distant chunks individually; the server reads them from disk or memory and streams serialized sections back, enabling LOD rendering mods to display terrain beyond vanilla render distance.

## Project Structure

Multi-project Gradle build with three subprojects:

```
lod-server-support/
├── common/   Pure Java utilities (no MC deps) — shared by fabric/ and paper/
├── fabric/   Fabric mod (client + server), Minecraft 26.2
└── paper/    Paper/Folia plugin (server only), Minecraft 26.2
```

## Build Commands

```bash
./gradlew :fabric:build -x runClientGameTest  # Build Fabric mod + Tier 1 & 2 tests
./gradlew :paper:shadowJar                    # Build Paper plugin JAR
./gradlew clean                               # Clean build artifacts
```

Output JARs:
- `fabric/build/libs/lod-server-support-fabric.jar` — Fabric mod (client + server)
- `paper/build/libs/lod-server-support-paper.jar` — Paper plugin (server only, shadow JAR; serves Paper and Purpur — `folia-supported` is declared again on the 26.2 line, so Folia loads this jar)
- `fabric/build/libs/voxy-server-side-fabric*.jar` / `paper/build/libs/voxy-server-side-paper*.jar` — the **Voxy Server Side** branded pair (`vssJar` tasks: near-byte-copies of the LSS jars that rewrite **only user-facing display + LOCAL command surfaces**, never identity or wire). What differs: fabric.mod.json name/icon/homepage, plugin.yml description/website + the command key (`lsslod`→`vsslod`) + permission node (`lss.admin`→`vss.admin`), the `lss-brand.properties` resource, and the CLIENT config filename a fresh install creates (`lss-client-config.json`→`vss-client-config.json` — brand-preferred with a cross-brand fallback, so a jar swap still adopts whichever file is already there and keeps its config; see `JsonConfig.brandedConfigCandidates` / `LSSClientConfig`). What stays verbatim (the LSS/VSS interchangeability + **wire-compatibility** contract — an LSS client ↔ VSS server works, and vice versa): mod id `lss`, plugin name `LodServerSupport`, package/class names, the SERVER config filename (`lss-server-config.json` — brand-invariant on both platforms, so a server jar swap trivially keeps config), and the entire `lss:*` channel/protocol/payload wire surface. The nested `common-*.jar` is byte-**identical** between the LSS and VSS Fabric jars — `release_check.py`'s `check_wire_identity_fabric` pins that SHA equality as the wire-compat proof, alongside the brand.properties + plugin.yml pair checks. Runtime display branding flows through `common/Brand` (loaded from `lss-brand.properties` as each platform's FIRST init step; defaults to LSS so tests and un-repackaged jars are unbranded), consulted by `LSSLogger`'s category, all chat/console/thread-name text, and the client/server command literals. Published to Modrinth project `84zcagOb` (voxy-server-side) by releases v0.7.0–v0.7.3; **VSS publishing is DISABLED since v0.8.0 on every line** (user decision at the tri-release) — the vssJar tasks still build the pair and `release_check.py` still gates it (wire-identity + pair checks), but release.yml attaches/uploads only the LSS pair, pinned by `ReleaseWorkflowContractTest`.

CI builds (env `CI=true`) name the jars `lod-server-support-<platform>-<mod_version>+<minecraft_version>.jar` (e.g. `lod-server-support-fabric-0.6.0+26.2.jar`); the release workflow feeds `mod_version` from the tag. Local dev builds keep the stable unversioned names.

## Test Commands

```bash
./gradlew :fabric:test -x runGameTest -x runClientGameTest  # Tier 1: JUnit unit tests only (~40s)
./gradlew :fabric:runGameTest                                # Tier 2: server gametests (starts dedicated server, ~13s)
# NOTE: `:fabric:test` alone runs Tier 1 ONLY — runGameTest hangs off check/build, not test.
# For Tier 1 + 2 run both commands above, or `:fabric:build -x runClientGameTest`.
./gradlew :fabric:runClientGameTest                          # Tier 3: client gametests (starts integrated server + client)
./gradlew :fabric:build -x runClientGameTest                 # Full build + Tier 1 + 2 tests
./gradlew :paper:test                                       # Paper JUnit tests (wire parity, NBT serialization, config)
```

Most tests live in the Fabric module. Paper has Tier 1 JUnit tests as well (`./gradlew :paper:test`, ~330 tests; also run by CI) — Fabric/Paper wire parity, payload edge frames, NBT section serialization (incl. a golden cross-module byte corpus + live-vs-NBT parity + the masked disk-path pin + the round-2 transcode-vs-object fuzz and fallback-ladder twins + the `PaperMemoizedNbtCodecTest` memo-Key twin), the x-ray mask filter/manager twins (`PaperXrayMaskFilterTest`/`PaperXrayMaskManagerTest` — masking semantics, engine-adoption ladder, owner-guarded static holder), the shared handshake gate (incl. both compat rungs), the v18 compat twins (`rewriteColumnToV18` splice goldens incl. the codec-1 throw, the glue dialect + kill-switch pins, forced-raw derivation + dim-change membership survival + the quit-race mailbox-Remove drain on the service twins, the compat-defaults pin), config validation + malformed-file tolerance, world-handler event reflection, the request-service / broadcaster / generation-service / disk-reader twins driven through injection seams, plugin enable-plan/dispatch/handshake glue (incl. the deferred-reply registration ordering) + plugin.yml contract (incl. `folia-supported` PRESENCE on the 26.2 line — declared again since 2026-08-01) + the release-workflow contract (`ReleaseWorkflowContractTest` — tri-line tag/PREV_TAG scoping, LSS-pair release assets + VSS-publish absence, Modrinth step pins; the support branches carry their own flavors), the Folia seams (lifecycle mailbox incl. foreign-thread visibility, the `FoliaSupport` platform probe, the soak driver's save-all mapping, the generation completion-thread extraction ladder, the experimental regionized loaded-chunk probe scheduling — `RegionProbeSchedulingTest`: EntityScheduler hand-off, region-ownership gating, one-tick batch hold-release alignment + the `republishHeldBatch` pins — a held batch must lose to a newer arrival (the CAS) AND to a batch that passed through the mailbox during the hold (the offer-generation guard), be counted superseded, and never be resurrected (modulo the nanoseconds-wide retract-failure window documented at the second guard — one interval of stale routing, re-superseded by the next declaration) — dimension-change discard, departed-player sweep), `FoliaWiringContractTest` (constant-pool scan of every production class, nested classes included: no legacy-scheduler references, lifecycle routed through the mailbox), exporter schema parity, and command output. Paper runtime behavior is validated live by the soak harness on both Bukkit platforms (`SOAK_PLATFORM=paper` / `SOAK_PLATFORM=folia ./scripts/soak.sh all` — fresh-backfill, warm-rejoin, dimension-trip, paper-dirty-falling-block against a real Paper / Folia server; Folia published 26.2-1 on 2026-07-28, so `runFolia` has something to download on this line, and the 26.2 plugin.yml declares `folia-supported` again and all four Folia scenarios passed 2026-08-01).

### Test Architecture

- **Tier 1** (`fabric/src/test/java/dev/vox/lss/`, ~1236 tests): JUnit 5 tests via `fabric-loader-junit`. Tests the LOD store family (`SqliteLodStoreTest` incl. the stat-driven sweep decision table, acq-stamp judgement, the C3 fault-seam pins — rollback-surviving deletes, re-queued failed deletes, the writer-failure latch with its `latched` state token — cap eviction + one-shot log + backfill un-mark, `MemoryLodStoreTest`, `StoreBackfillTest` incl. pacing windows, cap-stop/resume, estimate line, acq-before-read, `RegistryFingerprintTest` — the A3 permutation pins, `StoreEnvironmentContractTest` + Paper twin — source-regex wiring pins, `OffThreadProcessorStoreTest`/`DiskReaderStoreRungTest`/`SaveHookStoreBridgeTest`), position packing, bandwidth limiter, config validation + GSON whole-file-fallback semantics, column cache store (pre-v4 discard / path-sanitization / oversize / corruption guards), column state map (incl. answer-time mark consumption), the awaiting-answer tracker, the per-player mailbox/backlog replace path (`BacklogReplaceTest`: latest-wins overwrite, superseded accounting, the published want-set surviving a take, the empty clear batch), column timestamp cache (D0 tile shape: the §2.2 clamp truth table — every direction rounds up or refuses to fabricate — a 20k-op model-differential fuzz against a reference map, tile-granular oldest-lastTouch eviction, v1→v2 migration golden + v2 corruption/liveCount-disagreement discards, per-dimension isolation, save atomicity / the miss memo: TTL boundary, put()-clears, invalidate-vs-invalidateStamps split, never-persisted, kill-switch ttl 0, wrap-safe deadline compare; plus the region/slot addressing pins in `PositionPackingTest` and the router-level pre-epoch clamp pin in `IncomingRequestRouterTest`), the coalesced save scheduler (`TimestampSaveSchedulerTest` — latest-wins slot, one-drain queue bound, shutdown-rejection clear, discardPending, and the processor wiring pin that reds a raw-execute revert — issue #62), the served-set sweep (`DiskReadDoneSweepTest` — Chebyshev boundary, no-stamp no-op, in-pipeline/grace-window keeps, the eviction-cycle wiring pin — M3), the router drain-order rotation (`RouteRotationFairnessTest` — two saturated cycles serve two distinct players, empty-backlog no-wedge — M4), the LAN-hook descriptor contract (`LanHookContractTest` — pins the 2-arg publishServer overload the 26.2 GUI actually calls and resolves it against the real class, source-regex because mixin-package classes refuse classloading under fabric-loader-junit — M2), the composed fill-pass pins in `ClientColumnProcessorTest` (sky-before-air-fill through the real drain: above-band bright on resync, below-band dark, bright sky-dimension clears — M1), dirty tracking and content filtering (fail-open + zero-hash sentinel), slot admission and the off-thread processor mailbox (incl. same-UUID re-registration survival), payload codecs + protocol-constant envelope, Fabric/Paper wire parity, NBT section serialization + a golden cross-module byte corpus + section-light defaults + the round-2 NBT->wire transcode pins (transcode-vs-object fuzz, per-section fallback-ladder incl. the x-ray pre-gate, six nbt-corpus goldens generated by the object path), the palette-memo suite (`MemoizedNbtCodecTest` + Paper twin — capacity-history keying independence across FORCED different backing-map iteration orders incl. nested, defensive-copy remainder, cap-stop + warn-latch, foreign-ops bypass, error-result caching; raw-tag keying is a MEASURED decision — the B1/R3 precomputed-hash Key was reverted on a failed A/B gate, see the class javadoc), negative/oversized payload edge cases, the LodRequestManager request-loop contract (untracked-response gates, dimension guard, ingest-failure unstamping, tick decomposition, RTT distribution, the want-set pins: convergence sends nothing at all, entering backpressure halt — decode-queue count/bytes or the consumer ingest backlog — sends exactly one empty clear batch per crossing, awaited positions are re-declared and block ring confirmation, and the retired byte 0 stays as inert as an unknown type; plus the #71 ingest-pacing pins: the production backlog-supplier wiring behind the `enableIngestBackpressure` config gate; plus the adaptive-cadence integration pins: the near-answered fast fire through the real tick path, the partial-answer periodic hold — the tracker::size wiring pin — send-failure/disconnect disarms, the converged-fast-walk disarm through the production arming, the quarter-line pressure hold, the backpressure-clear disarm, and the kill switch's production config binding), the extracted client `ClientSessionGate` (handshake/session-config/disconnect ladder), `ClientColumnProcessor` decode drain (silent-clear paths report through ingest-failure), the reflective Voxy bridge (full report/latch/rethrow ladder + the ingest-backlog probe's separate failure domain — null-instance/null-service/throw all read no-signal, a missing chain never costs the bridge — stubbed incl. `VoxyCommon`/`VoxyInstance`), the `LSSApi` backlog aggregation (`LSSApiBacklogTest` — max-over-consumers, lambda keeps the no-report default, throw containment + the per-JVM warn-once latch, true Errors propagate), the AntiXray compat shim + engine probe (`AntiXrayCompatTest` — carrier/resolver ladders, exception transparency, probe latch, driven against real-package-name stub classes under `fabric/src/test/java/me/drex/`), probe-path containment (`ProbeContainmentTest`), the x-ray mask filter/policy/manager (`XrayMaskFilterTest`/`XrayMaskPolicyTest`/`XrayMaskManagerTest` — byte determinism across live/NBT paths, height + straddle gates incl. negative-Y, never-air + count-header recalc, registry-id tie-break, engine adoption incl. the empty-adoption asymmetry, owner-guarded static holder, the `xray-masked` golden fixture), spiral-scanner budget scaling (queue + ingest-backlog factors, min composition, floor-at-1), retry reset, and the adaptive-cadence trigger ladder (floor/threshold edges incl. geometric tightening, ¼-halt pressure gates on all three pipes, prefix-invalidation + actionable-retry holds, converged-walk + reset-family disarms, v16/kill-switch/null-config exclusions, the fallback-window restart, the missingVanilla probe skip, the numeric constant pins), IncomingRequestRouter conservation + clientTimestamp routing + the honest re-resolution ladder incl. the duplicate-serve grace (crossing re-asks absorbed inside `SEND_DEPARTURE_GRACE_MILLIS`, stamp-on-send-success-only, expiry sweep, production-default pin) + clock-skew, cross-player dedup fan-out, the disk-reader submit envelope + late-delivery drop + bounded shutdown + the `hasHeadroom` gate agreeing with a real pool rejection, OffThreadProcessor disk-result delivery and lifecycle + the invalidation-overtake stale guard (the disk twin, the per-player generation twin swept on player removal, the error-retry replay that must not un-taint a consumed outcome, and the multi-holder taint invariant), the miss-memo processor pins (authoritative-only seeding — error-triage, ghost, edit-overtaken, and gen-disabled deliveries never seed; every generation-outcome flavor clears incl. delivered-data and departed-player; memo-hit escalation skips the re-read; the pacing rules on BOTH admission paths — nearer-read hold incl. the delivery-path/movement case, cohort span, straggler-tightens-never-blocks), send-action batching, the shared HandshakeGate ladder (incl. both compat rungs — the v18/v16 flag-independence pins), the v18 compat rung family (`WireDialectTrackerTest` — the renamed membership tracker: lifecycle, cross-dialect sheds, the persists-after-disconnect diag line; the `asV18()` wire goldens in `WireParityTest` incl. store-source pass-through, ghost-clear, long-dimension, and the exact-one-codec-byte splice-position pin; the compat-defaults pin in `ConfigValidationTest`; the `V18Compat` diag slot ordering in `DiagnosticsFormatterTest`), client column decode + LSSApi dispatch, the data-capture exporter schema contracts (both platforms), DiagnosticsFormatter golden lines, the column cache pre-v4 discard-not-migrate gate, the VoxelColumn receiver glue (held-before ordering + authoritative 0-section clears), the gametest-entrypoint listing contract, the move-desync tracer suites (`MoveEventMathTest` — the vanilla-vs-LSS chunk-key packing pin, clamp/mask/gap-clock math; `MoveDesyncTracerTest` — writer lifecycle, deterministic overflow drops, rotate-once-then-cap; `MoveRowGoldenTest` — row goldens against the `scripts/testdata/move-trace-rows.jsonl` fixture SHARED with `check_move_trace.py --selftest` so writer and reader cannot drift, absent-vs-null discipline pinned; `MoonriseSendStateCompatTest` — the two-rung resolver against real-package-name stubs under `ca/spottedleaf/`, null-loader = none-per-call; `MoveTraceFieldPinsTest` — reflective existence pins for every accessor target + public member; `MoveTraceHookContractTest` — source-regex mixin pins incl. the non-required `lss-trace.mixins.json` + an ASM `ClassReader` scan of the real `handleMovePlayer` asserting the warn/teleport INVOKE census and relative order, the slice-anchor's tripwire; the `MoveTrace:` conditional diag slot in `DiagnosticsFormatterTest`; the probe-snapshot degrade VALUE pin in `ChannelPressureSnapshotTest`), the transport-yield gate suite (`TransportYieldFlushTest` — writable-never-yields (the CI-inertness pin: loopback channels never go unwritable, so armed soaks are provably identical), the retain/count/sweep/no-stamp unwritable tick, the exactly-one-payload starvation floor + reset, ceiling-fires-before-yield with NO floor on the ceiling path (the deliberate F2-7 asymmetry), UNKNOWN-never-yields for legacy-lambda/no-signal probes, short-overloads-pin-yield-off, and the §2.1 relevance prune — cadence-gated, in-grace keeps, pruned positions merged into the send-failure drop report; plus the S-9b both-platform wiring pin in `ChannelAccessorContractTest` and default-FALSE pins in both config suites), and malformed-config-file tolerance.
- **Tier 2** (`fabric/src/gametest/java/dev/vox/lss/test/`): 65 Fabric server gametests across 8 classes (seeded through the shared `GameTestSeeding` helper — the mailbox is latest-wins, so multi-position seeds must be ONE batch, not back-to-back declarations) (all listed in the `fabric-gametest` entrypoint of `fabric/src/gametest/resources/fabric.mod.json` — a new class MUST be added there or it compiles but never runs; `GameTestEntrypointContractTest` in Tier 1 enforces the listing) — `LSSGameTests` (service activation, diagnostics, command registration, config loading), `ServiceLifecycleGameTests` (player registration/cleanup, in-memory probe without dirty-filter seeding, batch-request distance guards incl. extreme-coord overflow safety, idempotent shutdown incl. the x-ray mask-manager activate/retract lifecycle pin, the crafted-frame handshake pins through the production receiver — caps-0 reply-no-register, foreign-version silence (specimen 17: both neighbors now have compat rungs), and the v18 rung registering natively + echoing 18 + forced codec-RAW), `SerializerParityGameTests` (disk-read vs live serializer byte parity, DirtyContentFilter re-save suppression, End-void all-air sentinel, the world-section-range inclusivity pin for the R2-5 disk range gate, and masked disk/live parity — `xrayMaskedDiskReadBytesMatchMaskedLiveBytes`, which flips the static mask manager in same-tick windows because concurrent gametest service ctors stomp it), `GenerationLifecycleGameTests` (generation caps/piggybacking/timeouts, dimension-change re-registration), `CommandGameTests` (`/lsslod` permission gating + stats/diag dispatch rendering + exporter/formatter agreement + the store-verb tree dispatched off/unavailable), `TwoPlayerGameTests` (two-player dedup convergence, bandwidth fairness + idle dilution, vanilla-player-without-handshake invisibility, edited-column broadcast fan-out to two holders), `RegionFaultGameTests` (corrupt-region containment: garbage-zlib resolves not-found, the read pool survives), `MoveTraceGameTests` (the move-desync tracer's hook layer through the production mixins via the `enableForTest` sink: too_quickly with both packet counts, the SILENT entity/block rejection in creative — the zero-observability path the tracer exists for — and the survival `wrongly`+`rejected` pair via a hand-rolled survival player, because the stock mock hard-codes CREATIVE and the wrongly warn is gated `!isCreative`; mock connections need `primeListenerForMoves` — placeNewPlayer arms the 60-tick client-load timer and latches the placement teleport, so un-primed moves are silently swallowed). The in-game runner additionally counts vanilla's built-in `minecraft:always_pass` test instance.
- **Tier 3** (`fabric/src/gametest/java/dev/vox/lss/test/LSSClientGameTests.java`): End-to-end client-server flow test. Validates handshake, session config, spiral scanning, chunk section receipt, decoded section content asserted at a registered `LSSApi` consumer (flat-world block layers, off-render-thread dispatch), and the ingest-failure recovery loop (a consumer rejecting a column — by `LSSApi.reportIngestFailure` or by throwing — gets it re-served end-to-end). Fabric Loom handles headless rendering automatically.

The system property `-Dlss.test.integratedServer=true` (set in fabric/build.gradle for gametest JVMs) allows `RequestProcessingService` to activate on integrated servers during testing.

### Known Test Flakes & Environmental Failures

**This section is the canonical home for known CI test flakes and environment-specific test failures — keep it up to date, and record all similar findings here.** When a test failure is diagnosed as a flake or an environment quirk (not a code regression), add an entry here so the next contributor/session **re-runs rather than chases a phantom regression**. Each entry gives the test, the symptom, why it's benign, and the action. (These are also mirrored in the assistant's project memory, but this file is the in-repo source of truth.)

Since 2026-07 `build.yml` contains the flakes automatically: docs-only changes (`**.md`, `docs/**`) skip the workflow entirely, the Tier 2 and Tier 3 gametest steps **retry once** on failure (each retry emits a `::warning::` annotation in the run summary — a retried-then-green run means a flake fired, not that nothing happened), and the first attempt's logs/crash-reports are uploaded as `gametest-flake-evidence-*` artifacts (failures upload `gametest-failure-evidence-*`, retention 30 days — GitHub's raw job logs for this repo expire within days, the artifacts are the durable record). A job that is red even after its retry is very unlikely to be one of the entries below — diagnose it from the artifacts before re-running.

- **`RegionFaultGameTests` (Tier 2) — environmental on some dev boxes.** `region_fault_..._corrupt_region_chunk_resolves_as_contained_error_and_reader_survives` can fail (`errors=1 ... not_found=0 on tick 1202`) on constrained / WSL2 machines **regardless of load** (reproduces with all changes stashed) yet passes in clean CI. The corrupt-region triage is version-robust (a broad `catch (Exception)` resolves to not-found). Action: trust clean CI — a local red here is not a regression.
- **`IncomingRequestRouterTest.loadedProbeBeatsAFreshMissMemo` (Tier 1) — polling-deadline flake, hardened 2026-08-03.** Failed once on a GitHub runner at `IncomingRequestRouterTest.java:188`, which is the `fail("timed out waiting for")` inside `waitFor`, NOT a logic assertion. Content-identical code passed the same commit's PR run, the release run (which builds Tier 1 too) and 5/5 locally — three passes to one failure. The helpers' deadline was 5 s for work that takes milliseconds, so it was measuring runner contention; raised to 30 s (`POLL_DEADLINE_NANOS`), which costs nothing on the passing path. **Note `build.yml` retries the GAMETEST tiers once but NOT Tier 1**, so a timing flake in a JUnit test fails the run outright with no second attempt — worth remembering when a green-everywhere-else commit reds only on the main Build. A failure at a DIFFERENT assertion in this test is a real miss-memo/probe-ordering regression, not this entry.
- **`LSSClientGameTests` superflat section count (Tier 3) — hardened in v0.6.1 (was a serialization-window flake).** `assertDecodedFlatWorldContent` used to fail with *"superflat columns must decode to exactly the one non-air section, got 24"* when a column was serialized during unsettled spawn-prep (its air sections not yet culled by `SectionSerializer`'s `hasOnlyAir` / block-light filter) and that stale delivery lingered in `recorder.snapshot()`. It flaked on the v0.6.0 and v0.6.1 PR CI (twice in a row on the latter — re-running did not clear it). The assertion now checks the content section (sectionY -4) and requires any extra sections to be **air-only** (sampled block states — the extras' blocks are genuinely air, only cull metadata/light differed), tolerating the transient un-culled sections while still catching real decode bugs. If a *new* "expected exactly one content section" or "must be air-only" variant appears, treat it as a real decode regression, not a flake. (`release.yml` runs `-x runClientGameTest`, so Tier 3 never gated the publish regardless.)
- **`TwoPlayerGameTests` fan-out (Tier 2) — hardened 2026-07 (was a one-shot re-ask race, twice).** `editedColumnPropagatesToBothHoldersThroughBroadcastFanout` failed as "A=3 B=1 on tick 1202" when a step-2 re-ask routed before the broadcaster's dirty-clear mailbox event applied on the processing thread and terminally resolved up_to_date off the stale done-bit. Step 1's version of the same bug was fixed in PR #19; step 2 flaked again 2026-07-10 and now retries its ts=1L re-asks under the same exactly-once guards (mirroring the real client's rescan retry, without weakening the pin — an undelivered clear still freezes the counts and fails at maxTicks). If this test fails again, treat it as a real fan-out regression, not a re-run.
- **`GenerationLifecycleGameTests` (Tier 2) — cold-gen timing, budgets raised 2026-07.** The four tests that wait on real cold chunk generation (piggybacked, same-tick re-request, End-void sentinel, generation-serve seed) ran at 600 maxTicks, which first-run generation can exceed on starved runners; all now run at 1200 (FP-033 needed the same bump earlier). A timeout beyond 1200 ticks is worth investigating, not re-running.
- **Tier 3 silent client-JVM hang → 35-min step timeout (first seen 2026-08-05, v0.9.1 release commit).** The Client GameTests job can fail with NO assertion, NO crash report, and no LSS/vanilla error: the client log simply stops (that instance: right after an `LSS-CacheIO` "Saved N cached column entries" line, world-teardown screen churn just before) and GitHub kills the step at its 35-minute timeout. Both attempts of the run redded this way while the BYTE-IDENTICAL tree had passed Tier 3 three times the same day in ~2 min each (both PR runs + the prior main commit). Recurred 2026-08-08 (PR #109: mute stop right after the session-config receipt, 15:04→15:38 kill, no crash-reports; rerun green first try — and the merge commit's own main run was green). Diagnosis: an environment-class headless-client freeze on a shared runner, not a code regression — the decisive signature is the silent log stop with an empty crash-reports dir in the `gametest-failure-evidence-client` artifact (a real regression fails a test or crashes; it does not go mute mid-teardown). Action: `gh run rerun <id> --failed` after confirming that signature (the rerun was green first try). Note `release.yml` runs `-x runClientGameTest`, so this can never block a publish — only the pre-tag green-check.
- **Vanilla `TicketStorage.purgeStaleTickets` NPE (Tier 3) — upstream MC 26.2 crash under ticket churn.** The integrated server can die with `NullPointerException: ... "this.wrapped" is null` in `Long2ObjectOpenHashMap$MapIterator.nextEntry` via `TicketStorage.removeTicketIf` ← `purgeStaleTickets` ← `ServerChunkCache.tick` (first seen 2026-07-10, run 29101338616). Investigated: every LSS ticket call is main-thread-only (`ChunkGenerationService` — submit/timeout/extract/removePlayer/shutdown all inside `tick()`), and LSS's generation ticket has no timeout so the purge never touches it; 26.2's `removeTicketIf` bytecode fires the `ChunkUpdated.update` callback *mid-iteration* of the fastutil ticket map, so any synchronous downstream ticket mutation corrupts the iterator — a vanilla fragility that Tier 3's generation-driven ticket churn makes likely to trip. Not fixable in LSS; the CI retry contains it. Action: if it recurs, pull the crash report from the `gametest-flake-evidence-client` artifact and confirm the stack is still vanilla-only before suspecting LSS.
- **Soak checker (`check_soak.py`) — benign timing / light-settle drift on constrained boxes.** Occasional dirty-resave / quiescence drift is tolerated by design (the checker carries settle tolerances); treat as a re-run, not a rewrite.
  - **`paper-store-unfired-event` premise red after a base-world reroll — world-gen pack animals grazing (diagnosed 2026-08-08 in one run via the dirty-mark trace).** A regenerated `soak-worlds/base-paper` saves its INITIAL pack spawns (world-gen packs ignore the later `doMobSpawning false`, which only stops new spawns); sheep near spawn then drive a graze/regrow cycle (`EntityChangeBlockEvent` grass→dirt + `BlockSpreadEvent` dirt→grass) that drips ambient dirty marks and reds the scenario's event-quiet premise. FIXED structurally: the timeline now kills non-player entities at t+2 (`kill @e[type=!minecraft:player]`), and the checker premise is WINDOWED to the edit (ambient marks outside [setblock .. save-all+10 s] are tolerated; a mark INSIDE the window is still a premise red). Diagnostic lever: `SOAK_DIRTY_TRACE=true` (→ `-Dlss.soak.dirtyTrace`) logs every mark-driving Bukkit event + its extracted shape — reach for it FIRST on any Paper ambient-dirty mystery.
  - **`A7` "disk.errors increased" on a memo-enabled fresh-backfill — the miss memo's generation acceleration starving background reads (diagnosed by A/B 2026-07-19).** With `missMemoTtlSeconds` > 0 (default 30), fresh-backfill on constrained boxes (WSL2, concurrent servers) can red A7 with 10-15 timeout-triaged `disk.errors`: the memo removes the churn reads that accidentally paced generation admission, generation runs at full caps, and its save traffic monopolizes the single-threaded vanilla IOWorker that `useBackgroundReadPriority` reads deliberately queue BELOW — a few real reads exceed the 10 s timeout. Decisive signature (counter-based — raw `TimeoutException` stacks no longer print; timeouts log as a throttled `"Disk read timed out"` WARN whose `+N more` aggregation and 60 s window make line counts under-count): `disk.errors` increased, `grep -c "Failed to read chunk" server.log` == 0 (every error was a timeout — non-timeout read errors still log that line with stacks), and **A5 exact** (the `disk.memo_hits` term closes it). Every timeout self-heals via the not-found ladder. Action: it is the documented memo tradeoff (`docs/planning/miss-memo-design.md` A/B finding), not a regression — verify with the kill-switch A/B (`"missMemoTtlSeconds": 0` in the scenario config) if in doubt. If A5 ALSO fires, it is a different problem. Since frontier outward damping (2026-07-19) the same box measures `disk.errors = 0` on fresh-backfill (the damper's admission cadence removed the IOWorker monopolization) — this entry stays for constrained-box variance, but a red here on a post-damping build deserves a second look before being written off. **The second look that settles it is a same-day BRANCH A/B, not another re-run** (established 2026-07-29: two consecutive post-damping reds at +10/+5 timeout-errors looked like a regression on the branch under test until the known-green base branch, run minutes later on the same idle box, redded at +50 with fewer quiescent windows — the box's WSL2 IO day-state was the variable, and the branch was exonerated in one run). The errors cluster at the END of the cold-generation wave (reads submitted during the gen-save flood time out 10 s later), matching this entry's mechanism; timing them via the snapshot deltas is part of the check. **The NBT transcode (round 2, 2026-07-29) intensifies the same tradeoff on a degraded box, kill-switch-verified:** on an evening when the box's WSL2 IO ran soak reads at 107-131 ms avg (vs ~2 ms healthy), fresh-backfill with `useNbtTranscode` on redded A7 three times at EXACTLY +5 = `diskReaderThreads` (one >10 s IOWorker stall event expiring all five blocked readers at once, mid-gen-wave), while a `"useNbtTranscode": false` control run between them was green and the Paper twin (Moonrise IO, no vanilla IOWorker) was green through the transcoder — the freed reader-pool CPU is another memo-style acceleration of the gen-save flood, NOT byte drift (A1-A6 + all client laws stayed green in every red run; a transcoder byte bug would fire those, never A7). Same triage as the memo entry: counter check first, then the `useNbtTranscode`/`missMemoTtlSeconds` kill-switch A/B if in doubt. (Note for any memo A/B since 2026-08: `missMemoTtlSeconds: 0` baselines recorded BEFORE the adaptive scan cadence understate the churn-read rate the memo now absorbs — the client re-declares up to 4 Hz, so record fresh baselines rather than comparing against pre-cadence runs.) **The `SOAK_DIALECT=19` legacy-emulation lever (C2, 2026-08-08) is another mild intensifier of the same mechanism on a constrained box:** the legacy egress adds per-column processing-thread work (v20→native translate + zstd recompress), which can tip a marginal WSL2 IO window into the timeout storm — two consecutive lever reds at exactly +10 timeout-triaged errors (0 non-timeout, A5 exact) were settled by the catalog's branch-A/B rule extended to a 4-arm matrix: base no-lever PASS, same-branch no-lever PASS, then the SAME lever arm PASS with 0 errors minutes later on byte-identical code. Same triage: counter check first, then arms (lever off = the kill switch), never a code hunt while A5 stays exact.
  - **`A7` "disk.errors increased" + `law-coverage` "fewer client-laws windows" together — environmental read-timeout storm on a loaded box (diagnosed 2026-07-17).** When the machine is under heavy IO load (e.g. concurrent Gradle builds/soaks/gametests, or WSL2), LOD disk reads can exceed `DISK_READ_TIMEOUT_SECONDS` (10 s) and throw `TimeoutException`, which increments `disk.errors` (A7 flags any increase) and disrupts convergence enough that fewer quiescent windows form (tripping the client-law coverage floor). **Decisive check** (counter-based since 2026-07-19 — timeouts log as a throttled `"Disk read timed out"` WARN, not a `TimeoutException` stack, and the `+N more` aggregation means line counts under-count): `disk.errors` increased while `grep -c "Failed to read chunk" <run>/server.log` == 0 — every error was a timeout (non-timeout read errors still log that line with a stack trace). A clean run has `disk.errors=0`. Critically, if `A5` also fires it is a DIFFERENT problem: A5's fold `d_nf += disk.errors` assumes a timed-out read lands on the not-found ladder (gen submit / miss_dropped), so an A5 imbalance == the timeout count means the timeout-triage path changed (see the v0.7.1 disk-timeout item in `docs/planning/v16-compat-design.md`). A7+coverage WITHOUT A5 = environmental, re-run on an idle box; the fresh-backfill history is overwhelmingly green. **"Idle" includes no leftover test servers:** 2026-07-28, two consecutive fresh-backfill A7 reds (25 and 40 timeout-triaged errors, exact documented signature) were six forgotten `lss-multi-test` servers (~7.7 GB heap, six ticking JVMs) from a prior session — `pgrep -af 'lss.multitest\|test-server'` before blaming code or re-running; both reds vanished once they were stopped.
  - **`dimension-trip` "dimension segment does not end verified-quiescent" — snapshot phase-drift artifact (diagnosed 2026-07-15).** The segment-end law (`check_soak.py:990`) requires the segment's LAST client row to be in `quiescent_client`, but `find_quiescent` only ever files a QPoint at the client row *nearest in wallMs* to a server snapshot. When the client and server 5 s cadences drift out of phase (routine at TPS ~14-19 on a loaded box), the segment's final row can sit in a gap where a neighbour is always closer — so it is **structurally unable** to be marked quiescent no matter how idle the system is. Seen on a Paper run where the boundary was provably converged (client `tracker_in_flight=0`, `queued=0`, `received_columns == requested_total` flat for 15 s; server counters unmoved across 4 snapshots); it passed clean on re-run. **Decisive check before blaming code:** for the failing row index `hi`, ask whether `hi` is ever `min(|client.wallMs - server.wallMs|)` for any server snapshot — if never, it's this artifact, not a convergence failure. Action: re-run.
  - **Law A5 — latent false positives on PERMANENT generation failures and boundary ticket drops (documented 2026-07-16, direction corrected 2026-07-18, never yet seen live).** A5 balances `disk.not_found` against `generation.submitted + not_generated + service.miss_dropped`. A permanent generation failure (extraction error / Moonrise null chunk) lands on BOTH right-hand terms for one miss — `generation.submitted` at submit AND the client's `responses.not_generated` when the answer arrives — so while the client is connected the RHS OVER-counts by one per failure; a disconnect before the answer rebalances it. Same family, opposite direction: the two generation-ticket drop paths in `drainGenerationTicketRequests` (state gone / dimension mismatch, both platforms) skip `miss_dropped`, so a near-boundary drop on `dimension-trip` UNDER-counts the RHS by one. No current scenario produces permanent gen failures (they require real extraction errors, not config), so an A5 red today is a code regression first; check whether the imbalance equals a small count of server-side generation errors or near-boundary ticket drops before chasing conservation. Also noted in `law_A5`'s comment in `check_soak.py`.
  - **Law A1 — latent false positives on lost sends (documented 2026-07-28, never yet seen live — the A5 latents' A1 siblings).** Three shapes count `requested_total` (or consume a disposition) with no balancing term: a client transport send that THROWS still counts the batch (count-at-send is pinned), a server batch-frame send failure loses up to 4096 dispositions, and a post-/reload declaration landing in the unregistered window before the re-attach prompt heals it (Paper). All are dying-connection/boundary shapes the quiescent-pair windowing excludes. An A1 red whose imbalance equals one batch or frame is one of these — re-run, don't chase conservation. Related observability hole, same family: a dirty NOTICE whose send fails after its clear/invalidation applied is invisible to every counter (`dirty.marked_total`/`dirty.broadcast_positions` book the drain as done) — a still-connected client keeps stale LOD until the next edit or rejoin, and a soak cannot distinguish that from a healthy quiet run. Full list in `law_A1`'s docstring.

## Local Test Servers (manual play)

```bash
./test-server.sh              # set up + run all three: Fabric :25564, Paper :25566, Folia :25567
                              # (:25565 stays free for the soak/benchmark harness — a test server
                              #  there is indistinguishable in the server list and an accidental
                              #  join contaminates a running soak)
./test-server.sh run-folia    # one platform only (also run-fabric, run-paper)
./test-server.sh run-fabric-no-c2me  # Fabric with c2me*.jar parked as .disabled (A/B vs C2ME; run-fabric re-enables)
./test-server.sh run-fabric-store    # run-fabric with the LOD store ON (lodStore=on) + the background
                                     # backfill (lodStoreBackfill=true auto-walks regions) — warm rejoins
./test-server.sh run-paper-store     # run-paper with the LOD store ON; store DB at <world>/lss-lod/
                                     # (no backfill on Paper — Fabric-only; warms from serves)
./test-server.sh update       # rebuild + reinstall the LSS jars into all three (restart to apply)
./test-server.sh clean        # delete test-server/
# The staged lss-server-config.json now carries the SHIPPED DEFAULTS and nothing else —
# it used to hand-write nine tuned values (distance 64, 8 MiB/player, diskReaderThreads 8,
# sendQueueLimitPerPlayer 9600, generation caps 40/40) that predated the config review, so
# the rig tested a configuration no player runs. Only genuinely rig-specific keys are
# written; everything else falls through to the mod's defaults.
# Knobs (the staging REWRITES the config each run, so hand-edits don't survive — these are
# the supported way): LSS_LODSTORE=off|on|full (default on — the shipped default; on==full),
# LSS_LODSTORE_BACKFILL=true|false
# (default true) — both follow the shipped defaults; LSS_LODSTORE_BACKFILL_CPS (unset =
# server default 500); LSS_MOVE_TRACE=1 stages config/lss-move-trace.enable on the Fabric
# server (the tracer's vanilla-rung environment; rows land in
# test-server/fabric/logs/lss-move-trace.jsonl — default 0, marker removed when unset);
# LSS_LOD_DISTANCE (unset = shipped default 512 since the 2026-08-08 config rework — the
# rig used to hardcode 64, and 512 is 64x that area, so set 64/96 on a small box or when
# running all three servers); LSS_VIA=1 stages ViaFabric+ViaBackwards (Fabric) / ViaVersion+ViaBackwards
# (Paper) via Modrinth-API resolution — the C5 Via mismatch guard's live rig (run-fabric-via
# = the convenience case; old-MC clients live in the lss-multi-test Prism profiles), with
# LSS_VIA_GUARD=0 as the guard's kill-switch A/B (writes enableViaMismatchGuard=false into
# the staged config — hand-edits get clobbered, use the knob; default 1). Every non-via run
# parks the Via jars so the baseline stays Via-free.
# run-fabric-store / run-paper-store FORCE the store on (immune to
# LSS_LODSTORE=off) and otherwise match plain run-fabric / run-paper now that on is default.
```

Downloads real server jars (Fabric launcher; Paper/Folia latest stable via fill.papermc.io),
installs the built LSS jars (Folia gets the same Paper plugin jar), and stages
offline-mode config under `test-server/<platform>/`. For joining with a real client and
eyeballing LOD behavior — the automated gates are the test tiers and the soak harness above.

## Architecture

### Entry Points

- **Fabric:** `LSSMod` — server initializer: registers payloads, starts `RequestProcessingService` on dedicated servers
- **Fabric:** `LSSClient` — client initializer: sets up client networking, commands, mod compatibility
- **Paper:** `LSSPaperPlugin` — plugin entry point: registers Plugin Messaging channels, starts `PaperRequestProcessingService`

### Networking Protocol (wire v20, the v17 design line — declarative want-set, server-owned generation)

Batch model with 6 payload types. Fabric uses `LSSNetworking` with Fabric `StreamCodec`;
Paper uses `PaperPayloadHandler` with raw `FriendlyByteBuf` encoding. Both produce identical wire format.

**C2S:** `HandshakeC2SPayload` (capabilities bitmask) → `BatchChunkRequestC2SPayload` (batch of packed position + clientTimestamp pairs — the client's **complete want-set**, not an increment; ts `>0` = resync "send if newer", `<=0` = "I have nothing" — there is no generate-request shape, the client never classifies)

**S2C:** `SessionConfigS2CPayload` (4 fields: protocolVersion/enabled/LOD distance/generation toggle — the concurrency caps are server-internal and left the wire) → `VoxelColumnS2CPayload` (coords + dimension + columnTimestamp + one-byte serve-source tag `COLUMN_SOURCE_*` in_memory/disk/generation/store — diagnostic attribution for the client trace; unknown values pass through verbatim; added in protocol 18, still the v17 design line — + one-byte codec tag `COLUMN_CODEC_RAW/ZSTD` added in protocol 19 (zstd-1 column frames for capability sessions; stripped at egress for v18-compat sessions) + MC-native sections) → `BatchResponseS2CPayload` (batch of responseType + packed position pairs — up-to-date and not-generated) → `DirtyColumnsS2CPayload` (server-pushed change notifications)

**The want-set model.** Each scan the client declares *every position it still wants*, closest-first, in one batch; the server **replaces** that player's backlog with it. The scan cadence is **adaptive** (docs/planning/adaptive-scan-cadence-design.md, `enableAdaptiveScanCadence` default true): a 20-tick (1 Hz) fallback that is also the self-heal cadence, plus a completion-triggered fast re-scan — when the last declared batch is ≥95% answered (`InFlightTracker.size()` vs the declared count), the NEXT walk is predicted cheap (`predictedWalkCost() <= FAST_RESCAN_MAX_WALK_COST` = 65536 ring positions, derived from `confirmedRing`/`scanRing` — a from-ring-0 re-walk across a disc satisfied far out still rides 1 Hz). **This term used to be `confirmedRing > 0`, which was a PROXY for walk cost and measured movement instead**: `recenter()` zeroes the prefix on every chunk crossing (~2.76 Hz in flight) while nothing re-derives it until the next walk, so the fast path was structurally inert for the whole of any sustained movement — measured as exactly 1.000 s scan gaps for 23 consecutive seconds of elytra flight while the same client ran 2–3 Hz standing still (docs/planning/elytra-chunk-wall-investigation-2026-08-01.md §8.6.3). The prediction is deliberately NOT a memory of the last walk: the two differ exactly at the movement transition, so a remembered cost would admit one full-price walk after every prefix collapse. **Coverage limit, decided and accepted 2026-08-02:** because `recenter()` zeroes the prefix, under sustained movement the prediction reduces to `4·s(s+1)`, which the 65536 budget admits only for `s <= 127` — so the fast path covers rings 0-127 while moving (~a quarter of a 256-distance disc; ~1/16 of the default-512 disc since the 2026-08-08 rework), and a MOVING client returns to 1 Hz once the frontier passes ring 128. Stationary clients are unaffected. This is a PARTIAL fix to the elytra wall and the conservative direction the investigation argued for; do not claim '4 Hz while flying' unqualified, and note that raising the constant only moves the cliff — extending coverage means decoupling the prefix from `recenter()`), pressure is mild (decode queue and consumer ingest backlog each below ¼ of their halt thresholds — proportional, NOT strict zero, or the gate would be inert for data-bearing backfill), and the session is not v16, the scanner fires after as little as 5 ticks (250 ms floor → max 4 Hz). Arming lives in `tickScanPhase` beside the tracker replace (`noteDeclared`); a 0-count walk, a send failure, `disconnect()`, and the reset family all disarm — a converged client still sends NOTHING and re-walks at 1 Hz only. A position drops out of the want-set only when it is SATISFIED. Consequences that are load-bearing, not incidental:
- **Re-declaration is the single self-healing mechanism.** There is no client drip-feed, no in-flight timeout sweep, and no rate-limit backoff — all deleted. The server may silently drop any not-yet-admitted ask (backlog replace, mailbox overwrite, Folia hold CAS loss, residual disk saturation); the next declaration (≤1 s away — the fallback bounds it) heals it. Silent drops never remove positions from the client's awaiting set, so churn-dominated phases (drops above the 5% completion threshold) keep outstanding high and hold the cadence at 1 Hz — the profile the soak churn ceilings were measured against; a sub-5% drop rate legitimately runs fast. Every such drop is counted (`service.superseded` / `service.range_filtered`).
- **The scanner must NOT suppress awaited positions at send.** An awaited position is an ordinary unsatisfied want-set member and blocks its own ring's confirmation until data lands. Re-introducing send-time suppression re-opens a 10s-class stall against the server's silent drops.
- **`RESPONSE_UP_TO_DATE` / `RESPONSE_NOT_GENERATED` are the only things that empty the want-set.** They are what makes `ColumnStateMap.classify` return SATISFIED. Never delete them. `NOT_GENERATED` is **permanent for the session**: the client adds the position to `sessionSatisfied` (no 0-stamp — legacy cache 0-stamps re-declare as -1) and never re-asks, even on movement; the ONLY revival is a dirty broadcast. It fires ONLY on permanent unservability (generation disabled, or a hard generation failure) — **never for transient pressure**: a full gen slot, gen-service capacity, a generation timeout, a submit no-op, or a ghost delivery all drop silently (counted `superseded`, disk-miss flavors also `miss_dropped`) and heal by re-declaration. On a gen-enabled server it never fires at all (pinned by the `generation-capacity-stress` soak: `not_generated == 0`).
- **At convergence the client sends NOTHING** — not an empty heartbeat. Soak quiescence depends on `service.requests_received` going still. The one exception is a single **edge-triggered empty batch** when the client crosses into backpressure halt — its decode queue mostly full (count or bytes), OR a consumer-reported ingest backlog at `INGEST_BACKLOG_HALT_SECTIONS` (issue #71: Voxy's unbounded ingest queue is invisible to the decode-queue signal; consumers report depth via `VoxelColumnConsumer.pendingIngestBacklog`, docs/planning/ingest-backpressure-design.md): it clears the server backlog so no work is done for a client that cannot ingest it.
- **Byte 0 is retired and reserved.** It was `RESPONSE_RATE_LIMITED` through v16; UP_TO_DATE(1)/NOT_GENERATED(2) were deliberately *not* renumbered, and the codec ships unknown types unaltered (pinned) so byte 0 stays forward-safe.
- **v17 is a breaking protocol bump — with legacy shims on BOTH sides.** A v17+ client against a v16 SERVER gets a translated session through the **client-side v16 compat shim** (`enableV16ServerCompat`, default true): a timeout-driven discovery re-handshakes as version 16, decodes the legacy 6-field SessionConfig + source-less columns, and by default drives on-demand generation on the old server (Tier B, `enableV16Generation`, default true; live-confirmed against v0.4.x–v0.6.2). Design: `docs/planning/v16-client-compat-design.md`. A v16 client against a v17+ SERVER gets a translated session through the **server-side v16 compat shim** (`enableV16Compat`, default true — `common/compat/V16CompatManager`): the handshake gate's `WireDialect.V16` rung replies with the legacy 6-field SessionConfig echoing version 16, ingress merges the old drip-feed into a per-player synthetic want-set (1 Hz sole declarer, sorted closest-first, 75 s TTL, overflow bounced with the revived byte 0), and the column egress strips the source byte. The pipeline never learns v16 exists; with no legacy clients every shim path is a no-op. Design + both review rounds: `docs/planning/v16-compat-design.md`.
- **Protocol 19 (v0.9.0, compressed columns) additionally has a server-side v18 compat RUNG** (`enableV18Compat`, default true, since v0.9.1 — since C1 tracked by `common/compat/WireDialectTracker` (the generalized per-dialect membership tracker), docs/planning/v18-compat-design.md): a protocol-18 client (the v0.7.x–v0.8.x install base) gets a NATIVE session, not the v16 fallback its 5 s discovery timer would otherwise degrade it to. Membership-only — the 18→19 wire delta is one S2C byte: the gate's `WireDialect.V18` rung echoes 18 in the CURRENT 4-field SessionConfig (the old client's gate hard-requires its own version), registration forces `wantsCompressedColumns` false (a v18 layout has nowhere to carry a codec byte), and the per-player column egress strips exactly the codec byte (`asV18()` / `rewriteColumnToV18` — a leaked codec byte is read as the section-array length VarInt and hard-kicks the client). C2S is byte-identical, so ingress and the whole want-set pipeline never learn the session is legacy. Identity lives at the network level (marked before registerPlayer — Fabric inline main-thread, Paper ONLY via the pump's dialectFlip; dropped at disconnect + Paper's quit-originated mailbox Remove, the quit-race leak guard) and SURVIVES the dimension-change remove+register cycle. `/lsslod diag` shows the per-dialect membership on the `Dialects: v20=…, v19=…, v18=…` line (`WireDialectTracker.diagLine` — the old `V18Compat:` line is gone). Client-side there is deliberately NO v18-server rung — a v0.9.1 client on a v0.8.x server still degrades via the v16 discovery fallback.
- **Protocol 20 (v0.10.0, cross-MC-version columns — the XVER program, docs/planning/ cross-version design set)**: column bodies carry an **identity dictionary** (block-state/biome IDENTITY strings, first-seen order) + per-section palettes of dictionary indices instead of registry ids, so a column decodes on ANY supported MC version (`common/wire/` — `IdentityCodec`, `IdentityDictionary`, `WireSectionCursor` parse/emit for NATIVE and V20 layouts, `NativeToV20Translator`/`V20ToNativeTranslator`, all corpus-golden-pinned). Produce paths: the live path (SectionSerializer) emits native bytes and translates at the return boundary (the C1 translate-at-producer decision); the DISK transcode path emits v20 DIRECTLY from the memo-resolved descriptors (`emitV20Direct`/`NativeToV20Translator.convertIndexed` — the C6-gated follow-up; byte-identical by the indexed rule, `DIRECT_V20_EMITS`/`direct_v20=` diag receipt, mixed/fallback/masked columns keep the translate route). Server legacy rungs: **v19** (`enableV19Compat`, default true — v0.9.x clients get their native zstd-frame wire via memoized enqueue-time translation), v18, v16 (all through the shared `WireDialect` gate). Client ladder: v20 native → v19 → the v16 discovery fallback; unresolvable identities render via `unknownBlockFallback` (default `minecraft:stone`) / `crossVersionBlockFallbacks` (per-block map, client config), reported through the ingest-failure containment. The **Via mismatch guard** (`enableViaMismatchGuard`, default true — C5) declines LOD sessions for Via-translated different-version clients running an OLDER LSS (the gate fires on legacy-dialect selection — a vanilla client never handshakes); a PROXY-side Via (Velocity/Bungee) is invisible to it (documented hole). Store schema 4: rows hold v20 bodies; a schema-3 store migrates via a paced background walk (lazy 3→4, all-air rows retag rather than delete), never a rebuild; wire/store bytes measured ~+10% (C0 settling: zstd frame +9.7%).
- **The server owns generation.** The disk miss IS the generation trigger: a cold column always reads disk first (one route, one SYNC slot), and on not-found the server generates if generation is enabled and a GENERATION slot is free — no request shape can force or forbid it. `ts == 0` (the retired v16 generate-request) routes identically to `-1`, as inert as the retired wire byte 0.
- **No client budget derives from any server cap.** The scanner declares the constant `WANT_SET_BUDGET` (800) per scan; the old Global Constraint #28 cross-clamp is gone. Its successor is a static inequality pinned by `WantSetBudgetInvariantTest`: `SYNC_ON_LOAD_SLOT_CAP (200) + MAX_CONCURRENT_GENERATIONS (512) + WANT_SET_FRONTIER_RESERVE (64) <= WANT_SET_BUDGET <= MAX_BATCH_CHUNK_REQUESTS (1024)` — the want-set dominates the worst-case in-flight set with frontier headroom, re-derived at build time if any constant drifts.

Backpressure lives in three places now, none of them on the wire: the bounded want-set (the constant `WANT_SET_BUDGET` = 800, inside the `MAX_BATCH_CHUNK_REQUESTS` = 1024 wire cap), the server's per-player dequeue gates (a full slot cap or disk pool *retains the entry in the backlog* — it never bounces), and the client's edge-triggered clear above.

Requests are keyed by packed chunk position end-to-end (no request ids): every response is a statement about a position, so late or duplicate responses are idempotent, and a column the client no longer tracks is still ingested. Delivery honesty: the server's per-player served-set (diskReadDone) answers re-declarations with up_to_date only when the client claims data (ts>0); a ts<=0 re-declaration declares "I have nothing" and re-resolves honestly (skipped silently while the payload is still in the send pipeline, deliberately without clearing the done-bit — see `IncomingRequestRouter.resolvedAsDuplicate`), so any post-resolution loss — send-queue drop, decode failure, consumer rejection — self-heals instead of becoming a permanent hole. The **duplicate-serve grace** (docs/planning/duplicate-serve-grace.md) extends the silent-skip window ~500 ms past the payload's send-success departure (`SEND_DEPARTURE_GRACE_MILLIS`; stamps live in the state, so they die with it on removal/dimension change): a ts<=0 re-ask that crossed its own delivery in flight (~7-8% of cold-backfill asks) is absorbed (counted `duplicate_skips` — law A1's disposition term — plus the `grace_skipped` observability subset) instead of costing a redundant read+send; past the grace the clear-and-re-resolve applies unchanged, so a genuine post-send loss heals at most one scan later. Send-FAILURE drops never stamp — their done-bits are proactively cleared, keeping that loss class instant-heal. Consumers report rejected columns via `LSSApi.reportIngestFailure`, which forgets the client's received-stamp so the next scan re-declares it.

Flow: client handshakes with capabilities bitmask (CAPABILITY_VOXEL_COLUMNS set if LSSApi has consumers) → server sends the 4-field session config → client scans in an expanding spiral (adaptive cadence: 1 Hz fallback, up to 4 Hz fast re-scans while the pipeline drains fast) and ships its whole want-set with clientTimestamps (<=0 for "I have nothing", >0 for resync) → the batch lands in the player's latest-wins mailbox and, on the processing cycle, **replaces** the backlog (superseded entries counted, out-of-range ones range-filtered) → the router drains the backlog in declaration order (proximity-ordered by the client): up-to-date checks, loaded-chunk probe, then for a cold column `tryAdmitAndSubmit` — every entry takes a SYNC slot and reads disk; if the slot cap is full the entry is **retained and the drain continues**, and if the disk pool has no headroom it is retained and the pass **stops** → a disk miss escalates server-side (`handleDiskNotFound`): gen enabled + a GENERATION slot free swaps the entry into a generation ticket; gen slot full drops silently (`superseded` + `miss_dropped`, re-declared next scan); gen disabled answers `NOT_GENERATED` (session-permanent) → server batches lightweight responses (up-to-date, not-generated) per tick; column data payloads are sent individually. After the initial scan, the server periodically pushes `DirtyColumnsS2CPayload` listing changed columns; a dirty position re-enters the client's want-set on the next scan — for a `NOT_GENERATED`-parked position this is the ONE revival path (on Fabric a first-time vanilla chunk save fires it; on Paper walk-in generation does NOT mark dirty by default — `ChunkPopulateEvent` is an opt-in `updateEvents` entry — so a parked position there heals on reconnect). One accepted corner rides this: with generation disabled, a LOADED but never-saved chunk whose probe was deferred by the global probe cap disk-reads as not-found and parks `NOT_GENERATED` despite being live in memory — healed by its first save's dirty broadcast or reconnect (see the `MAX_PROBES_PER_TICK_GLOBAL` comments in both services). Since the 2026-08-05 review round a probe suppressed inside the 1.5 s post-serve window (`AbstractPlayerRequestState.probeSuppress` — a ts<=0 re-ask of a lost delivery landing between the 500 ms departure grace and that TTL) is a second, equally-healed entry path into the same corner.

### Backfill visual ordering — settled model, do NOT re-investigate (2026-07-16/17)

"LOD chunks load far-before-near / parts of rings missing" was root-caused across a full day of
live traces (`/lss trace`, source-tagged columns) and is CLOSED. The observable is the sum of
three layers — two irreducible, one platform-owned. Re-deriving this costs a day; read this first:

1. **Arc-fill geometry (irreducible, all platforms, scales with confirmed ring).** Ring r has 8r
   positions but only `generationConcurrencyLimitPerPlayer` tickets fly at once, so rings beyond
   ~cap/8 fill as sequential arcs; missing-arc time ≈ ring_size/gen_rate. Platform completion
   scramble (C2ME ≈ 8% stationary, measured via the `inversions` counter; Moonrise mild; vanilla
   ≈ FIFO) makes the arcs non-contiguous. Bounded by the order-spread gate to ~2 rings.
2. **Trailing view-edge crescents (irreducible, movement only).** Chunks inside the vanilla view
   circle are LOD-excluded; the circle moves with the player, so positions exiting it become
   LOD-needing for the first time and pop in NEAR (ring ≈ view distance, `src:0` probe serves)
   while the eye watches the far frontier — reads as inversion, is correct first-time service.
3. **The platform generating ahead of LSS (C2ME; the only tunable layer).** On a fresh world
   ~10% of the C2ME stream arrives `src:1` (disk) — terrain C2ME completed and saved BEYOND
   LSS's tickets (dependency pyramid / batch completion, notickvd where enabled), landing a
   median 5 (max ~18) rings ahead of the frontier. LSS finds it on the next 1 Hz re-declaration
   and serves it immediately — CORRECT (refusing to serve wanted, existing terrain is strictly
   worse). The lever is C2ME's config, not LSS code.

What WAS LSS's fault is already fixed and pinned — do not un-fix under performance pressure:
the movement cadence debounce (starved all declarations during flight), the spread-gate anchor
ladder (batch[0]-origin wedge → min-outstanding straggler leak → the shipped router-stamped
LIVE frontier), the disconnect bulk ticket release (60 s C2ME distance-graph freeze; staggered
via `DeferredTicketReleases`), and the vanilla-load scan budget scale (retired — server-side
priority owns that protection). Diagnosis instruments if something NEW appears: `/lss trace`
(client JSONL; `col` events carry `src` 0=probe/1=disk/2=generation) and `/lsslod diag`'s
`order_gated`/`inversions`. A far `src:2` beyond frontier+2 on a fresh world is an LSS gate bug;
far `src:1`/`src:0` is the platform — go tune C2ME, not the router.

Chunk coordinates are packed as `((long)chunkX << 32) | (chunkZ & 0xFFFFFFFFL)`.

### Common Module (`common/`)

- `LSSConstants` — protocol version, channel IDs, wire format limits, status codes
- `LSSLogger` — SLF4J logging wrapper
- `SharedBandwidthLimiter` — fair per-tick bandwidth allocation across active players (global + per-player caps)
- `PositionUtil` — chunk coordinate packing/unpacking
- `DiagnosticsFormatter` — formats diagnostic data for `/lsslod diag` output
- `ColumnTimestampCache` — in-memory per-dimension timestamp cache for up-to-date checks without disk IO. **Tile-based since v0.10.0 (D0, docs/planning/timestamp-cache-tile-redesign.md)**: one `int[1024]` tile per 32×32 region holding u32 epoch-second OFFSETS from `TS_EPOCH_SECONDS` (2025-01-01; slot 0 = absent), ~4.25 B/column dense vs the old 64 B/entry two-map shape (~13× denser; a full 256-distance disc's tiles fit ~1.6 MB). Every §2.2 clamp rounds UP or refuses to fabricate freshness (pre-epoch → offset 1, so a pre-epoch claim re-serves; ts<=0 clears the slot; overflow ~year-2161 saturates) — the safe direction is a redundant serve, never a false up_to_date. Eviction is tile-granular oldest-lastTouch under the byte budget (ctor takes `maxBytesPerDimension`, floored at one tile); persistence is record format v2 (4108-byte tile records, liveCount-validated) with in-place v1 migration on load. Also hosts the **miss memo** (docs/planning/miss-memo-design.md): a sibling per-dimension map of TTL-bounded authoritative disk misses (`missMemoTtlSeconds`, default 30, 0=off) so gen-waiting positions skip the redundant per-scan (1-4 Hz adaptive) not-found re-reads. `put()` (any serve) and `invalidate()` (edits) clear entries; `invalidateStamps()` is the disk-drain's stamps-only variant (a not-found delivery is the memo's own evidence — full invalidation there would erase every memo write); misses never persist across save/load
- `store/` — the **LOD store** (docs/planning/lod-store-implementation-plan.md + lod-store-progress.md, incl. the final 4-agent review round): `SqliteLodStore` (per-dimension SQLite tables of served wire bytes, zstd-1; WAL, thread-confined readers, single batcher writer, tombstoned deletes + immediately-committed DeleteRows — never batched into the shared txn, a later rollback would resurrect rows — startup + periodic freshness sweeps — STAT-DRIVEN since the review-fixes round: unchanged regions cost one mtime stat, changed regions re-read only their own rows via indexed pos-range seeks, vanished regions found by a ts-index key scan; a deposit clears its region's seen_mtime so post-judgement deposits re-examine — against region-header timestamps with `src_stamp` = byte-ACQUISITION second (read start / gen serialization, plumbed through `ChunkReadResult.srcStampSeconds` — deposit-call stamping left mid-read saves sweep-invisible), size-cap eviction with incremental_vacuum (OPT-IN since store-cap-behavior-plan.md — `lodStoreMaxMB` 0 = uncapped is the default; db + WAL-capped-at-64MB accounting; evicted regions' backfill marks reset; the eviction INFO latches to ONE line per session with totals in `/lsslod store status` `evicted=`; eviction loops batches per gauge tick so the cap actually BINDS, over an indexed ts scan, un-marking backfill regions BEFORE the row deletes), drop-and-rebuild on any unknown-schema/wire/mask/**registry** drift — EXCEPT the C4 schema 3→4 step (the v0.9.x→v0.10.0 upgrade), which MIGRATES in place: a paced background walk translates 19-rows to v20 (all-air rows retag), serves ride the 19-row inverse-translator rung meanwhile, never a rebuild (`registry_fingerprint in meta: id-ordered block-state + biome identity hashes via `common/store/RegistryFingerprint`, platform-supplied — a bare count was blind to same-size id permutations — mod/datapack changes shift the global ids stored bytes embed — DERIVED data: any drift outside the one migrated step just drops), `MemoryLodStore` (the SQLite-init degrade ONLY since 2026-08-02 — `lodStore=memory` was retired as a config value and now normalizes to `off`; ~12.5k resident columns at a fixed 64 MB under random eviction is ~6% of one disc, and the Phase 2 A/B had already deleted the tier from `full` for costing +14.6% CPU/col), `LodStores` factory (full→memory→null degrade), `StoreBackfill` (opt-in nearest-SPAWN region walk (`getRespawnData().pos()`, origin fallback), restraint-gated; done-marks wait for the deposit-queue drain + own-shed accounting + a DropAll generation fence; cap-aware — logs a walk-size estimate up front and HARD-STOPS at ≥95% of an active cap, unwalked regions left unmarked as the resume point). Serve rung lives in `AbstractChunkDiskReader` (hits serve STORED ts, excluded from disk.* counters and the adaptive throttle); deposits ride the delivery choke ONLY — the Fabric save hook is DELETE-only (a hook deposit provably never survived the fan-out its own mark triggers; the prompt delete closes the pre-edit-hit window); the dirty→store invalidation fan-out is the correctness backstop on BOTH platforms. `/lsslod store status|invalidate all` exist on BOTH platforms (backfill verbs Fabric-only; status carries `state=ok|sweeping|latched` — a latched store must LOOK dead — plus `mem=` tokens, which now appear only when a boot degraded to the in-memory tier); on Paper the x-ray mask manager activates inside `productionWiring` BEFORE the store Environment snapshots fingerprints (ctor-body activation fingerprinted every dim "off"). `store.queue` is a drain-side gauge (SERVER_DRAINS contract).
- `OffThreadProcessor` — abstract base for main-thread → processing-thread handoff (prepares pre-serialized column data off-thread, routes requests via `IncomingRequestRouter`). `handleDiskNotFound` carries the **generation order-spread gate** (`MAX_GENERATION_RING_SPREAD` = 2): a new generation ticket may enter at most 2 Chebyshev rings beyond the **client-declared frontier** — the applied want-set's first entry (nearest unsatisfied by closest-first construction), measured from the player's real chunk, stamped main-thread each lifecycle tick via `updatePlayerChunk`. Two wrong anchors preceded this (all 2026-07-16): the batch's first entry as the RING ORIGIN wedged the gate to ~97% refusals (the scan excludes the vanilla view distance, so that entry sits ON a ring perimeter), and the oldest OUTSTANDING ticket as the reference leaked — when the near band momentarily completed, a far corridor straggler re-anchored the window and legally admitted positions many rings past the frontier (fresh-server trace evidence). The frontier cannot be dragged outward by in-flight work. Pinned by `spreadGateAnchorsOnTheDeclaredFrontierNotOutstandingTickets` (the frontier anchor) and `generationRefillMustNotRaceBeyondTheOldestOutstandingTicket` (re-calibrated 2026-07-19 to the tighter unified-pacing invariant: refill holds at nearest-outstanding+1, so the cohort rule fires before the spread gate in that ladder). The reference is additionally **outward-damped** (`FRONTIER_OUTWARD_DAMP_MILLIS_PER_RING` = 333, ~3 rings/s; inward instant — movement-inversion fix, admission-trace diagnosed 2026-07-19): under movement the honest frontier is NON-MONOTONIC — at instants when the near field is momentarily all-satisfied it observes the far edge, and an undamped window admits far waves that complete AFTER the near work movement mints a moment later (the far-then-near "line pop" on stopping — every such admission was verified IN-window by `[lss-adm]` logs; the gate was honest, the observation was about to be falsified). Stationary backfill advances ring-by-ring inside the +2 window, which covers the damper's ≤333 ms reference lag (soak-verified: identical throughput — but "never waits" would over-round; each ring transition's reference advance can lag up to one interval); a long-idle stamp gap accumulates outward budget, so a teleport FROM IDLE into explored terrain self-heals immediately (a teleport during active backfill has no banked budget — far generation resumes after a few seconds of climb; reads/memoization proceed ungated meanwhile). Rigs disable damping (`setFrontierDampingForTest(0, …)`) — existing gate pins calibrate instant-outward semantics; the damping tests re-enable it with an injected clock, and `productionDefaultEnablesOutwardDamping` pins the wired nonzero default. Admission-order diagnosis instrument: `-Dlss.admissionTrace=true` (test-server.sh enables it on dev Fabric servers by default; `LSS_ADMISSION_TRACE=0 ./test-server.sh run-fabric` silences it — it is verbose during backfill) logs one `[lss-adm]` line per escalation decision (candidate ring, damped frontier, nearest in-flight rings, via, verdict). Without the gate, per-slot refill starves the nearest chunk while the frontier races outward on non-FIFO schedulers (C2ME). A gated miss is the standard transient drop (`superseded` + `miss_dropped`); `/lsslod diag` shows `order_gated` (gate refusals) and `inversions` (completions that finished while a nearer ticket was outstanding — the far-before-near evidence counter). Holds the invalidation-overtake stale guard: an edit that overtakes an in-flight disk read (tracked via its dedup group) or generation (tracked per-player in `generationInFlight`, swept on player removal) marks the buffered result stale so its delivery skips the timestamp stamp + `diskReadDone` and the client re-resolves. On a failed cycle, `requeueLosslessEvents` re-queues only the events whose phase did not complete (phase-completion flags) so a consumed generation outcome is never re-delivered un-tainted. Owns the server side of server-owned generation: `handleDiskNotFound` escalates any miss into a GENERATION slot + ticket when gen is available (gen slot full = silent drop), and `processGenerationReady` routes transient outcomes (timeout/capacity — `transientFailure` on `GenerationReadyData`) to silent drops while only permanent failures answer `NOT_GENERATED`. The **miss memo** rides this path: an AUTHORITATIVE miss (never an error/timeout triage — `ChunkReadResult.authoritativeMiss`, never a ghost) is memoized before the gen ladder, the router's memo rung (gated on generationAvailable, placed after the duplicate/timestamp/probe rungs) skips the redundant re-read via the shared `escalateMissToGeneration` helper (GENERATION slot only, counted `disk.memo_hits` — law A5's virtual not-founds), and EVERY drained generation outcome clears the entry before the per-player continues (plus the `put()` serve choke point). ALL generation admission — memo rung AND delivery path — runs the **pacing rules** ("generation never overtakes nearer in-flight work": nearer-pending-SYNC hold + `MAX_GENERATION_COHORT_SPAN` (1) beyond the NEAREST outstanding ticket, refusals counted `gen_order_gated`); the delivery path's old "emergent pacing" only held for stationary players with fast reads — unpaced stale deliveries under movement were the far-ahead isolated-chunk bug. The outstanding set is a RESTRICTION, never the window's anchor (straggler tightens, never widens — pinned). Every disk-miss silent drop counts `superseded` + `miss_dropped` (the dedicated soak-law-A5 term; timeouts count only `superseded` — their submission already balanced the miss). Residual disk saturation is a silent drop counted `superseded` and healed by the next want-set declaration — it no longer reaches the wire. Timestamp-cache saves route through `TimestampSaveScheduler` (issue #62): due saves publish to a latest-wins slot and each empty→full transition queues exactly one drain on the single-thread save executor — never one closure per save (the old shape backlogged ~42 MB snapshots without bound when a save outran the ~2 s invalidation-debounce cadence; the D0 tile snapshots are ~13× smaller, but the one-drain bound is what actually closed #62); `ColumnTimestampCache.save()/load()` are buffered (64 KB) so a save is no longer ~3M 8-byte syscalls. The 2026-07-28 four-agent review round (M1–M4, `docs/planning/review-majors-m1-m4-fix.md`) additionally landed: the client fill-pass ORDER (implicit-sky BEFORE the resync air-fill — the shipped v0.8.0 order silently disabled the sky fill on every resync delivery; sky-dimension 0-section clears now fill BRIGHT, vanilla's all-air-column rule), the per-player `diskReadDone` served-set sweep (ALL players each eviction cycle, radius = ingress filter + `SWEEP_RADIUS_MARGIN_CHUNKS`, keeps in-pipeline/grace entries, `trim()`s the backing array when a sweep removed more than remains — fastutil sets never shrink, so entry-count bounds alone left peak-size arrays resident; the set previously grew monotonically all session), the router's per-cycle drain-order rotation (fixed HashMap order + the global headroom gate starved later players of disk reads under saturation), and the 26.2 LAN hook retarget (the GUI calls the 2-arg `publishServer`; the 4-arg wrapper the port pinned never fired) + LAN service construction hopping to the server thread (all lines). The 2026-08-05 tri-lens review round (docs/planning/codebase-review-2026-08-05.md, executed per review-fixes-2026-08-05-plan.md with its three review rounds) landed: the client exclusion-radius-shrink rescan + clear-path ingest-strike dedupe + 8-chunk movement-prune hysteresis, the store reader-connection leak fixes + cross-dimension oldest-first cap eviction + sweepReopened rollback un-poisoning + per-thread reader statement cache, PROBE SUPPRESSION (`probeSuppress`/`skipProbe` — served or up_to_date-answered want-set heads no longer re-serialize every probe pass; stamps die at dirty broadcasts via the broadcasters' direct clear), the save-hook skip gate (Fabric: serialize+hash skipped only while never-registered + store-inert + the persisted timestamp cache BOOTED EMPTY — all three conjuncts load-bearing, pinned as a truth table), and the Paper lifecycle tightenings (v16 sweep drop, onDisable channel-unregister-first, volatile diag counters).
- `IncomingBatch` — immutable `record IncomingBatch(IncomingRequest[])`; the unit the client declares, the mailbox holds, and the backlog is replaced with
- `AbstractPlayerRequestState` — per-player state. Under v17 it owns the want-set path: a latest-wins `pendingBatch` mailbox (`offerIncomingBatch`/`takeIncomingBatch`, an `AtomicReference` — network/pump/processing safe; an overwritten batch is counted superseded), the processing-thread-only `ArrayDeque` **backlog** (`replaceBacklogWith`/`pollBacklog`/`restoreBacklog`, with a volatile `getBacklogSize` mirror for diagnostics), and `publishedWantSet`/`peekWantSet` — the probe's fallback source, which survives `takeIncomingBatch` and clears only when the backlog drains (`restoreBacklog` republishes; without that the steady state is ~zero probe coverage). Also hosts the duplicate-serve grace's departure-stamp map (`departedColumns` — stamped on send success in `flushSendQueue`, swept once per grace interval, dies with the state) and the served-set sweep (`sweepDiskReadDoneOutsideRange` — M3: culls `diskReadDone` beyond the ingress radius + margin, keeps in-pipeline/grace entries, trims the backing array; without it the set grew monotonically all session)
- `IncomingRequestRouter` — replaces each player's backlog with the newest declared want-set, then drains it in declaration (proximity) order. The PLAYER drain order rotates once per cycle (M4 — the snapshot map iterates in identical UUID-hash order every tick, and against the GLOBAL disk-headroom gate an unrotated order let the first player starve the rest of disk reads under saturation; within-player order unchanged): checks timestamps for up-to-date, probes loaded chunks, dispatches disk reads (every entry — the client no longer classifies; generation is decided at the disk miss), with cross-player dedup via `DedupTracker`. Nothing is bounced: a full slot cap **retains the entry and keeps scanning** (the cap means "dequeue at most N concurrently", not "reject above N" — entries behind a full slot can still resolve via the ladder/probe), no disk headroom **retains and stops the pass** (saturation never reaches the wire — issue #32 at its source), and a full send queue retains and stops. A submit no-op (level not yet registered) drops silently — transient conditions never answer the session-permanent `NOT_GENERATED`. Retained entries are re-prepended in order by `restoreBacklog`, which also republishes the want-set
- `PendingRequest` / `SlotType` — derived admission: a pending-map entry IS the held slot (no separate limiter object). Every entry holds `SYNC_ON_LOAD` until a disk miss escalates it to `GENERATION`
- `DirtyColumnTracker` — thread-safe tracker of per-dimension dirty chunk positions, drained by broadcasters
- `SendActionBatcher` — reusable per-tick accumulator for batching `SendAction` responses by player
- `HandshakeGate` — pure decision ladder for the C2S handshake (version mismatch → no reply; no consumer → reply without registering; disabled → advertise disabled; else register), shared by `LSSServerNetworking` and `LSSPaperPlugin` so reply/registration policy cannot drift between platforms
- `JsonConfig` / `ServerConfigBase` — GSON config base classes; `ServerConfigBase` holds the server config fields, defaults, and clamps shared verbatim by Fabric and Paper

### Fabric Server-Side (`fabric/`)

- `RequestProcessingService` — orchestrates per-player processing, ticks on server thread, broadcasts dirty columns
- `PlayerRequestState` — per-player request state, pending-request slot admission, send queue, metrics
- `ChunkDiskReader` — async region file reader with configurable thread pool. With `useBackgroundReadPriority` (default true) the per-submit ladder (`chooseReadPath`) is: **(1) the Moonrise rung** — when Moonrise-Fabric is present and `MoonriseReadCompat` resolved its IO API, reads route through `MoonriseRegionFileIO.loadDataAsync(..., intendingToBlock=false, Priority.LOW)` reflectively (the exact Paper read path — defers to gameplay's NORMAL, keeps read-your-writes, no adaptive throttle since LOW is the protection; consulted BEFORE the IOWorker accessor because Moonrise nulls the vanilla worker; observable as `read_path=moonrise-low` in `/lsslod diag`, docs/planning/moonrise-read-priority-design.md). A linkage/adaptation throw from the invoke (TYPED domain: `WrongMethodTypeException`/adaptation CCE/`LinkageError` — never Moonrise's own runtime throws, which stay per-chunk triage like Paper) latches a one-way `moonriseIncompatible` + warns once + serves via an execution-time inline foreground fallback (no `disk.errors` burst from queued closures), then degrades down the vanilla ladder. **(2) the vanilla IOWorker path** — schedules reads on the vanilla `IOWorker`'s own single-threaded `consecutiveExecutor` at BACKGROUND priority (below vanilla's FOREGROUND chunk loads) via `AccessorSimpleRegionStorage`/`AccessorIOWorker`. **Since v0.10.0 (perf Phase 3 / R1, `useBackgroundReadSplit` default true) the executor task is FETCH-ONLY**: it resolves the chunk's raw compressed record (`RegionFileRawRead` via `AccessorRegionFileStorage`/`AccessorRegionFile` — shadowed vanilla helpers, external `.mcc` bytes read on the executor) and zlib inflate + NBT parse run on the LSS reader pool (`NbtSectionSerializer.parseRawChunk`, mirroring vanilla's `createChunkInputStream` three branches; backfill reads split too, parse landing on the backfill thread) — the IOWorker used to carry pread+inflate+full-parse per LOD read and was the busiest serving thread ~7× per-thread (the A7 timeout-storm mechanism). `useBackgroundReadSplit=false` restores the full-read-on-executor closure (a rollback narrower than `useBackgroundReadPriority=false`, which also drops the Moonrise rung). The ordinal is pinned (`IOWorker$Priority` is package-private, and Fabric remapping rules out resolving it by name — a `SerializerParityGameTests` byte-parity test pins the path). This path skips IOWorker's `pendingWrites`, so it gives up read-your-writes; a just-saved column self-heals via dirty-broadcast. Flag false restores `chunkMap.read` (a true rollback — no throttle, no Moonrise rung either). **Chunk-IO-overhaul mods (C2ME's chunkio rewrite, and structurally similar) replace vanilla's single-threaded IOWorker with their own concurrent IO, leaving `consecutiveExecutor`/`storage` null on the vanilla shell — `backgroundReaderOrFallback` catches a null handle OR any `Throwable` from accessor resolution, latches a server-wide one-way `backgroundIncompatible` flag, warns exactly once (naming Moonrise instead of C2ME when that mod is detected), and falls back to `chunkMap.read` + engages the adaptive read throttle (Approach B — `AdaptiveReadThrottle`, which yields to gameplay via self-restraint by narrowing `hasHeadroom()`, so the want-set router retains the read and the client re-declares it). So LSS keeps gameplay protection on C2ME servers, not just correctness. These branches are unreachable from gametests/soak (both run plain vanilla IO); only a live C2ME/Moonrise server surfaces them, so `ChunkDiskReaderTest` pins the fall-back CONDITION as a pure predicate, the throwable-latch→enable-throttle→warn-once wiring via an injected throwing resolver, AND the full Moonrise-rung ladder (rung order, rollback, typed latch split, inline fallback) via injected bridge seams, with `MoonriseReadCompatTest` pinning the reflective resolution ladder against real-package-name stubs under `fabric/src/test/java/ca/spottedleaf/`; the throttle control law + the enable→narrow-`hasHeadroom`→recover wiring are pinned in `common/` unit tests. The DIRTY path is no longer the C2ME gap it used to be: since the issue-#69 retarget, dirty detection hangs off `SerializableChunkData.copyOf` (`ChunkSaveDataHook`), which C2ME's rewritten chunk system and Moonrise's save pipeline both call — the old `ChunkMap.save` hook was silently bypassed by C2ME's rewrite and fatally incompatible with Moonrise.** See `docs/planning/read-scheduler-design.md` §10.4.
- `FabricOffThreadProcessor` — extends `OffThreadProcessor`; main thread probes loaded chunks via `SectionSerializer`, processing thread prepares payloads and schedules sends
- `SectionSerializer` — serializes loaded MC `LevelChunkSection` + light data into wire-format bytes using MC's native `section.write(buf)`
- `NbtSectionSerializer` — region-NBT → wire bytes (used by `ChunkDiskReader`). Since round 2 (2026-07-30, `useNbtTranscode` default true) the default path TRANSCODES: palette global ids via the extended `MemoizedNbtCodec` memo + the disk long array verbatim, no `PalettedContainer`/`LevelChunkSection` ever constructed — byte-identical by the unpack-verbatim invariant, pinned by the object-path-generated goldens + a transcode-vs-object fuzz. Exotic shapes (>256-entry block / >8-entry biome palettes, malformed data, x-ray-mask-needing sections — the pre-gate mirrors `needsMasking` via `MaskSet.containsId`) fall back PER SECTION to the object path (codec parse, the permanent rung and the flag-off rollback)
- `ChunkGenerationService` — ticket-based chunk generation, triggered by the server on any disk-read "not found" (gen enabled + slot free). Timeouts are TRANSIENT outcomes (silent drop, retried via re-declaration); only extraction errors are permanent. Fabric generation has no priority hand-off (vanilla pins worldgen priority to ticket level 33) and no latency throttle (worldgen's variable baseline makes latency a poor congestion signal) — the concurrency caps are its honest limiter; a future adaptive backoff should key on tick health (MSPT), not latency
- `DirtyColumnBroadcaster` — periodically pushes dirty column notifications to connected players
- `DirtyContentFilter` — FNV-1a hash of served column bytes per position; chunk saves mark a column dirty only when LOD-visible content actually changed (filters vanilla's ~10s metadata-only re-saves, e.g. inhabitedTime). Fabric only — Paper's dirty detection is event-driven.
- `LSSNetworking` — `PayloadTypeRegistry` registration for all C2S and S2C payload types
- `LSSServerNetworking` — server-side event listeners, `RequestProcessingService` lifecycle (started via `ServerLifecycleEvents.SERVER_STARTED`)
- `AccessorServerChunkCache` (mixin) — exposes `ChunkMap` for disk reads
- `ChunkSaveDataHook` (mixin) — dirty-detection source: static @Inject at `SerializableChunkData.copyOf` RETURN with `require = 0` (issue #69 — the retarget from `ChunkMap.save`, which Moonrise @Overwrites into a throw-stub; that made LSS's injector match zero targets and fatally crash Moonrise servers at world load). `copyOf(level, chunk)` is the snapshot-for-saving choke point EVERY chunk system funnels through — vanilla's `ChunkMap.save`, Moonrise's `NewChunkHolder.saveChunk`, and BOTH of C2ME's save paths (whose rewritten chunk system never invokes `ChunkMap.save` at all — the old hook silently never fired there) — verified against decompiled vanilla 26.2 + Moonrise-Fabric 1.1.0 + C2ME 0.4.2 bytecode. Body in `LSSServerNetworking.onChunkSaveData` (mixin classes refuse classloading under fabric-loader-junit); may run OFF-main under C2ME/Moonrise — safe wherever `copyOf` itself is legal, `DirtyContentFilter`'s synchronization is load-bearing for it. Pinned by `SaveHookContractTest` (copyOf target + require=0 + vanilla shape)
- `IntegratedServerLanHook` (client mixin) — starts `RequestProcessingService` when an integrated server is published to LAN
- `dev.vox.lss.trace` — the **move-desync tracer** (docs/planning/move-desync-tracer-plan.md; ships in the release jar, fully inert by default): server-side observability for 26.2's three movement-rejection paths (`too_quickly`, `wrongly`, and the previously-invisible SILENT rejection) plus armed-player flight telemetry (1 Hz rows + a 5 Hz in-memory ring flushed ahead of events). Activation is `-Dlss.moveTrace=true` OR the marker file `config/lss-move-trace.enable`, read once at SERVER_STARTING — no config key, deliberately absent from the documented config surface (user decision); output `logs/lss-move-trace.jsonl` (rotate once at 128 MiB, 256 MiB total cap), validated by `scripts/check_move_trace.py`. Mixins live in their own **non-required** `lss-trace.mixins.json` (`@Accessor` cannot soft-fail — a required config would crash tracer-DISABLED servers on MC field drift); hook bodies delegate to `MoveDesyncHooks` and every capture is catch-all-contained. Chunk-delivery state resolves per JVM through `ChunkSendState` (Moonrise rung via reflective loader reads, vanilla rung via public `chunkSender.isPending`; moonrise-present-but-unresolvable = rung `none`, never a wrong answer). `/lsslod diag` shows a `MoveTrace:` line ONLY while active (rung + row/drop/event counters — the post-deploy verification and the silent-rejection rate in one call). Lifecycle hangs off `LSSMod`, never the service — control-arm rows (LSS disabled/unregistered, `lss:null`) are the point. The probe-snapshot refactor rode this change (mega plan R-2): `ChannelPressureProbe.snapshot()` default method + public `FabricChannelPressure`/`PaperChannelPressure` factories.

### Paper Server-Side (`paper/`)

- `PaperRequestProcessingService` — same orchestration as Fabric, sends via Plugin Messaging instead of Fabric networking; ticks on the GlobalRegionScheduler pump (main thread on Paper, the global-region thread on Folia) and drains a cross-thread lifecycle mailbox (`enqueueRegister`/`enqueueRemove` — handshakes and quits arrive on region threads on Folia) at the top of `tick()`. Since v0.8.0 a REGISTERING handshake's SessionConfig reply is a `Runnable` DEFERRED through `enqueueRegister` and run by the drain right after `registerPlayer` — replying inline from the handshake thread invited the client's first want-set into the pre-registration gap, where it was dropped uncounted (the Folia first-declaration A1 deficit of exactly `WANT_SET_BUDGET`; Paper had the same race one tick wide). Reply-only outcomes (NO_CONSUMER, disabled advertisement) still reply inline; a register racing shutdown discards its reply. On Folia only (`regionizedProbing = FoliaSupport.IS_FOLIA`, experimental) the loaded-chunk probe runs on the chunk's OWNING region thread via the player's `EntityScheduler` behind `Bukkit.isOwnedByCurrentRegion`, instead of on the pump — with a one-tick hold-release at **batch** granularity (`heldForProbe`, a `Map<UUID, IncomingBatch>`) so a batch and its region-published probe results meet in the same snapshot. The pump releases last tick's held batch back into the mailbox via `republishHeldBatch` (a `compareAndSet(null, held)` guarded by an **offer-generation counter** — bumped before every mailbox write, recorded by the pump before its hold take) **before** taking anything new, and a successful release ends the tick: taking first would empty the mailbox, make the CAS succeed unconditionally, and resurrect a batch the client has already superseded. A lost republish means a newer declaration arrived — still in the mailbox (the CAS catches it) or already passed **through** it, offered + taken by the processing thread during the hold (the generation guard catches it; the bare CAS found the mailbox empty and used to resurrect the stale batch) — and the held batch is dropped and counted superseded, never resurrected (one documented exception: a retract that fails because routing took the republished batch first routes one stale interval — see the second-guard comment). `RegionTaskScheduler`/`RegionOwnershipCheck` are injection seams for the JUnit tests
- `PaperPlayerRequestState` — per-player state adapted for `byte[]` payloads instead of `CustomPacketPayload`
- `PaperChunkDiskReader` — async disk reader using direct NMS `ChunkMap` access (no mixin needed). With `useBackgroundReadPriority` (default true) reads route through `MoonriseRegionFileIO.loadDataAsync(..., intendingToBlock=false, Priority.LOW)` instead: Paper/Folia gameplay loads run on Moonrise's own prioritised IO pool rather than the vanilla `IOWorker`, so IOWorker priority would be a no-op here and LOW is what actually defers to gameplay's NORMAL. Unlike Fabric's vanilla-IOWorker path this keeps read-your-writes (`loadDataAsync` serves in-progress writes — Fabric's Moonrise rung keeps it too). Flag false restores `chunkMap.read`. The live LOW path has no cheap unit test — the soak harness on real Paper/Folia is its gate (no `read_path` diag token here, deliberately: on Paper "flag on" IS the Moonrise path; the token exists for Fabric's live-only rung)
- `PaperOffThreadProcessor` — extends `OffThreadProcessor`; same main-thread/off-thread split as Fabric
- `PaperSectionSerializer` — Paper-side serializer using NMS `LevelChunkSection.write(buf)` + light data
- `PaperNbtSectionSerializer` — region-NBT → wire bytes (used by `PaperChunkDiskReader`); textual twin of `NbtSectionSerializer` incl. the round-2 transcode path + per-section object fallback (`useNbtTranscode`)
- `PaperChunkGenerationService` — async chunk generation via Moonrise `PlatformHooks.get().scheduleChunkLoad(..., ChunkStatus.FULL, addTicket=true, Priority.LOW, consumer)`, triggered by the server on any disk-read "not found": LOD generation DEFERS to player-driven NORMAL generation (the generation analogue of the `Priority.LOW` read path). The consumer delivers an NMS `ChunkAccess` with no throwable channel — null is the (permanent) failure outcome; timeouts stay transient. Serializes the generated chunk inside the completion (`completeAsyncLoad` — on Folia that is the owning region thread and the chunk can unload before a hop to the global tick) and hands only the immutable `LoadedColumnData` to the pump. The live LOW path has no cheap unit test — the soak harness on real Paper/Folia is its gate (the `Priority.LOW` argument is unit-pinned via the `launchAsyncLoad` seam)
- `FoliaSupport` — platform probe (`Class.forName` on Folia's regionizer class); used only where Folia removes functionality outright (e.g. the soak driver's save-all mapping)
- `PaperPayloadHandler` — encodes S2C / decodes C2S payloads using same wire format as Fabric
- `PaperCommands` — Bukkit `CommandExecutor` for `/lsslod stats` and `/lsslod diag`
- `PaperConfig` — GSON JSON config (same defaults/validation as Fabric)
- `PaperDirtyColumnBroadcaster` — broadcasts dirty column notifications to connected players (same as Fabric's `DirtyColumnBroadcaster`)
- `PaperWorldHandler` — registers configurable Bukkit event listeners for dirty chunk detection via reflection-based position extraction
- `PaperSoakBridge` — reflective gate from `LSSPaperPlugin` into the dev-only `dev.vox.lss.paper.soak` package (soak scenario driver + metrics exporter twins; excluded from the release shadowJar, retained by `soakShadowJar` for `SOAK_PLATFORM=paper` and `SOAK_PLATFORM=folia` runs)

### Client-Side

- `LodRequestManager` — orchestrates the client request loop: each scan the scanner writes the whole want-set into its send buffers and it ships in the same tick; handles dirty column re-requests, pruning on movement, and the one edge-triggered empty batch that clears the server backlog on backpressure halt (decode queue OR consumer-reported ingest backlog). No drip-feed, no timeout sweep, no rate-limit retry — re-declaration replaces all three
- `SpiralScanner` — expanding Chebyshev ring scan (adaptive cadence: 20-tick fallback + the completion-triggered fast re-scan, `fastRescanDue` — 250 ms floor, ≥95%-answered threshold with geometric tightening, **predicted**-walk-cost + ¼-halt-threshold pressure gates, v16 sessions excluded; armed only via `noteDeclared` from `tickScanPhase`) that produces the want-set closest-first; owns scan policy: the constant `WANT_SET_BUDGET` (800 — no client budget derives from any server cap) with pressure scaling — the MIN of the decode-queue factor and the consumer-reported ingest-backlog factor (issue #71; <=0 backlog = no signal, bit-identical to pre-#71; the vanilla-load scale is retired — server-side read/generation priority owns that protection). Does NOT suppress awaited positions — an awaited position blocks its own ring's confirmation (the fast trigger reads the awaiting-set SIZE as a cadence input, never as a send filter)
- `ColumnStateMap` — single owner of per-column client state (timestamps + dirty/retry/validated marks); its `classify` ladder decides whether a position is still wanted, and marks are consumed at the terminal answer (up-to-date / not-generated), not at send. `onNotGenerated` is a PERMANENT session-satisfy (no 0-stamp; only `markDirtyIfKnown` — a dirty broadcast — revives it); legacy cache 0-stamps from released clients classify as "no data" and re-declare -1
- `InFlightTracker` — the awaiting-answer set (positions of the last want-set minus answers since), replaced wholesale each scan. NOT a send-suppression structure: it exists only to gate status responses (BatchResponse carries no dimension) and to detect a dirty broadcast crossing an in-flight answer
- `ClientColumnProcessor` — off-render-thread decode of received column payloads before dispatch to consumers
- `ColumnCacheStore` — per-server per-dimension binary cache of column positions and timestamps (enables resync across sessions)
- `LSSClientNetworking` — packet handlers, dispatches sections to `LSSApi` consumers

### Public API (`LSSApi`)

LOD rendering mods register a `VoxelColumnConsumer` via `LSSApi.registerColumnConsumer()` to receive `VoxelColumnData` (MC-native `LevelChunkSection` + `DataLayer` light data per section, with coordinates). Consumer list is `CopyOnWriteArrayList` for thread safety. The client sets `CAPABILITY_VOXEL_COLUMNS` in the handshake only if consumers are registered.

### Compatibility

`VoxyCompat` uses `MethodHandle` reflection to call Voxy's `rawIngest` API at runtime — zero compile-time dependency, only 2 core MethodHandles (`WorldIdentifier.of` + `VoxelIngestService.rawIngest`). MC types (`LevelChunkSection`, `DataLayer`) must be referenced via direct class literals (not `Class.forName()`) due to Fabric's runtime remapping. `ModCompat` checks `FabricLoader.isModLoaded()` before attempting.

`MoonriseReadCompat` (Fabric, docs/planning/moonrise-read-priority-design.md) is the Moonrise-Fabric read bridge, same zero-compile-dep pattern: resolves `MoonriseRegionFileIO`'s 7-arg `loadDataAsync` overload by SHAPE (`getMethods` scan matching `(ServerLevel, int, int, <enum>, BiConsumer, boolean, <enum>)` — the `RegionFileType`/`Priority` classes come from the matched method's own parameter types, because Moonrise-Fabric SHADES concurrentutil under `ca.spottedleaf.moonrise.libs.…` unlike Paper), then invokes at `Priority.LOW` with `intendingToBlock=false`, the exact Paper semantics. Gated on `isModLoaded("moonrise")`, resolved once per JVM (lazy holder; instance-scoped `build(modLoaded, lookup, warn)` for order-independent tests), every failure shape → null = the unchanged existing ladder, with a once-warned drift message when the mod is present but unresolvable.

`AntiXrayCompat` (Fabric, docs/planning/antixray-compat-design.md) is the DrexHD AntiXray accommodation, same zero-compile-dep pattern: (1) the crash shim — binds AntiXray's obfuscation-context ScopedValues to null around both serialize choke points (their mixins' raw `.get()` throws unbound otherwise; the probe path additionally has generic containment in `RequestProcessingService.serializeProbeContained`), and (2) the engine probe — per-world adoption off the mod's controller for the x-ray masking feature: engine mode 1 (Hide controller) adopts its hidden list + max-block-height; modes 2/3 (Obfuscate/ObfuscateLayer) surface `ReplacementNoise` — their obfuscateGlobal is the hidden ∪ replacement UNION (bulk terrain), and adopting it flattened whole sections to their dominant non-masked state (the sculk/water black-LOD bug, found live 2026-07-27) — so the manager masks the LSS `xrayHiddenBlocks` list at the ENGINE's height instead (source label `antixray-mod+lss-list`) (`XrayMaskPolicy` in common, `XrayMaskFilter`/`XrayMaskManager` + Paper twins; masking hooks INSIDE `serializeColumn`/the NBT serializers so every serve path and the DirtyContentFilter hash see identical masked bytes; the static manager holder is published by the service lifecycle with an owner-guarded retract). All failure shapes degrade fail-safe (inactive shim = contained blank LODs, unreadable engine = LSS-config-keys masking) with once-bounded logging.

### Configuration

**Fabric:** GSON configs built on the shared `common/` base classes (`JsonConfig` loader + `ServerConfigBase`, which holds the server fields/defaults/clamps verbatim for both platforms). Two configs auto-created in `config/`:
- `lss-server-config.json` — full audit of every default and clamp in `docs/planning/config-defaults-and-clamps-review-2026-08-02.md`; the **2026-08-08 config rework** (user decision) renamed the bandwidth keys, hid the expert booleans, and flipped distance/store/gen-cap defaults — see below. Bandwidth caps (**`mbPerSecondLimitPerPlayer` 15.0 / `mbPerSecondLimitGlobal` 60.0** — decimal MiB/s since the rework; the legacy `bytesPerSecondLimit*` byte keys are `@HiddenFromFile` readers, honored forever, mb wins when both present, resolved by `resolveBandwidthKeys()` in validate() and re-sentineled to −1 (consumers MUST use `bytesPerSecondPerPlayer()`/`bytesPerSecondGlobal()`, never the fields); the values are the 2026-08-05 decision (25 → 15, 256 → 60): RAW bytes, not wire bytes, because they bound CLIENT DECODE WORK, the confirmed binding constraint, so wire compression does not loosen them; 15 MiB sits comfortably BELOW the ~25 MB/s elytra-wall rate; the global cap binds at four concurrent full-rate LOD players, raise it first on bigger fleets), LOD distance **512** (256 → 512 on 2026-08-08, reversing the 2026-08-02 revert — the D0 tile cache made the tracking cost affordable), **five expert booleans `@HiddenFromFile`** (`useBackgroundReadPriority`, `useBackgroundReadSplit`, `useSelectiveNbtParse`, `useNbtTranscode`, `useCompressedColumns` — all default true, absent from generated files, still honored from files that carry them, and a NON-default value survives re-saves: `JsonConfig.save()` drops a hidden key only while it equals the compiled default), **disk reader threads `0` = AUTO** (derived from the resolved read path: Moonrise `Priority.LOW` → `min(8, cores/2)`, vanilla's single-threaded IOWorker → 3, since there concurrency IS the vanilla-delay tradeoff), read priority, send queue limits, generation toggle, dirty broadcast interval, generation concurrency caps (**40/40 global/per-player since 2026-08-08**, was 32/16; the old `syncOnLoadConcurrencyLimitPerPlayer` knob is retired — a constant now, and the key is dropped from the file on the next save), transport deference (`outboundBufferCeilingKB`, default 0 = OFF — nonzero clamps 4096..262144 KB; above it a tick's column flush is skipped and the queue retained so LSS stops deepening the head-of-line delay ahead of vanilla's chunk packets on the shared channel. Ships off because the mechanism was measured ABSENT; arm it from `obuf_hw` in `/lsslod diag`, and treat nonzero `deferred=` on a healthy link as a red flag), **transport yield** (`lodYieldsToVanillaTransport`, default **false** — v0.10.0 stage A2, vanilla-first-lod-yield-plan.md: while the player's netty channel reports NOT WRITABLE the per-tick column flush is skipped and the queue retained, so LSS never deepens a backed-up channel's queue ahead of vanilla's chunk packets; a 5 s starvation floor sends one payload per window and a once-a-minute relevance prune drops left-behind queue entries (pruned positions ride the send-failure return path to `clearDiskReadDone`). Writability UNKNOWN — legacy probe, no signal, closed channel — never yields. Ships UNARMED; the default flips only in a later release citing the live E3 A/B (event rates, not `yielded=`); behind a buffering proxy the gate is best-effort (under-yields, never over-yields). Diag: per-player `yielded=`, a conditional `Yield: armed=…, ticks_total=…, bytes_withheld=…` line — armed shows immediately as the arming receipt, and the counters are service-scoped so armed-then-disarmed sessions keep their evidence; `bytes_withheld` is a byte-tick pressure integral, never a delivered-bytes count), legacy-client shim toggles (`enableV16Compat` default true; `enableV18Compat` default true — the protocol-18 native rung, docs/planning/v18-compat-design.md; `enableV19Compat` default true — the protocol-19 native rung), miss-memo TTL (`missMemoTtlSeconds`, default 30, 0 = off), **per-dimension timestamp cache `0` = AUTO** (derived from `lodDistanceChunks` at the D0 tile cost — 8× the scanned disc area at ~5 B/column, net at the default distance ~2.5× less RAM tracking ~5.3× more columns than the pre-tile 1.5×/64 B model; a fixed value silently under-provisioned when an admin raised the distance), LOD store (**`lodStore` off/on, default `"on"` — ON BY DEFAULT since the 2026-08-08 rework (user decision, superseding the 2026-08-03 opt-in decision); `"on"` is the canonical spelling of FULL and `"full"` a permanent read alias (`LodStoreMode.normalize`); the diag token still reports the RUNNING ENGINE name (`store=full`/`memory`/`off`), which the harnesses grep. `lodStoreBackfill` stays default TRUE so the default experience is the whole feature — inert while the store is off. `PaperConfig.validate()` warns whenever the store is armed on Folia (now the default state there)** — `memory` retired 2026-08-03, an existing `"memory"` normalizes to `off` and the retired `lodStoreMemoryMB` key drops from the file on the next save; **the test harnesses pin `lodStore: "off"` explicitly** (all 26 soak scenario configs, benchmark.sh's neutral staging, and the gametest run-dir configs staged by fabric/build.gradle's doFirst) so their baselines did not shift with the default; `lodStoreResweepSeconds` 0 shared / 300 Paper-default — Paper's unfired-event staleness bound; **`lodStoreBackfill` default true, inert while the store is off**; `lodStoreBackfillColumnsPerSecond` **500**, clamp 10..1000 — the walk's pace, raised from 100 so the walk is a ~23 min bounded event not a ~2 h drag; the MSPT gate that pauses it is now the constant `LOD_STORE_BACKFILL_TICK_CEILING_MS`=45, retired as config; `lodStoreMaxMB` 0 = UNCAPPED default, nonzero opt-in cap clamps 64..1048576 — size-cap eviction sized to the byte DEFICIT (never a flat row batch: a fixed 512-rows/dim x 8 passes emptied a store in one tick) and measured against LOGICAL bytes `(page_count - freelist_count) * page_size`, never `Files.size()` which WAL freezes; the incremental_vacuum is DRAINED in a loop because JDBC steps it exactly once per call, one 16 KB page — all three defects compounded into a permanent eviction treadmill at 14% cap utilisation before v0.9.0. One cap-log line per session, backfill hard-stops near an active cap; store-cap-behavior-plan.md. Two guards independent of the cap: `enabled: false` opens NO store and runs NO backfill (the constructor builds the store, and SERVER_STARTED runs it unconditionally, so this needs its own check), and the backfill stops at a 2 GiB free-space floor — nothing else bounds the walk on the shipped uncapped default, and the first casualty of a full volume is Minecraft's own region saves, not LSS), NBT→wire transcode (`useNbtTranscode`, default true — false forces every section through the object path as a rollback), background-read split (`useBackgroundReadSplit`, default true — v0.10.0 perf Phase 3/R1, Fabric-only in effect: the vanilla-IOWorker executor fetches only the raw compressed record and inflate+parse run on the reader pool; false restores the full-read-on-executor closure, a rollback narrower than `useBackgroundReadPriority=false`), selective NBT parse (`useSelectiveNbtParse`, default true — v0.10.0 perf Phase 4/R2 at the split's pool-side parse site: only the serializer-consumed root keys `{Status, sections}` materialize (whitelist build-pinned against the serializer source), other root subtrees skip without building Strings/HashMaps/tags — an allocation-churn win, not an inflate win; one documented one-directional leniency divergence (corrupt non-section subtrees skip instead of condemning the column), any selective throw falls back to a full parse of the same buffer; false restores the full root parse), x-ray LOD masking (`xrayObfuscation` "auto"/"on"/"off" default auto — auto detects the AntiXray mod on Fabric / Paper's built-in anti-xray per world and adopts the engine's hidden list + max height; `xrayHiddenBlocks` + `xrayMaxBlockHeight` (default 64) are the fallback tier — docs/planning/antixray-compat-design.md)
- `lss-client-config.json` — receive toggle (`receiveServerLods`), distance override (`lodDistanceChunks`), legacy-server backward-compat (`enableV16ServerCompat`, default true) + its Tier B generation-drive (`enableV16Generation`, default true — see `docs/planning/v16-client-compat-design.md`), ingest-pressure request pacing (`enableIngestBackpressure`, default true — issue #71 kill switch), adaptive scan cadence (`enableAdaptiveScanCadence`, default true — completion-triggered fast re-scans up to 4 Hz, false restores the fixed 1 Hz cadence; docs/planning/adaptive-scan-cadence-design.md), manual column-rate cap (`lodColumnsPerSecondLimit`, default 0 = off — bounds LOD columns/sec via budget clamp + size-weighted fast-fire spacing, the manual override for what the #71 taper can't see; nonzero clamps 50..100000, ~3200+ inert; also a Sodium slider "Max LOD Download Rate"; the 1 Hz fallback is never gated; docs/planning/client-column-rate-cap-design.md)

**Paper:** `PaperConfig` extends the same `ServerConfigBase` — identical fields, defaults, and clamps as Fabric. Config at `plugins/LodServerSupport/lss-server-config.json`, plus an `updateEvents` list of Bukkit event class names for dirty chunk detection.

All config values are clamped to safe ranges (min and max) on load.

## Benchmarking

Automated benchmark system for measuring server pipeline performance. Gated behind `-Dlss.benchmark=true` — never activates in production.

### Quick Start

```bash
./scripts/benchmark.sh fresh 60       # Fresh world, 60s — tests generation + serialization
./scripts/benchmark.sh no-cache 60    # Pre-built world — tests disk-read + serialization
```

Run `fresh` first to generate a base world, then `no-cache` reuses it.

### Optimization Workflow

1. Run baseline: `./scripts/benchmark.sh fresh 60`
2. Make optimization changes
3. Run again: `./scripts/benchmark.sh fresh 60`
4. Compare `benchmark-results/server.json` — key metrics: `throughput.sections_per_second`, `throughput.bytes_per_second`, `disk_reader.avg_read_time_ms`, `jvm.gc_time_ms`
5. Client metrics in `benchmark-results/client.json` — key: `avg_rtt_ms`, `columns_received`, `total_not_generated`

### Output Files

- `benchmark-results/server.json` — server throughput, sources, disk reader, generation, backpressure, bandwidth, JVM stats
- `benchmark-results/client.json` — columns received, bytes, send cycles, RTT, bandwidth rate
- `benchmark-results/*.jfr` — Java Flight Recorder profiles for both server and client

### How It Works

The shell script builds the mod, starts a dedicated server (`runBenchmarkServer`) and client (`runBenchmarkClient`) as separate Gradle tasks. The client auto-joins via `--quickPlayMultiplayer`. After the configured duration, the server exports metrics and halts; the client exports on disconnect. Duration is configurable via `-Pbenchmark.duration=N` on the server Gradle task.

### Key Files

- `fabric/src/main/java/dev/vox/lss/BenchmarkBridge.java` — reflective gate from the entrypoints into the dev-only harness (the `dev.vox.lss.benchmark` package is excluded from release jars; production no-ops)
- `fabric/src/main/java/dev/vox/lss/benchmark/BenchmarkHook.java` — tick counter, auto-shutdown, metric dump
- `fabric/src/main/java/dev/vox/lss/benchmark/BenchmarkMetricsExporter.java` — JSON serialization of all diagnostic + JVM data
- `scripts/benchmark.sh` — orchestrator (build → start server → start client → wait → collect); sources `scripts/lib/mc-run.sh` for the shared server/client lifecycle helpers

### Scenarios Explained

| Scenario | World State | Tests |
|----------|------------|-------|
| `fresh` | Empty world, chunks generated on demand | Generation + serialization pipeline |
| `no-cache` | Pre-built world | Disk-read + serialization pipeline |

### Interpreting Results

**Server `server.json`:**
- `throughput.sections_per_second` — primary throughput metric
- `sources` — where data came from: `in_memory` (loaded chunks), `disk_read`, `generation`
- `disk_reader.avg_read_time_ms` — disk I/O latency
- `backpressure.superseded` / `range_filtered` — asks silently dropped by a newer want-set declaration, transient generation pressure (gen slot full / capacity / timeout), a generation-ordering refusal (pacing/spread — these dominate the volume during backfill; see `gen_order_gated`), or the distance filter; healed by re-declaration. Nonzero is normal under v17 — `superseded` rises with any backlog the periodic declare outruns. The disk-miss flavors also count `service.miss_dropped` (soak law A5's term)
- `backpressure.queue_full` — send-queue overflow drops (a real loss signal, unlike the two above)
- `jvm.gc_time_ms` — GC overhead during benchmark

**Client `client.json`:**
- `avg_rtt_ms` — round-trip time for chunk requests
- `total_not_generated` — chunks the server permanently couldn't serve; ~0 on a gen-enabled server (transient pressure never answers NOT_GENERATED anymore)

## Soak Testing

Scenario-driven correctness harness: a real dedicated server + headless client execute a scripted timeline, both sides append JSONL diagnostic snapshots, and a Python checker enforces conservation invariants (request/delivery/generation/disk accounting) plus per-scenario expectations. Gated behind `-Dlss.soak.scenario=<path>` (server) and `-Dlss.soak=true` (client) — never activates in production. Design spec: `docs/planning/soak-test-design.md`.

### Quick Start

```bash
./scripts/soak.sh fresh-backfill    # Fresh superflat world — generation backfill; saves soak-worlds/base
./scripts/soak.sh warm-rejoin       # Two client runs — warm cache resync after a kick
./scripts/soak.sh dimension-trip    # Overworld → End → Overworld dimension boundaries
./scripts/soak.sh dirty-broadcast   # setblock → dirty broadcast → client re-request
./scripts/soak.sh all               # All 19 Fabric scenarios in order; stops at first failure
SOAK_PLATFORM=paper ./scripts/soak.sh all   # Paper port: fresh-backfill, warm-rejoin, dimension-trip, paper-dirty-falling-block, paper-store-unfired-event
SOAK_PLATFORM=folia ./scripts/soak.sh all   # Folia: same four scenarios against a real Folia server (:paper:runFolia)
```

Further scenarios (same invocation): rate-limit-storm, disk-saturation, generation-disabled, generation-capacity-stress, bandwidth-throttle, cold-restart-resync, enabled-false, teleport-prune, dirty-range-filter, dirty-during-backfill, dirty-while-offline, clearcache-mid-session, dimension-rejoin-warm, store-second-join (lodStore=full; absorbed the old `-full` twin when `memory` was retired), store-save-storm. Store-specific extras: `store_offline_edit.sh` (three chained phases via SOAK_WORLD_FROM + cross-phase probe-hash verdict), `store_save_storm.sh` (paired process-CPU), `store_gate.sh warm|cold` (the §0 A/B), `SOAK_LODSTORE_OVERRIDE=full` (burn-in lever merging the store into every scenario).

`rate-limit-storm` and `disk-saturation` keep their **historical names** but no longer test rate limiting — v17 has none:

- **`disk-saturation` is the harness's supersession proof.** `disk.saturated == 0` (the `hasHeadroom` gate must *prevent* what used to bounce — issue #32's root cause; **measured: 0** under threads:1) **plus** `service.superseded >= 100` (**measured: 420**, backlog high-water 760) — without the second, zero saturation would prove nothing, because a flood that never built a backlog cannot demonstrate absorption.
- **`rate-limit-storm` is on its THIRD premise** (the file name is kept — renaming touches six keyed tables). Under server-owned generation the client always declares the constant `WANT_SET_BUDGET` (its old `syncCap: 4` lever is a deleted knob), so it now pins **bounded transient-drop churn**: a small fresh disc converges through default gates with `service.superseded` stopping at convergence (ceiling 1500 in `check_soak.py` — re-baselined 2026-08-01 for the adaptive scan cadence, whose up-to-4 Hz converging-tail re-declares measured 742 vs the 1 Hz era's 370) plus a generation-progress floor.
- **`generation-capacity-stress` pins the permanence guarantee negatively**: on a gen-ENABLED server `NOT_GENERATED` must NEVER reach the wire (`not_generated == 0` — a transient capacity/timeout leak would blank columns for the whole session), while `superseded >= 100` proves the repeated-miss churn loop (a miss that can't take the single gen slot drops silently, is re-declared, re-misses) actually ran. Law A5 carries the dedicated `service.miss_dropped` term for exactly these drops.

**The general rule: under this model, supersession is produced by slow *service* relative to the want-set** — the want-set is a constant, so a scenario that means to create contention must slow the service (disk threads / generation caps). A failure in these scenarios is a premise question first (did the config stop creating contention? did the headroom gate leak?) and a threshold question only after.

Scenarios needing a base world auto-run `fresh-backfill` first. warm-rejoin, dirty-while-offline, and dimension-rejoin-warm use two client runs. Exit code = checker exit code.

### Output Files

- `soak-results/<scenario>-<timestamp>/server.jsonl` — server snapshot/command/join/end event rows. Snapshots carry the data-capture fields: `service.{superseded,range_filtered,re_resolved,grace_skipped,miss_dropped}`, `disk.memo_hits` (miss-memo rung hits — law A5's virtual not-founds), `players[].backlog`, `*_hw` high-water gauges (per-tick sampled), `dedup.groups`, `jvm.*`, `tscache.*`, `dirty.{marked,suppressed}_total`, `mspt_avg_window`; command rows carry `ok`.
- `soak-results/<scenario>-<timestamp>/client-run<N>.jsonl` — client snapshot/disconnect rows per run (incl. `ingest_failures`, `effective_lod`, `rtt.{p50,p95}_ms`)
- `soak-results/<scenario>-<timestamp>/verdict.json` — checker verdict (conservation laws + named checks)
- `soak-results/<scenario>-<timestamp>/report.md` — `soak_report.py` anomaly digest (auto-written; a lens, not a gate)

### How It Works

`soak.sh` stages world/cache/config per scenario, pre-validates the timeline (`check_soak.py --validate`), then starts `runSoakServer` and `runSoakClient` (no JFR). `SoakScenarioDriver` fires timeline commands at join-anchored offsets, snapshots every 5 s, and halts the server at scenario end; the soak client synchronously flushes its column cache before exiting. The checker evaluates deltas between verified-quiescent snapshots and writes `verdict.json`; any violation exits nonzero. `SOAK_PLATFORM=paper` runs the identical scenario timelines against a real Paper server (`:paper:runSoakServer`, a run-paper task fed by the dev-only `soakShadowJar` that retains the soak package) with the unchanged Fabric soak client and checker; Paper keeps its own base world at `soak-worlds/base-paper`. `SOAK_PLATFORM=folia` does the same against a real Folia server (`:paper:runFolia`, run-paper's Folia download task with `pluginsMode = INHERIT_NONE` — the default plugin-jar detection would add the soak-stripped release jar alongside the soak jar; base world `soak-worlds/base-folia`): the driver maps `save-all` to an acknowledged no-op (`"mapped": true` command-row key — Folia unregisters the command) and the staging writes a `bukkit.yml` with a 100-tick autosave so chunks still flush mid-run. Per-scenario client instrumentation: `-Psoak.probes=x:z,...` adds per-position timestamp probes to client snapshots; `-Psoak.clientActionAt=SECONDS:ACTION` fires one scripted client action (e.g. `60:clearcache`). warm-rejoin, dirty-while-offline, and dimension-rejoin-warm use two client runs.

### Key Files

- `scripts/soak.sh` — orchestrator (stage → validate → run → collect → check)
- `scripts/soak-scenarios/<name>.json` + `<name>-config.json` — driver timeline + sparse server-config overrides
- `scripts/check_soak.py` — stdlib Python invariant checker (`--validate` pre-flight, post-run laws, per-run completion gates — a missing server `end` row OR a missing client `disconnect` row means that JVM died mid-run, `client_run_completion_violations` — `--selftest` 199 in-memory pass/catch cases incl. all four oldest named checks, the disconnect gate, the quiescence client mirror, and the xray config-key type branches). **The harness is v17-only:** `players[].backlog` is a required schema field and `service.superseded`/`range_filtered` are required + monotonic, so it will correctly reject any pre-v17 recording — re-record rather than debug.
- `scripts/soak_report.py` — stdlib post-run anomaly digest (spikes/stalls, concerning-vs-mechanism counters, high-water marks, cadence/TPS, law margins, cross-identity audits); a lens, never a gate (`--strict` to exit nonzero on any anomaly; `--compare`, `--selftest`)
- `scripts/check_move_trace.py` — stdlib validator for the move-desync tracer's JSONL (`--validate FILE...`, `--selftest` — 36 pass/catch cases over the shared `scripts/testdata/move-trace-rows.jsonl` fixture the Tier 1 goldens write; run it on collected traces BEFORE analysis)
- `scripts/release_check.py` — release-jar safety gate (no dev-only benchmark/soak packages ship, incl. inside nested Jar-in-Jar entries and dev/vox/lss/common namespaces; stale-jar ambiguity guard + `--version` pinning; version expansion; mappings-namespace manifest; glob hygiene). Wired into `.github/workflows/build.yml` alongside the three `--selftest` runs.
- `scripts/lib/mc-run.sh` — shared server/client lifecycle helpers (sourced by soak.sh and benchmark.sh)
- `fabric/src/main/java/dev/vox/lss/benchmark/SoakScenarioDriver.java` — server-side timeline executor + JSONL snapshots
- `paper/src/main/java/dev/vox/lss/paper/soak/PaperSoakScenarioDriver.java` — Paper twin of the Fabric driver (Bukkit join anchors + 1-tick scheduler, same JSONL contract)
- `paper/src/main/java/dev/vox/lss/paper/soak/PaperSoakMetricsExporter.java` — Paper twin of the server-side exporter (same snapshot schema)
- `paper/src/main/java/dev/vox/lss/paper/PaperSoakBridge.java` — reflective gate; the soak package is excluded from the release shadowJar
- `fabric/src/main/java/dev/vox/lss/BenchmarkBridge.java` — reflective gate to the dev-only harness package (excluded from release jars)

## Key Patterns

- **Java 25** target for Fabric and Paper (`options.release = 25`), **Java 21** for common. Uses `var`, records, sealed-pattern `instanceof`, switch expressions.
- **Payloads:** Fabric uses `StreamCodec` with lambda encode/decode registered in `LSSNetworking`; Paper uses raw `FriendlyByteBuf` in `PaperPayloadHandler`. Both produce identical wire bytes.
- **Raw chunk shipping:** Server serializes MC-native `LevelChunkSection` data (block states + biomes via `section.write(buf)`) plus light `DataLayer` nibbles and sends. MC's built-in zlib network compression handles packet compression. No server-side voxelization or caching — client receives MC objects directly.
- **Off-thread processing:** Main server thread serializes loaded chunks into `LoadedColumnData` (pre-serialized bytes); a dedicated processing thread prepares payloads and schedules sends via `OffThreadProcessor`. `ColumnTimestampCache` provides in-memory up-to-date checks.
- **Folia (Paper module, experimental):** the service tick runs on the `GlobalRegionScheduler` (main thread on Paper, the single global-region thread on Folia — same pump semantics both ways); handshake registration and PlayerQuit arrive on region threads and go through a lifecycle mailbox drained at the top of `tick()` (the registering handshake's SessionConfig reply is deferred into that drain — never sent from the region thread — so the client cannot declare before its state exists); generated chunks are serialized inside the Moonrise `scheduleChunkLoad` completion (the owning region thread — the chunk can unload before a hop to the global tick). Loaded-chunk probing on Folia runs on the chunk's owning region thread (the player's `EntityScheduler`, gated by `Bukkit.isOwnedByCurrentRegion`) rather than the global pump, so palettes are read on their owner; a one-tick hold-release at batch granularity aligns each declared batch with its region-published probe results in the same snapshot (release-then-take, never take-then-release — see `PaperRequestProcessingService`). `FoliaWiringContractTest` scans every production class's constant pool (nested classes included) so no `BukkitRunnable`/`BukkitScheduler` reference creeps back in (they throw on Folia). `/reload` is unsupported on Folia. LSS disk reads go through Moonrise (`loadDataAsync` at `Priority.LOW`, see `PaperChunkDiskReader`) — called from the LSS reader pool, off any region thread, which is safe because region files are not regionised. Release notes for Folia-affecting changes should mention the experimental status.
- **Thread safety** via `ConcurrentHashMap`, `AtomicLong`/`AtomicInteger`, `volatile`, `CopyOnWriteArrayList`
- **No compile-time optional deps** — all mod compat uses `MethodHandle` bridges (MC types must use direct class literals, not `Class.forName()`, due to Fabric remapping)
- **Mappings:** Mojang official (not Yarn). Paper uses Mojang mappings natively via paperweight-userdev.
- **Package:** `dev.vox.lss` (Fabric), `dev.vox.lss.paper` (Paper), `dev.vox.lss.common` (shared)

## Releasing

Releases are triggered by pushing an **annotated tag** (`git tag -a`). The tag annotation message becomes the release notes on both GitHub and Modrinth. The CI workflow extracts it automatically.

### Tagging a Release

`main` is branch-protected (changes must land via PR; tags are **not** protected). Full flow:

1. Review commits since the last tag: `git log $(git describe --tags --abbrev=0)..HEAD --oneline`
2. **Pre-flight the exact release build locally** before tagging — the tag triggers an irreversible GitHub + Modrinth publish, so it must be green first:
   `CI=true ./gradlew :fabric:build -x runClientGameTest :paper:test :paper:shadowJar -Pmod_version=<version> && python3 scripts/release_check.py --version <version>`
   (`release_check.py` must print `OK`; `--version` pins the check to the jars just built — stale jars in build/libs otherwise fail the run; `:fabric:build` runs Tier 1 + Tier 2, `:paper:test` gates the Paper jar. CI runs Tier 3 (`:fabric:runClientGameTest`) as a separate build.yml job — check it is green on the release commit before tagging.)
3. Get the release commit onto `main` via PR (protected branch): push the release branch, `gh pr create --base main`, then `gh pr merge --merge`. Use **`--merge`** (a merge commit) — `--squash`/`--rebase` rewrite SHAs and orphan the tag.
4. Write release notes to a file (format below) and create the annotated tag with **`--cleanup=verbatim`** so the `###` headers survive:
   `git tag -a v<version> -F <notes-file> --cleanup=verbatim`
   Verify before pushing: `git for-each-ref --format='%(contents)' refs/tags/v<version>` (are the headers present?).
5. Push the tag — this triggers `release.yml`: `git push origin v<version>`
6. Watch CI (`gh run watch <id> --exit-status`); after it publishes, **verify the rendered notes** on GitHub (`gh release view v<version>`) and Modrinth — see Pitfalls.

### Release Pitfalls (all hit v0.4.0 — see also memory `release-tag-notes-gotchas`)

- **`###` headers stripped from the tag message.** `git tag` defaults to `--cleanup=strip`, which deletes `#`-leading lines as comments. Always pass `--cleanup=verbatim`.
- **CI can publish a commit-dump instead of the annotation.** `actions/checkout` leaves a *lightweight* tag on tag-triggered runs, so the extract step's `objecttype` reads `commit` and falls back to `git log` commit subjects. Fixed in `release.yml` by force-fetching the annotated tag (`git fetch --force origin "refs/tags/$TAG:refs/tags/$TAG"`); if the notes still render wrong, fix GitHub instantly with `gh release edit v<version> --notes-file <file>`.
- **Modrinth changelog can't be fixed locally.** `MODRINTH_TOKEN` is a CI-only secret, so a wrong Modrinth changelog must be edited by hand on the web (`lKiXKLvv`; the `84zcagOb`/voxy-server-side listing stopped receiving versions at v0.8.0 — its v0.7.x changelogs are still hand-editable there). Keep a short, player-focused variant of the notes ready for this.
- **Never `gh run rerun` a partially-published release.** A re-run rebuilds byte-different jars, silently replaces the GitHub release assets, and creates DUPLICATE Modrinth versions (nothing rejects a second `v<X>+fabric+mc<Y>`). Red BEFORE the gh-release step = nothing published, full re-run safe. Red AFTER any publish step (the likely case: the voxy step 403s on missing membership) = recover by hand-uploading the GitHub-attached jars to the channel that missed.
- **Do NOT smoke-test the pipeline with a throwaway pre-release tag** — `release.yml` triggers on every `v*` tag and would irreversibly publish the LSS jars. (The old "verify 84zcagOb membership before tagging" step is retired with VSS publishing — disabled since v0.8.0.)
- **A gametest can flake on CI** — see **Known Test Flakes & Environmental Failures** (in the Test Commands section) for the full catalog. Since 2026-07 `build.yml` retries the gametest tiers once automatically (retries surface as `::warning::` annotations; first-attempt evidence lands in the `gametest-flake-evidence-*` artifacts), so a red job already survived two attempts — diagnose from the artifacts before `gh run rerun <id> --failed`.

### Release Notes Format

Write for **Minecraft server admins and mod users**, not developers. Use markdown with category headers — omit empty categories:

```
### Bug Fixes

- **Short summary** — What changed and why it matters to users.

### New Features

- **Short summary** — What it does and how users benefit.

### Configuration

- **Short summary** — New/changed config options with defaults.

### Performance

- **Short summary** — What improved and the user-visible impact.
```

Rules:
- Each item: bold summary + dash + 1-2 sentence explanation
- Mention Fabric or Paper when a change is platform-specific; omit qualifier when it affects both
- Folia-affecting items must mention Folia support's **experimental** status (the label stays
  until concurrent multi-region soak validation — see the spec's exit criterion)
- Skip internal-only changes (CI, README, refactoring) unless they have user-visible impact
- Use present tense ("Fixes X" not "Fixed X" — GitHub/Modrinth display these as current release notes)
- No version heading — the tag name is displayed as the title automatically

### Example

```bash
git tag -a v0.2.4 -m "$(cat <<'EOF'
### Bug Fixes

- **Fixes infinite generation loop in The End** — All-air chunks were treated as missing, causing endless re-generation in void dimensions.

### New Features

- **Purpur server support** — Paper builds now work on Purpur servers.

### Configuration

- **Configurable timestamp cache size** — New `perDimensionTimestampCacheSizeMB` option (default 32 MB) controls memory used for chunk freshness tracking.
EOF
)"
```
