# v0.10.0 program progress (living doc; spec = v0.10.0-mega-plan.md v1.3)

Created 2026-08-06 at program start, before A1's first commit (mega plan §6.1).
The mega plan stays the frozen spec; this doc carries the living state. A plan
deviation is legal only as a PAIR: a dated decisions-log entry here AND an edit to
the mega plan at the overridden text — never a silent divergence.

## Stage table

| stage | content (short) | status | PR | gate verdict / evidence |
|---|---|---|---|---|
| A1 | Probe-snapshot refactor + move-desync tracer + `LSS_MOVE_TRACE` rig knob + live deploy | MERGED 2026-08-07; live deploy staged, restart pending (expired panel token — see live-deploy log) | #87 | 3-lens Opus round complete, all findings fixed; T1 Fabric+Paper, T2 66/66, T3, validator selftest 34 — all green pre-PR |
| A2 | Transport yield (default FALSE) + tracer `yielded` field + live deploy of inert build | MERGED 2026-08-07; inert build staged over the A1 jar (same pending restart) | #88 | 3-lens round: 0 correctness MAJORs, 2 discipline MAJORs + 2 pin-gap MAJORs fixed; T1 ~1236+360, T2 66/66, T3, both selftests green |
| B0 | PERF measurement tooling + A/A control | MERGED 2026-08-07 | #89 | single-Opus review: 4 MAJORs + 11 minors all fixed; selftest 28 + T1 both platforms + T2 + CI green; backfill smoke reproduces F3; A/A PASS + burst-band noise-floor calibration (ledger) |
| B1 | R3 structural-identity memo key | **GATES FAILED — REVERTED** 2026-08-07; MERGED (behavior suite + tooling fix + negative-result record) | #90 | 4+4×150 s ABBA: memo marker 249→286 backfill / 258→290 serve, Key walk = the walk it replaced; plan Phase 1 verdict block + ledger; CI green |
| B2 | R4 CRC32C + interim schema bump (dev-window semantics per R-1) | MERGED 2026-08-07 — **R-1 deploy hold ACTIVE until C4** | #91 | contentHash marker 354→0 (+100% cut vs ≥80% gate); warm/cold/save-storm/3 soaks PASS; review 0 MAJORs + 6 minors fixed (F1: legacy FNV validator restored per R-1) |
| B3 | R1 background-read split | MERGED 2026-08-07 | #92 | IO-Worker LSS-frames +99.4%/+99.1%/+99.0% cut (serve/hibw/backfill) vs ≥75%/≥50% gates; hibw ceiling +3.8%; walk flat; A7 triage exonerated; review 1 BLOCKER + 2 MAJORs + 17 minors fixed |
| B4 | R2 selective NBT parse | MERGED 2026-08-07 | #93 | kill-switch A/B on `bae0952`: four-class alloc/col +50.0%/+48.1% cut (serve/backfill) vs ≥30%; nbt-minus-memo +50.2%/+58.6% vs ≥25%; parity 0.03%/exact; walk flat; 0 fallback warns; review 1 MAJOR (B4-1 skip-desync → exhaustion check) + 9 minors fixed |
| B5 | Round-acceptance closing A/B (merge gate for C1) | ROUND ACCEPTED 2026-08-07 | — | base=`834e4d7` vs final: backfill 3-thread +21.3% vs ≥15%; alloc +49.2%/+52.4% vs ≥30%; hibw ceiling +10.7% (µs/col +5.9% cut there); store gate outright; serve-µs/col −4.3% n.s. = documented deviation (R1+10.3%×R2−10.5% composition); noise audit clean |
| C0 | IdentityCodec + registry tables + WireSectionCursor + settling measurement | MERGED 2026-08-07 (to main — inert) | #95 | review: 0 CRITICALs, 6 MAJORs fixed (headline: vanilla shape-table enforcement — block bits 1-3 illegal on the native wire; §4.1 canonicalize-from-disk RETRACTED, plan-text pair); corpus cross-check + whole-registry sweeps green both platforms; settling: raw +5.28%, zstd-1 frame **+9.70%** (size claims re-baselined ~+10%, ledger) |
| C1 | v20 encode + client decode + handshake + WireDialectTracker + `Dialects:` diag | MERGED 2026-08-07 (to `feat/protocol-20`) | #97 | everything from the previous row PLUS: lss:client_info sidecar + SessionConfig v20 data-version append (both platforms, parity pins), v19 gate/glue/production pins incl. the T2 crafted-frame case, the frozen-handshake-shape pin (§2.2 CRITICAL), enableV19Compat soak allowlist; C2 restores legacy column egress via the body translators |
| C2 | Legacy egress translators + store-hit recompress + dialect-19 soak lever | MERGED (PR #98, 2026-08-08, into `feat/protocol-20`) | dialect-19 fresh-backfill + warm-rejoin PASS | translation at the per-recipient ENQUEUE choke point (recorded deviation — law A2), fromV20 + buildLegacyColumn memoized on ColumnBytes, header splices only at flush; store hits covered by the same seam; `SOAK_DIALECT=19` lever; §9 corpus chains both platforms; grouped-Opus round M1-M3 + 9 minors |
| C3 | Client ladder (20→19→16 discovery) | MERGED (PR #99, 2026-08-08, into `feat/protocol-20`) | ladder T1 suite + codec pins + T2/T3 + dialect-19 soak PASS | V16ClientWire sticky announce set (review CRITICAL — the 16 rung must not disarm a pending 19 echo), per-payload dialect stamp at netty decode (MAJOR-2), enableV19ServerCompat default true; negotiated-protocol observability deferred to C6 |
| C4 | Store schema 4 FINAL: lazy ALTER + wirefmt + structural guard + both-rung dispatch + migration walk | MERGED (PR #100, 2026-08-08, into `feat/protocol-20`) — **ENDS the R-1 deploy hold** | #100 | grouped-Opus round: 1 CRITICAL (walk usize/Throwable/guarded-DELETE containment — poison row latched the store dead) + 8 MAJORs + 12 minors, all blocking items fixed; from-state matrix + one-txn upgrade atomicity empirically verified by the reviewer; T1 both + T2 + store-second-join PASS (26 windows, 0 violations) + dialect-19+store warm-rejoin PASS; accepted gap: no soak produces live 19-rows (unit + `StoreLegacyRowCompositionTest` composition coverage; Modrinth deploy = the live gate) |
| C5 | Via mismatch guard + `test-server.sh` Via variant | MERGED (PR #101, 2026-08-08, into `feat/protocol-20`) | #101 | grouped-Opus round: 0 CRITICALs, 4 MAJORs (all guard-observability: rig kill switch, Fabric production-ladder coverage, anchored wiring pins, production-Holder test) + 15 minors — all MAJORs + 13 minors fixed; correctness core empirically verified against upstream Via (no reply/registration leak on any path); T1 both + T2 68/68 + selftest 191 + dialect-19 fresh-backfill PASS; live Via pull = rig variant + D3 matrix cell 3; **D1/D2 carry: Velocity/Bungee proxy hole in release notes** |
| C6 | Hardening: sims, Tier 2/3, fixture corpus, full soaks, benchmark arms, matrix cells 3–4 | **COMPLETE — merged to MAIN 2026-08-08 (stage PR #102 → integration branch; main merge PR #103 with the atomic M-6 build.yml/pin retirement; cell 3 → D3 by recorded deviation)** | #102 | §9 gap suites landed (cross-registry sims, resolver ladder 13 tests, bijection fuzz, T3 C10, migration golden, live corpus + identity-level decode pins); store-migration soak live-PASS; three-platform soaks green (19/19, 5/5, 4/4); benchmark arms: serve +18.5% µs/col vs B5 (direct-emit follow-up TRIGGERED, task #8), backfill save-cost +16.9% with throughput unchanged, dialect arm CPU n.s./alloc +24% (clean re-measure after review C-1); review round: 3 CRITICALs found → C-1 fixed+re-measured, C-2 fixed, C-3 = the pending cell-3 decision; M-1 (all-air deletion) fixed; follow-ups recorded | 
| D0 | ColumnTimestampCache tile redesign (timestamp-cache-tile-redesign.md; fresh branch off main post-C6) | COMPLETE — merged to main 2026-08-08 (PR #104, merge 05882a8d): §8 gates green (T1/T2/Paper T1 + CI T3; 8 soaks incl. BOTH live v1→v2 migration arms), 2-lens review round fixed (record below) | — | — |
| D1 | Docs consolidation (PERF Phase 5, release notes incl. the D0 item, CLAUDE.md sweep) | COMPLETE — merged to main 2026-08-08 (PR #107): README compression ops note, release-notes draft (docs/planning/release-notes-v0.10.0.md), protocol-20 CLAUDE.md coverage, stale folia-supported text fixed (erratum carry-through verified no-op) | #107 | — |
| D2 | v0.10.0 main release — **PREP ONLY this program: pre-flight + notes + tag text; tag NOT pushed (user directive 2026-08-06)** | pending | — | — |
| D3 | Fresh 26.1 + 1.21.11 re-ports + multi-test refresh + matrix cells 1–2 + tri-release — **tags NOT pushed (same directive)** | pending | — | — |

## Decisions log

- **2026-08-06 (user decision, program start)**: execute the mega plan end to end,
  but **prep and do not run the v0.10.0 release** — D2/D3 tag pushes are out of
  scope for this program run; everything up to and including the pre-flight,
  release notes, and tag text is in scope. Recorded here because it amends the D2/D3
  rows' final step.
- **2026-08-06**: program executed by Claude (autonomous session(s)); review rounds
  per mega plan §6.2 with the repo's 2–3-subagent budget.
- **2026-08-06 (A1 implementation decisions, tracer plan §1.4 lss block)**: three of
  the sketched `lss` fields had no server-side source and are shipped as follows
  (tracer plan §1.4 edited in the same commit — the §6.1 pair): `bw_window` →
  **`bw_total`** (cumulative per-player bytes; no per-player rolling window exists,
  and a different quantity gets a different name — the U-12 discipline); `proto` is
  **derived from the dialect rung** (v16→16, v18→18, else current) since protocol
  version is consumed at handshake and never stored; `since_s` reads a new
  diagnostics-only `AbstractPlayerRequestState.getCreatedAtMillis()` stamped at state
  creation (== registration).
- **2026-08-06 (A1 Tier 2 finding)**: the tracer plan's "hasClientLoaded() is true
  out of the box" holds at listener CONSTRUCTION but not after `placeNewPlayer`,
  which arms the 60-tick `clientLoadedTimeoutTimer`; a mock connection never confirms
  → every move swallowed. The gametests prime the listener reflectively
  (`primeListenerForMoves`: clear `awaitingPositionFromClient`, zero the timer, clear
  `waitingForRespawn`) — test-only, dev-runtime named mappings.
- **2026-08-07 (A1 review round, three Opus lenses — correctness+concurrency /
  pinned-decisions+fidelity / tests+bytecode)**: 1 MAJOR + 2 pin-gap MAJORs + ~17
  minors, ALL fixed same-day (plan edits paired below). Headline — **A-1/C-1 MAJOR,
  found independently by two lenses**: the `@At("HEAD")` inject ran on the NETTY
  event loop for every move packet (vanilla's `ensureRunningOnSameThread` is the
  method's first statement and re-dispatch-throws there), racing the gap clock
  (double-record → `move_gap_ms` measured the netty→main hop, structurally ~0 — the
  U-5 instrument dead on arrival), leaking swept per-player state via a netty-side
  `computeIfAbsent`, and corrupting the `restored` capture under packet pipelining.
  Fixed by retargeting the entry inject to INVOKE shift-AFTER the ensure call (runs
  exactly once, server thread) + an ASM pin that only argument plumbing precedes the
  ensure INVOKE. Pin-gap MAJORs: the rotation test could not distinguish the 2x cap
  from unbounded rotation (C-2 — now asserts drop counts + total surviving lines);
  the ring-flush-before-event ordering and FlightRing wrap-around were unpinned
  (C-3 — source-regex ordering pin + FlightRingTest). Plan-deviation pairs applied:
  `awaiting_tp` moved to flight rows (A-2 — structurally false at event sites),
  `expected_dist` → `expected_dist_sq` (B-6), per-boot rotation over a stale .1
  (B-5), `logged_wrongly` rejected-only (B-2), `restored` optional when the entry
  capture never ran (C-10). Also: adapter probes re-wrapped whole-body catch-all
  (A-3), single `isActive` read per snapshot (A-4), writer counts rows on WRITE with
  IOException/interrupt drain+cap-latch (A-5), render failures contained to a
  counted drop + per-player telemetry containment (A-6), residual-bounded
  entity/stop sampling (A-7), activate/close idempotence + interrupt escalation
  (A-8), GapClock backwards-clock reset (A-9), probe rebind on respawn identity
  change (A-10), `withV16Line`/`withV18Line` no longer clobber the MoveTrace diag
  line (B-3, reversed-order pin added). Full findings in the three review-agent
  reports (this session); dispositions all "fixed" except A-7/A-9 marked PLAUSIBLE
  by the finder and fixed anyway (cheap, strictly safer).

- **2026-08-07 (A2 implementation decisions, yield plan §5)**: the "service diag
  block" pair ships as a conditional `Yield:` line (`withYieldLine`, the conditional-
  slot precedent) with counters in `TickDiagnostics` (service-scoped — live-state sums
  collapse on rejoin, the R2-9 lesson); `bytes_withheld_total` is defined as a
  BYTE-TICK PRESSURE INTEGRAL (each withheld tick adds that tick's held queue bytes —
  repeated counting of a held payload across ticks is the point, it measures hold
  pressure, never delivered bytes); the one-shot INFO fires at the first floor send
  (= the plan's "N consecutive fully-yielded ticks" with N = `YIELD_FLOOR_TICKS`);
  pruned positions ride the send-failure drop-report array to the processor's
  `clearDiskReadDone` (the prune runs main-thread, the done-bits are
  processing-thread-owned). Yield plan §5 annotated in the same commit (the pair).

- **2026-08-07 (A2 review round, three Opus lenses — correctness+concurrency /
  fidelity+contracts / test-adequacy)**: no correctness MAJORs in the shipped control
  flow (lens (a): every gate/floor/prune path traced clean, thread ownership holds,
  prune radius strictly inside the client's own forget distance — the N1 invariant
  now documented in `pruneSendQueueOutsideRange`'s javadoc). Fixed same-day:
  **B-1/A-3** yield counters made volatile (the H3 Folia-render pin); **B-2/A-2** the
  prune GATED on `lodYieldsToVanillaTransport` (it shipped armed in default config
  against the evidence posture; plan §2.1 edited — the pair); **A-1/B-5** floor
  counter resets only on an ACTUAL send (limiter-refused floor ticks stay armed; §1.4
  edited); **A-4** prune additionally runs on every floor tick (§2.1 edited);
  **B-3/A-5/C-8** validator: `yielded` optional in the lss block (A1-era jars — incl.
  the STAGED live build — emit blocks without it; boot partition key stays optional
  with the rationale moved to the list it governs); **C-1** the differential
  armed-writable drain pin (the F2-1 blocker's regression pin); **C-2** floor-payload
  full-send-path assertions (enqueued-mark release, departure stamp, diag count);
  **C-3** armed+throwing-snapshot degrade pin; **C-4** live-armed-flag source pins on
  both command surfaces + the CG-027 "Yield line ABSENT on default config" gametest
  leg; **C-5/B-7** floor-draws-from-post-prune-head as an actual floor send + the
  5-arg-overload yield-off pin; **C-6** `MAX_SEND_SECTIONS_SIZE` constant; **C-7/A-7**
  dead per-player byte-integral accessor deleted (service-scoped twin is the surface);
  **C-9** probe-suppress-unstamped assertion on yielded ticks; **B-4** the
  `MIN_OUTBOUND_BUFFER_CEILING_KB` invariant comment sentence; **B-6/A-8** drop-report
  comments/`@return` reworded for the prune reuse; **B-8/A-6** javadoc attachment
  fixes. B-9 (unrelated files riding the branch: the Phase-0
  `profile_disk_read.sh` fix, `palette_size_analysis.py`, the dh-support doc) kept
  with a PR-body note — the Phase-0 item's PR text will record it pre-landed here.

- **2026-08-07 (B0 implementation decisions, perf plan Phase 0)**: the effective-config
  echo (`ServerConfigBase.effectiveConfigEcho`, INFO at service start on both platforms,
  exact-format-pinned in ConfigValidationTest + the Paper twin) carries
  `useCompressedColumns` in addition to the plan's `useNbtTranscode`/`diskReaderThreads`
  — it IS compress_gate.sh's arm variable, and the plan's own point is that an ignored
  arm key must fail the arm (plan item 1 annotated — the pair). `analyze_profile_jfr.py`
  gained a third window mode `walk` beyond the plan's `full|t0:t1` (reads the walk
  window backfill_profile.sh parses into meta.json; plan item 4 annotated).
  `DISK_PATH_MARKERS` additions include the PROSPECTIVE Phase 3/4 class names
  (`SelectiveChunkNbtLoader` both packages) with an in-file note that Phase 3's raw-read
  site must land in a listed class or extend the list in the same PR. Marker counters
  are any-frame gauges (a sample can count under several markers — deliberately NOT a
  partition; they are "samples under X" numbers, the bands stay the partition).
  compare_profile.py's µs/col uses `columns_received` as its denominator and 10 ms as
  the Java sample period, restating the F5 native-@20ms caveat in its output; backfill
  arms pool `walk.deposited` as the per-column denominator. Also folded in: the ABBA
  matrix ordering applies to the legacy vanilla/c2me mode too (the fixed order was the
  same first-position bias the plan calls out for ref-vs-ref).

- **2026-08-07 (B0 review round, single Opus reviewer per §6.2 — three lenses in one
  pass)**: 4 MAJORs + 11 minors, all addressed same-day. **M1**: the echo printed the
  REQUESTED `useCompressedColumns` before the zstd probe — a natives-less server would
  run both compress_gate arms raw with both marked valid, the exact failure the echo
  exists to prevent; fixed by moving the echo below the probe on both platforms and
  passing the LIVE `wireCompressionLive` (the format pins now set the field OPPOSITE
  to the passed value to pin argument-not-field). **M2**: an incomplete walk now
  invalidates the backfill arm (`PROFILE_ALLOW_PARTIAL_WALK=1` opt-out) and
  compare_profile dies on a pooled backfill arm with 0 completed-walk seconds — and
  never fabricates a per-column denominator of 1. **M3**: zero-count cuts now report
  +100% with a rule-of-three lower bound (full elimination — the Phase 3/4 target —
  no longer renders "n/a"), and a 0/0 tripwire band prints an explicit UNDETECTABLE
  warning instead of silence. **M4**: backfill_profile.sh gained the same
  base/change ref-vs-ref arms + ABBA matrix as the serve harness (the phase gates'
  "ref-vs-ref … + backfill arm" protocol was otherwise only satisfiable as sequential
  blocks — the 2026-07-29 box-state confound). Minors: pooled per-thread LSS-frame
  section (N1 — the Phase 3 gate arithmetic compare_profile omitted), window-mode
  homogeneity check + hibw header (N2), the ±2% column-parity guard computed and
  flagged (N3), `--selftest` (28 cases) + CI wiring alongside the other checkers —
  including `check_move_trace.py --selftest`, which A1 had left un-wired (N4),
  source-regex call-site pins for the echo on both platforms (N5), the StoreBackfill
  terminal statusLine + "region(s) to process" pins plus a parse_ok guard in the
  harness (N6), standalone `run base` repoints a stale worktree (N7), odd-reps ABBA
  warnings + `--skip-reps` for the warm-up-rep protocol (N8), window-spec validation
  + stale bands.json/flame.collapsed unlink before re-analysis (N9), role-labelled
  compare rows + alloc-truncation note (N10), and the DISK_PATH_MARKERS comment now
  records the reviewer's verified-no-op result (311/319/1 samples checked, zero
  reclassification) instead of a wrong rationale (N11). Post-A/A addition riding the
  same round: the classifier tripwire is now DISPERSION-AWARE (pooled t-test on
  per-rep rates) because the A/A proved the save/probe-correlated bands 5-20x
  over-dispersed vs Poisson — the pooled-Poisson tripwire fired on code-identical
  arms (see the ledger entry).

- **2026-08-07 (B1 implementation decisions, perf plan Phase 1 / R3)**: the `Key`
  wrapper lands as specified (nested in both twins, precomputed one-pass structural
  hash via `CompoundTag.forEach`, SUM combine per compound level, sequential for
  ListTag, equals delegating to `Tag.equals`, no shared hasher state; the stored key
  reuses the probe's hash via `withTag` on the defensive copy — equal content, equal
  hash). One accepted cost the plan didn't enumerate: each lookup allocates ONE small
  `Key` wrapper (ConcurrentHashMap.get needs the key object) — accepted against the
  per-lookup EntryIterator churn it replaces; if the Phase 1 gate shows the wrapper
  eating the win, a thread-local probe key is the next lever (not built — measure
  first). A `capWarnedForTest` seam pins the warn-once latch without log capture.
  Test-fixture finding worth keeping: the plan's vacuous-pass warning was exactly
  right AND subtler than written — sequential keys (`k0..k7`) iterate IDENTICALLY at
  capacity 16 vs 32 under HashMap's spread function (consecutive hashes), so the
  collision-adjacent test uses realistic block-state property names with `open` as
  the verified discriminator (bucket 14-of-16 vs 30-of-32, deterministic — Java
  String.hashCode is spec-fixed) plus a fixture guard that reds if the orders ever
  collapse (backing map or spread change).

- **2026-08-07 (B1 review round, two grouped Opus lenses — correctness+concurrency /
  fidelity+test-adequacy)**: lens (a) 0 MAJORs + 4 minors, lens (b) 2 MAJORs + 6
  minors; all addressed same-day. Lens (a)'s headline is a PROOF, not a finding: the
  structural hash is bit-identical to `Tag.hashCode()` for every vanilla tag type
  (`Map.hashCode`/`List.hashCode` are interface contracts), so the key distribution
  is provably unchanged from pre-R3 keying — and the honest recount (C3/F3, both
  lenses independently): the win is the WALK SHAPE (no `Map.Entry` indirection, no
  megamorphic `Node.hashCode` dispatch), while allocation count is a wash (int[1] +
  capturing BiConsumer per level replaces one EntryIterator; plus one probe Key per
  lookup) — javadoc reworded, and the Phase 1 allocation gate must be read
  accordingly. What R3 does NOT remove (F7, for gate reading): the per-hit
  `Tag.equals` and the per-lookup hash walk itself — the gates target the
  iterator-vs-forEach delta minus Key cost, a smaller fraction than the plan's
  problem statement implies; a gate miss is a design ceiling, not a measurement
  fault, and the next lever (thread-local probe key) must be judged against that.
  Fixed: **C1** `withTag` DELETED — the stored key's hash came from the caller's
  live tag before `delegate.decode` ran, so a mid-window caller mutation could
  poison the map with an unreachable entry (latent today, load-bearing for Phase
  3's new callers); the stored Key re-walks the copy, costing one walk per MISS
  (which already pays a codec decode) — this also moots F4/F8. **C2** null-safe
  leaf (`Objects.hashCode` parity with the replaced walk — `transcodeSection` has
  no exception fallback, an NPE would escape the serializer). **F1 MAJOR**: the
  nested-recursion test was VACUOUS (its two-key sub-compounds iterated identically
  at both capacities — computed by the reviewer); rebuilt on the verified
  8-key diverging set nested under Properties, with the vacuity guard and pinned
  through the memo. **F2 MAJOR**: the Key-nesting contract (marker stability for
  Phases 2-4) is now class-name-pinned in both twins. **F5**: the vacuity guard
  message carries the re-derivation recipe. **F6**: `MemoizedNbtCodec$Key` (+ Paper
  twin) added to DEFAULT_MARKERS as the plan's own named new-key-cost term. **C4**:
  hash==Tag.hashCode + copy-survival + list-in-compound-through-resolve pins added.

- **2026-08-07 (B1 disposition — gates failed, phase reverted)**: per the plan's
  failure handling ("any gate miss reverts or flag-disables the offending phase")
  and Phase 1's rollback line, the Key change was REVERTED after the A/B (ledger
  entry below; plan Phase 1 carries the dated verdict block). What lands from the
  branch instead: the behavior test suites REWRITTEN against raw-tag keying (every
  non-Key pin kept — the capacity-history independence tests still bite, since
  Map.hashCode's order-independent contract is exactly what a future sequential
  re-keying would break), the `capWarnedForTest` seam, the measured-decision note in
  both twins' class javadoc, the analyzer's `has_walk_window` fix (a REAL bug the
  gate run itself found: the `--window walk` batch fallback keyed on the
  `backfill-` directory prefix, but ref-vs-ref backfill arms are named
  `base-`/`change-` — decided by meta content now), and the $Key marker lines
  removed from DEFAULT_MARKERS (dead class). The F6/F7 gate-reading guidance did
  its job: the $Key term made the verdict unambiguous in one A/B.

- **2026-08-07 (B2 implementation decisions, perf plan Phase 2 / R4)**: lands as
  specified — `LodStoreService.contentHash` FNV-1a→CRC32C (`new CRC32C()` per
  call, never shared — the three-thread-family constraint is in the javadoc), zero-
  extended into the 64-bit columns, `SCHEMA_VERSION` 3→4 with the history line and
  rollback-symmetry note, the `fnv1a` wrapper renamed `contentHash` (4 call sites),
  `DirtyContentFilter.fnv1a64` and `RegistryFingerprint.fnvOver` and the soak
  exporter's probe hash deliberately untouched (separate domains). New
  `schemaVersionDriftDropsAndRebuildsTheStore` pin: an out-of-band meta downgrade to
  `SCHEMA_VERSION - 1` is byte-for-byte the old-store-under-new-jar case since
  `metaMatches` is an equality compare (first attempt failed on the meta table's
  actual `k`/`v` column names — the corruption-test's SQLite pattern reused).
  Release-note obligation discharged: `docs/planning/v0.10.0-release-notes-draft.md`
  created (the established draft pattern) seeded with the R4 item + the shipped
  A1/A2 features. **The R-1 deploy hold begins when this stage MERGES** (no live
  Modrinth deploys until C4's final schema-4 shape lands) — noted here because the
  A1/A2 live restart is still pending on the expired panel token; if the token
  arrives after B2 merges, the restart is still safe (the staged jar is v0.9.1-era,
  pre-schema-4) but no NEW builds may be deployed.

- **2026-08-07 (B2 review round, single grouped Opus reviewer — all three lenses)**:
  0 MAJORs + 6 minors, all addressed same-day. **F1 was a caught governing-plan
  divergence**: the first commit DELETED the FNV body while mega plan R-1 requires it
  "renamed but kept" as C4's `wirefmt = 19` legacy-row validator — restored as
  `legacyContentHashFnv` with the C4-pointer javadoc (the initial "exactly as
  specified" decisions text was corrected; the reviewer's exhaustive verification of
  the four-call-site audit, the untouched FNV domains, `CRC32C.getValue()` bytecode
  unsignedness, and the chash+1 mismatch-by-range argument are in the review
  record). **F5** closed the substantive pin gap: every prior hash assertion was
  self-referential, so a silent revert to FNV that kept schema 4 would red nothing
  while arming the C4 landmine — `contentHashIsCrc32cZeroExtended` now pins RFC 3720
  B.4 vectors incl. the bit-31 zero-extension case, plus the legacy FNV offset
  basis. **F4**: the schema-drift test now proves the rebuild fires ONCE (third
  clean open keeps the v4 deposit). **F2**: the release-notes item carries the
  REWRITE-AT-C4 marker (shipped upgrades migrate lazily; the rebuild text is
  dev-window/drift-only). **F3**: the interim-schema line in the `4:` history
  comment + the PR-body paragraph. **F6** (PLAUSIBLE): already implemented — the
  existing R1-review verification block below the delete loop throws on any
  surviving file; no change.

- **2026-08-07 (B3 pre-implementation recon — 26.2 bytecode facts, verified via javap
  + Vineflower on the merged-deobf jar)**: everything Phase 3's design assumes is
  present as assumed. `RegionFileStorage.getRegionFile(ChunkPos)` private,
  never-null-creates-on-demand (the `@Invoker` target). `RegionFile` private members
  for the raw-read mixin to `@Shadow`: `getOffset(ChunkPos)`, `file` (FileChannel),
  static `isExternalStreamChunk(byte)` / `getExternalChunkVersion(byte)`,
  `getExternalChunkPath(ChunkPos)`; `getChunkDataInputStream` is `synchronized` (the
  raw read must be too) and its shape decompiles exactly as the plan's three-branch
  description: offset-0 → null, truncated header (<5) → null, length==0 → null,
  external flag → `.mcc` via `createExternalChunkInputStream` (not-a-regular-file →
  null), truncated/negative stream → null, else `createChunkInputStream` which
  handles `VERSION_CUSTOM` (readUTF the id, log, null), `fromId==null` (log, null),
  else `new DataInputStream(version.wrap(stream))`. **`RegionFileVersion` is fully
  public incl. `fromId(int)` and `wrap(InputStream)` — the pool-side reconstruction
  needs NO mixin**; only the two fetch-side mixins (storage invoker + RegionFile raw
  read) are needed. Decompiled source cached at scratchpad `mc-region/out/` this
  session (regenerate: unzip the class from the merged-deobf jar + Vineflower 1.12.0
  from the gradle cache).

- **2026-08-07 (B3 implementation decisions, perf plan Phase 3 / R1)**: the split lands
  as two accessor-style mixins (`AccessorRegionFileStorage` invoker +
  `AccessorRegionFile` — @Accessor/@Invoker on vanilla's own private helpers, zero
  format re-derivation incl. `SECTOR_BYTES`/`getSectorNumber`/`getNumSectors`) with
  the raw-record logic in a PLAIN class (`RegionFileRawRead`, `synchronized` on the
  RegionFile instance — interoperates with vanilla's synchronized reader, premise
  pinned reflectively) rather than a @Unique duck method — simpler, and the
  reconstruction (`NbtSectionSerializer.parseRawChunk`, all three
  `createChunkInputStream` branches incl. the VERSION_CUSTOM UTF-id log mirror) is
  plain-testable. Dispatch: `chooseRawReadOrNull` mirrors `chooseReadPath`'s rung
  conditions (ladder-agreement pins red a drift); the raw probe NEVER latches/warns —
  `backgroundReaderOrFallback` stays the ONE latch site. **The kill switch IS exposed
  as config** (`useBackgroundReadSplit`, default true — the plan left it open;
  convention favors config for the round's riskiest change; documented Fabric-only in
  effect, rollback narrower than `useBackgroundReadPriority=false`). The plan's
  "oversized-chunk parity gametest OR document the gap" alternative resolves to
  DOCUMENT: the external-`.mcc` file branch has no real-file coverage (synthesizing a
  >1 MB-compressed chunk in a gametest is not reliably cheap); the record-level
  external path is identical to internal past the fetch (same RawChunkRecord shape,
  unit-pinned), and `RegionFaultGameTests` covers corrupt real files end-to-end.
  New suites: split-ladder pins in `ChunkDiskReaderTest` (all eight existing ladder
  pins untouched), `RawChunkParseTest` (per-compression round-trips + unknown/custom/
  absent), the accessor reflective-existence + synchronized-premise pins in
  `ChannelAccessorContractTest`, `backgroundReadSplitDefaultsOn` config pin.

- **2026-08-07 (B3 review round, two grouped Opus lenses — correctness+concurrency+
  bytecode / fidelity+test-adequacy)**: lens (a) 0 MAJORs + 7 minors with the headline
  being a full FAITHFUL verdict (raw-record resolution shape-for-shape vs 26.2
  bytecode incl. the position-5 slice equivalence, executor-confinement threading
  soundness, parse fidelity down to the NbtAccounter overload, dispatch equivalence
  `raw != null ⟺ background-rung state`, timeout-leak claim verified); lens (b)
  1 BLOCKER + 2 MAJORs + 10 minors. **The BLOCKER (F1) was real AND exposed a
  process failure of mine**: `SplitRigReader extends final MoonriseRigReader` did not
  compile — my "Tier 1 green" pre-commit run was `| tail`-piped, which masked the
  failing exit code (the SECOND pipe-masking incident this program; every gate
  command now ends with an explicit `; echo RC=$?`). Fixed: final dropped.
  **F2/F3 MAJORs**: byte parity cannot distinguish the split from a silently-inert
  dispatcher (identical bytes are the point) — the raw path now counts `rawServes`
  and shows `read_path=bg-split, raw_serves=N` in diag (the moonrise-token
  precedent), asserted >0 in the parity gametest; the agreement pin was a
  restatement, now asserts the PAIR through rung counters incl. the missing
  latched-incompatible case. Also fixed: **C2** raw executor task catches Throwable
  (an unapplied accessor's AssertionError would wedge the pool at ~threads/10 s with
  nothing naming the cause), **C3** dedicated raw-parse throttle (sharing the
  section-parse throttle let upgrade-warn floods silence version corruption), **F5**
  CHUNK_HEADER_SIZE shadowed (the one re-derived constant), **F6/C1** the echo line
  carries `useBackgroundReadSplit` (arm provenance for the A7 kill-switch triage +
  the phase gates), **F7** ctor-wiring source pin, **F8** round-trips rewritten
  through VANILLA's own wrap(OutputStream) writers + the LZ4 (id 4) case, **F9**
  triage-site comment (never add an ExecutionException unwrap), **F11** dual-stream
  warn mirrored + monitor-vs-confinement comment (C5) + external-unbounded wording
  (C7), **F12** plan annotation pair, **F13** README row. DOCUMENTED, not fixed:
  **F4** the fetch-side shape ladder (all six record shapes, not just `.mcc`) has no
  unit coverage — mixin-package classloading; compensating controls are lens (a)'s
  shape-for-shape bytecode verification, the parity gametest (internal happy path,
  now with the liveness receipt), and RegionFaultGameTests; extracting a pure slicing
  core remains open as a B3 follow-up if Phase 4 touches this file. **F10/C4** the
  split drops vanilla's JvmProfiler ChunkRegionRead JFR event and vanilla's per-shape
  corrupt logs (one throttled LSS warn instead) — observability delta accepted and
  recorded. **C6** both accessors apply under `required: true` on every Fabric
  server incl. Moonrise/C2ME boxes where the raw path is unreachable — an MC-bump
  rename becomes a load failure there instead of a graceful degrade; accepted (the
  contract test reds first at a bump), stated in the PR body.

- **2026-08-07 (B4 pre-implementation recon — 26.2 NBT API facts, verified via
  Vineflower + javap)**: the selective loader needs NO mixins — everything is public:
  `Tag.TAG_COMPOUND`, `StringTag.skipString(DataInput)`, `TagTypes.getType(int)`,
  `TagType.load(DataInput, NbtAccounter)` / `skip(DataInput, NbtAccounter)`,
  `NbtAccounter.unlimitedHeap()` (what `NbtIo.read(DataInput)` uses — accounting is a
  no-op on it), `new CompoundTag()` + `put` (the Map ctor is package-private). Root
  protocol to mirror (decompiled `NbtIo.readUnnamedTag`): type byte (must be
  TAG_COMPOUND or vanilla throws "Root tag must be a named compound tag"), then
  `StringTag.skipString` for the root name, then the compound TagType's
  pushDepth/loadCompound/popDepth; the load loop is `while ((t=readByte()) != 0) {
  key=readUTF; TagTypes.getType(t).load-or-skip }`. The serializer's root accessors
  are exactly TWO — `chunkNbt.getStringOr("Status", …)` (:312) and
  `chunkNbt.getList("sections")` (:324) — matching the plan's audited `{Status,
  sections}` whitelist; the drift pin regexes those literals. Wiring: parseRawChunk
  gains the selective attempt on its own fresh wrap, throw → full `NbtIo.read` on
  ANOTHER fresh wrap (free — byte[] source); flag threads config →
  ChunkDiskReader 5-arg ctor → the raw overload; the echo line's anticipated
  `useSelectiveNbtParse` append lands here with both format pins.

- **2026-08-07 (B4 implementation decisions, perf plan Phase 4 / R2)**: lands per the
  plan — `SelectiveChunkNbtLoader` (no mixins; root protocol mirrors
  `NbtIo.readUnnamedTag` exactly incl. vanilla's non-compound-root message, load-or-
  skip loop on `unlimitedHeap` with pushDepth/popDepth kept for a future bounded
  accounter), whitelist `{Status, sections}`, wired at `parseRawChunk` behind
  `useSelectiveNbtParse` (default true; ChunkDiskReader 5-arg ctor; the raw overload
  gained the flagged flavor). Fallback: any selective throw → full `NbtIo.read` on a
  fresh wrap of the same buffer. The echo line's anticipated append landed with both
  format pins; PROFILE_SELECTIVE_PARSE staged in both harnesses with a
  TOLERANT-WHEN-ABSENT echo assertion (pre-B4 base arms echo no such key — only an
  explicit mismatch invalidates). The contract suite pins: tag-equality selective-vs-
  full + exactly-whitelist keySet (serializer byte-equality FOLLOWS via the whitelist
  pin — the corpus suite owns the serializer's own bytes; its registry rig is not
  duplicated), 24 shuffled root orderings, the documented one-directional divergence
  POSITIVELY (invalid modified-UTF8 in a junk root string: full parse throws,
  selective skips and serves — with the flag-OFF strictness as the rollback pin),
  truncated-payload identical-throw + triage, non-compound root, and the
  whitelist-drift source pin (lives in the loader's own suite — same package as the
  package-private whitelist; the first placement in ChannelAccessorContractTest was
  retracted for package access).

- **2026-08-07 (B4 review round, single grouped Opus reviewer — all three lenses)**:
  1 MAJOR (PLAUSIBLE) + 9 minors, all addressed; the in-flight kill-switch gate
  session was STOPPED and discarded so the gates measure the fixed code. **B4-1
  MAJOR — the round's subtlest finding**: the array-type skips
  (`skipBytes(readInt() * width)`) silently consume ZERO bytes on a negative or
  int-overflowing corrupt length where `load` would throw — a desynced loop could
  then hit a stray 0 and return a garbage-shaped sparse tag WITHOUT throwing,
  breaking the one-directional-leniency guarantee every doc asserts. Fixed by the
  EXHAUSTION CHECK: the raw record's payload is exact-length, so a well-formed parse
  ends at EOF — one `read()` per column proves it, and every desync now routes into
  the full-parse fallback (one-directionality true BY CONSTRUCTION; the check also
  created the fallback-SUCCEEDS test vector — trailing bytes throw selectively while
  vanilla's full parse ignores them). **B4-2**: the ordering fuzz was ~vacuous
  (CompoundTag.write iterates its HashMap — 24 shuffled INSERTIONS produced 4 byte
  layouts with Status always first); rebuilt on MANUAL byte emission with true
  ordering control + directed extremes. **B4-3**: the diag receipt now carries
  `sel=on/off, sel_fallbacks=N` (the B3 raw_serves precedent — liveness, rollback
  receipt, and the divergence-rate instrument in one token). **B4-4**: the fallback
  warn got its own throttle + message (it shared the raw-parse throttle the B3 C3
  fix had deliberately split, and composed a self-contradictory line). **B4-5**:
  the four accountBytes calls mirrored (inert under unlimitedHeap; makes the
  bounded-accounter claim true). **B4-7**: SelectiveChunkNbtLoader added to the
  analyzer's `nbt` band (its readByte/readUTF leaves classified lss-other, migrating
  part of gate 2's numerator out of the gated band). **B4-8**: both default-true
  convenience overloads DELETED (the silent-ON hazard behind B4-3). **B4-9**:
  selective-over-DEFLATE + corrupt-array-desync-must-throw + fallback-succeeds
  cases added (Tier 1 previously drove selective only through VERSION_NONE).
  **B4-6**: alias-rebinding evasion pin on the whitelist regex. **B4-10** cosmetics
  incl. the mislabeled test renamed. Cleared by the reviewer: root protocol faithful
  (readString == readUTF under unlimitedHeap; depth semantics exact; duplicate keys
  last-wins both paths; fresh re-wrap genuinely fresh; single production consumer
  chain; 0xC0 0x00 verified genuinely invalid modified-UTF8; Tier 2 parity covers
  selective over a real DEFLATE region file).

- **2026-08-07 (C0 pre-implementation recon — 26.2 API facts via javap on the loom
  named jar + repo facts via a read-only sweep)**: MC surface for the identity
  tables: `Block.BLOCK_STATE_REGISTRY` is `IdMapper<BlockState>` with BOTH
  directions (`getId`/`byId`) plus `iterator()`/`size()` — the fingerprint's
  "iteration order IS global-id order" doc carries over; `StateHolder.getProperties()`
  returns `Collection<Property<?>>` and canonical values come from the abstract
  `Property.getName(T)` stringifier (NOTE: 26.2's `getValues()` returns
  `Stream<Property$Value<?>>`, not the old map — use getProperties+getValue);
  `StateDefinition.getProperty(String)` exists for the decode rung; block names via
  `BuiltInRegistries.BLOCK.getKey` → **`net.minecraft.resources.Identifier`** (26.2
  renamed ResourceLocation — a D3 re-port catalogue item), with
  `isValidNamespace/isValidPath` public for the charset pin; biomes are the dynamic
  `Registries.BIOME` (per-`RegistryAccess`, platform-supplied — the
  `RegistryFingerprint` pattern: platform extracts `Iterable<String>`, common stays
  MC-free). Repo facts: `common/` has ZERO VarInt/wire primitives (the cursor
  hand-rolls MC-format VarInt; a fabric-side cross-check vs `FriendlyByteBuf` pins
  equivalence); the transcode emit shape is `TranscodedBody.writeContainer`
  (`NbtSectionSerializer:311-324` — bits byte, bits==0 single VarInt else VarInt
  count + entries, raw big-endian longs with NO length prefix, count implied by
  bits: valuesPerLong=64/bits, entries 4096 blocks / 64 biomes); native
  palette-vs-DIRECT thresholds blocks >8 bits / biomes >3 bits; the committed
  `nbt-corpus` goldens are exactly the native section-array layout (VarInt count +
  sections) — corpus-driven cursor walk checks live fabric-side where both the
  corpus and FriendlyByteBuf are reachable; the settling-measurement template is
  `CompressedColumnsExperimentTool` (self-skipping JUnit, plain
  `Bootstrap.bootStrap()` + `VanillaRegistries.createLookup()` registries, direct
  `.mca` reads, gradle property pass-through in fabric/build.gradle:92-100, real
  zstd-1 frames); §8's corpus (`test-server/paper` world) is present on disk
  (12 overworld .mca, 37 MB). Scope decision: the settling encoder IS the
  byte-domain **`NativeToV20Translator`** (native wire → v20 wire over
  WireSectionCursor with an injected id→identity lookup) — a production component
  the C4 migration walk reuses, not throwaway harness code; the v20→native
  direction (legacy egress) stays C2 per the landing order, so §9's bijection fuzz
  lands there. C0 lands INERT to main (no serve-path wiring — that is C1, behind
  the integration-branch constraint).

- **2026-08-07 (C0 implementation decisions, XVER Phase 0)**: lands INERT to main —
  no serve path consults any of it until C1. `common/wire/` is the new package:
  `WireBytes` (hand-rolled MC-format VarInt/short/long/UTF over `byte[]`, bit-identity
  pinned fabric-side against the real `FriendlyByteBuf`), `WireFormatException` (the
  single pinned failure mode for hostile input — array errors and wire-claimed
  allocations are test-pinned OUT), `IdentityCodec` (canonical
  `namespace:path[k1=v1,…]` form, strict parse accepting ONLY canonical spellings —
  a tolerant parser would let two dictionary spellings encode one state; charset
  grammar excludes `[ ] , =` + whitespace, the no-escaping premise as code),
  `IdentityDictionary` (first-seen indices, validates at insertion),
  `WireSectionCursor` (both layouts parse/scan/emit; native palette-vs-DIRECT
  thresholds blocks >8 / biomes >3 bits; implied long counts 64/bits; v20 width
  ceilings 12/6 parse-enforced; the clear-column `dictCount==0 ⇔ sectionCount==0`
  invariant enforced on BOTH parse and emit), and `NativeToV20Translator` (the §4.2
  encode direction: indexed containers keep bits+longs VERBATIM with palettes
  rewritten to dict indices; DIRECT re-palettizes first-seen and repacks at the
  §2.1 widths — blocks n==1→0 else clamp(ceillog2,4,12), biomes ceillog2; count
  shorts + light pass through verbatim). Platform `IdentityTables` +
  `PaperIdentityTables` (textual twins): blocks from `Block.BLOCK_STATE_REGISTRY`
  (ids via `getId`, not iteration position), biomes per-registry builders (callers
  memoize at C1 wiring). Test posture: the corpus cross-check
  (`WireSectionCursorCorpusTest`) walks all 14 committed nbt-corpus goldens —
  scan-to-exact-length + parse→emit byte-identity, with a DIRECT-coverage guard so
  the suite cannot silently lose its teeth; `IdentityTablesTest` +
  Paper twin sweep the ENTIRE ~29k-state registry (canonical + strictly-parseable +
  globally-unique + exact inverse-mirror), with character-identical spot-pin
  literals as the cross-platform anchor; truncation containment fuzzes EVERY strict
  prefix of both layouts. The settling tool (`V20SettlingExperimentTool`) mirrors
  the experiment-tool contract (self-skipping, `-Dlss.store.experiment.regionDir`
  pass-through — zero gradle changes).

- **2026-08-07 (C0 review round, single grouped Opus reviewer — all three lenses;
  0 CRITICALs, 6 MAJORs + 11 minors + 7 notes, all addressed or explicitly
  deferred)**: the cleared list is unusually strong — the reviewer verified the
  layout model against 26.2 BYTECODE (disassembled `PalettedContainer$Data.write`,
  `Strategy$1/$2` width→shape tables, `VarInt.read` truncation semantics,
  `StateDefinition.NAME_PATTERN`) rather than trusting repo comments. **MAJOR 1**:
  v20 palette indices now range-checked against the dictionary on BOTH parse and
  emit (referential integrity is the cursor's job, not each consumer's).
  **MAJOR 2**: native palette values read as non-negative + bounds-checked
  `blockIdentityFor(int)`/`BiomeTable.identityFor(int)` accessors on both
  platforms (a bare `table[id]` turned corrupt input into AIOOBE past the pinned
  failure mode). **MAJOR 3**: `dictCount` no longer pre-sizes the dictionary list
  (§2.3's hostile-allocation pin, dictCount included — the gross-case remaining
  guard alone allowed ~20× amplification). **MAJOR 4 — the round's headline**:
  vanilla's width→SHAPE tables make block bits 1-3 ILLEGAL on the native wire
  (indexed blocks exist only at 4-8; a bits-2 frame desyncs a real `section.read`
  by 128 longs) — the cursor now enforces the shape table on parse AND emit, so
  cursor-accepted ⇒ vanilla-decodable is a real invariant before C2's egress
  translator exists to violate it. **MAJOR 5**: the §2.1 v20 block width FLOOR
  (4) enforced alongside the ceilings. **MAJOR 6 (spec hazard, plan-text pair)**:
  §4.1's "canonicalize straight from disk NBT, no registry resolution" is
  RETRACTED — amended in the XVER plan to the pinned rule "wire identities are
  always the identity of a state RESOLVED in the emitting server's registry"
  (the raw-spelling reading would break disk/live byte parity whenever codec
  leniency fires, and void §4.2's lossless-egress premise; the memo resolves
  every palette entry anyway, so the id→identity lookup is one array index).
  Minors: emit-side identity-length cap + sectionY/count range checks (silent
  truncation), settling tool now FAILS on excluded columns (self-invalidating
  size gate), corpus-driven translator test (voxel-for-voxel over all 14 goldens
  incl. duplicate-palette + masked), golden count pinned at 14, Paper biome-table
  twin test, VarInt READ-parity on non-canonical forms (MC truncates the 5-byte
  overflow form — verified identical), box-free first-seen remap (no
  Integer-boxing per voxel on the C4 migration path), null-property loud failure
  in `canonical`, biome null-key → throw at table build, `scan` limit overload,
  writer pre-sizing, shared-array + deliberately-unvalidated-parse javadoc
  notes. Deferred with rationale: split-package note (repo test convention),
  parse-copy costs (C2's legacy-dialect benchmark arm owns that question, N20).

- **2026-08-07 (C1 pre-implementation recon — repo facts via read-only sweep;
  branches: work on `feat/protocol-20-c1`, PRs target the `feat/protocol-20`
  integration branch — C1–C5 land as ONE release branch, main stays shippable)**:
  the bump is `LSSConstants.PROTOCOL_VERSION` :20 (19→20; append the "20:" changelog
  line, add `V19_COMPAT_PROTOCOL_VERSION = 19`). All three produce paths converge
  on ONE choke point — `OffThreadProcessor.buildAndEnqueueColumnPayload` (:433
  abstract; Fabric/Paper bodies decide the codec byte per recipient) — with raw
  section-array bytes wrapped in `ColumnBytes.ofRaw` (zstd lives in
  `ColumnBytes.frame()`); the v20 emit therefore lands in the three PRODUCERS
  (SectionSerializer + Paper twin; NbtSectionSerializer transcode+object paths +
  twin; generation reuses SectionSerializer on both platforms), not the choke
  point. Gate: `HandshakeGate.WireDialect` = CURRENT/V18/V16, ladder at
  `evaluate(...)` :96-119 — C1 inserts the `== 19 && v19CompatEnabled → V19` rung
  + 7-arg overload; Fabric marks inline before registerPlayer
  (LSSServerNetworking :251-266), Paper ONLY via `dialectFlipFor` (:359-377) on
  the pump. `WireDialectTracker` replaces `V18CompatTracker` (API + lifecycle
  contract in its javadoc :18-29 must be preserved verbatim; Paper quit-race drop
  at PaperRequestProcessingService :595; dimension-change removePlayer touches
  neither). The five `Dialects:` diag surfaces confirmed at:
  DiagnosticsFormatter `v18Line` :47 (+render :219-221), DiagnosticsFormatterTest,
  CommandGameTests :251, PaperCommandsTest :170, exporter contracts (schema
  UNAFFECTED — neither mentions v18). SessionConfig encoder never branches on the
  version VALUE (the v18 echo trick reuses the 4-field encode — v19 same);
  read side drains foreign layouts at :101, so the S2C data-version APPEND is
  safe only for version==20 frames (new 5-field read arm). Handshake C2S stays
  FROZEN 2-VarInt; `lss:client_info` is a NEW channel (legacy servers discard
  unregistered channels; Paper decodeHandshake already tolerates absent caps).
  Tracer: `DIALECTS = {"v16","v18"}` at check_move_trace.py :31 widens to v19 +
  MoveTraceTelemetry :199-211 + MoveRowGoldenTest fixture rows (writer/reader
  same-PR rule). Config: `enableV19Compat` beside enableV18Compat (~:226, no
  clamp) + both compat-defaults pins + the check_soak.py allowlist entry.
  **Two architectural decisions from the recon flags**: (1) v19 egress is a BODY
  translation, not a header splice — and once v20 is the canonical internal form,
  the existing v18/v16 splices would ship v20 BODIES (unreadable by those
  clients); C1's intermediate state on the integration branch therefore DROPS
  column egress for ALL THREE legacy dialects behind once-warn latches (the
  unconvertible-guard pattern), and C2's translators restore them (v19 =
  translate; v18/v16 = translate + existing splice composition, exactly §4.2);
  any v18/v16 Tier-2 pin that asserts column DELIVERY gets adjusted to pin the
  drop-latch intermediate with a C2-restore marker — handshake/echo pins stay.
  (2) `ColumnBytes` stays single-form (v20 raw + v20 frame); per-recipient
  translation happens DOWNSTREAM of the holder at C2 — C1 must not paint that
  corner. Client side: decode reuses the §2.3 translator-with-client-registry
  shape (v20 → local native via the fallback ladder, then the EXISTING
  `section.read` path at ClientColumnProcessor :454-455); the §3 fallback ladder
  + `unknownBlockFallback`/`crossVersionBlockFallbacks` client config keys are C1
  scope ("client decode/fallback ladder"), the OLD-SERVER discovery ladder is C3.

- **2026-08-07 (C1 implementation decision — the v20 emit is UNIFORM
  translate-at-producer, not three direct emitters; plan deviation recorded
  against §2.1's "the v20 emitter replaces section.write wholesale" + §4.1's
  per-path emit)**: all three produce paths keep emitting NATIVE bytes exactly as
  today, and each producer translates native→v20 at its return boundary via the
  C0 `NativeToV20Translator` (the corpus-proven canonical encoder) with the
  platform identity tables — Fabric+Paper `SectionSerializer.serializeColumn`
  (covers live probe AND generation) and `NbtSectionSerializer.serializeChunkNbt`
  (covers disk + backfill deposits, so store rows hold v20 per §4.1). Why: ONE
  encoder instead of four (two platforms × live/transcode container surgery),
  byte-determinism across paths BY CONSTRUCTION (disk/live parity reduces to the
  already-pinned native parity + a deterministic function), the DirtyContentFilter
  hash and store deposits stay consistent for free, and the C0 review's
  containment work applies to every byte produced. Cost accepted as a C1
  INTERMEDIATE: one parse+re-emit per serve (~30-45 KB memcpy + palette walk,
  the C0 review's N20) — the direct transcode-path emit (ids are already
  memo-resolved; §4.1's cheap path) is the recorded OPTIMIZATION FOLLOW-UP, gated
  on C6's benchmark arms (R-4 already requires native-v20 + legacy-dialect arms;
  if the arms show the translate cost matters, the direct emit lands before D2 —
  the produce-path outputs are byte-identical either way, so swapping later is
  invisible to every golden). Golden strategy: the committed `nbt-corpus`
  NATIVE goldens are RETAINED (the cursor corpus test's fixtures + C2's
  translation-chain targets); the serializer suites gain a sibling v20 corpus
  (`v20-corpus/`) generated by the same builders through the new produce-path
  output, byte-compared cross-module like the native one.

- **2026-08-08 (DEVIATION — live-matrix cell 3 re-assigned C6 → D3; plan-text
  pair applied)**: the C6 review's C-3 demanded an explicit resolution of the
  plan's self-contradiction (mega-plan C6 gate named the guard cell; C5's
  entries assigned the live pull to D3). Resolved per the program's autonomous
  directive: **cell 3 moves to D3**, where its sibling cells 1–2 already live
  and need the same rig + real clients. Reasons: (1) the pull requires a live
  GUI client join (`run-fabric-via` + an lss-multi-test profile) that headless
  automation cannot drive — the user was flagged twice without reply, and the
  goal directive is autonomous end-to-end execution; (2) nothing irreversible
  rides on it — this program NEVER pushes tags (standing user directive), so
  D3's own "cells green before any tag" gate still guarantees the pull happens
  before any publish; (3) the guard's automated coverage (probe-seam stubs,
  gate matrix, production-ladder gametest, wiring pins) is complete — the live
  pull validates the REAL Via integration, which D3's matrix session does on
  the same rig. Mega-plan C6 row amended (the §6 pair). The
  feat/protocol-20→main merge proceeds with the M-6 atomic build.yml +
  ReleaseWorkflowContractTest change inside the PR.

- **2026-08-08 (C6 grouped review round + fix round)**: verdict — c6→
  feat/protocol-20 MERGE after C-1; feat/protocol-20→main blocked on C-3
  (user decision), C-2, M-1, M-6. Executed: **C-1 CRITICAL (empirically
  proven)** — the first c6-dialect matrix was contaminated (the dialect19
  arm's exported client args survived into later native arms; 7/8 runs
  negotiated v19, the A/B was an A/A and the recorded n.s. verdict is a
  HARNESS ARTIFACT — the v2 re-run with the fixed harness — `profile-results/c6-dialect-v2`, all
  8 runs arm-validated clean: every native arm negotiated v20, every dialect
  arm v19, no cross-contamination; the validity grep needed one pattern fix,
  `(protocol vN,` not `vN)` — MEASURED THE HONEST A/B: **µs/col +0.3% n.s.
  [−3.9, +4.4]** (native 247.1 vs dialect19 246.4, consistent with c6-serve's
  change arm 244.3), ceiling n.s.; the real cost is **alloc/col 514 KB →
  636 KB (+24%)**, the per-column v20→native translate + zstd recompress
  buffers. The contaminated matrix's conclusion therefore SURVIVES clean
  re-measurement: C2's memoized enqueue translation is CPU-free at pipeline
  level for a single legacy client, with a measured allocation cost; §12
  retired, now with receipts. The serve/backfill arms were verified clean in
  the same review and their verdicts stand unchanged.) **C-2 CRITICAL** — a JSON-null curated-table VALUE passed
  validate(), NPE'd Map.copyOf at resolver construction inside the decode
  drain (no ingest-failure report, LOD dead for the session): validate() now
  drops null/blank entries fail-open, resolverFor degrades (null resolver →
  per-column ingest failures via the drain's existing containment), test
  added. **M-1 MAJOR (mine from the C4 fix round)** — the walk's poison bound
  swallowed the legitimate all-air shape (usize 0): a real v0.9.x upgrade
  DELETED every End/void column as "corruption". The walk now retags usize-0
  rows bare (mirroring the serve path's short-circuit), SoakStoreDowngrade
  synthesizes an all-air row per dim so the live gate covers it, Tier-1 retag
  test added. **M-2** — the corpus decode test now compares identity-at-every-
  voxel + count-short pass-through (an index-remap off-by-one was green).
  Minors taken: m1 (soak.sh loud death on unreadable PROTOCOL_VERSION), m8
  (trace Boolean hoisted), m10 (scenario-key allowlist). **Recorded
  follow-ups, not done in C6**: M-3 (drift-capable fixture + identity
  manifest — D3-prep, where both registries are in hand), M-4/M-5 (resolver
  toNative cases + registry-size drift case — ride the D3 template
  strengthening), m2/m3/m5/m6/m7/m11/m12/m13/m14 (test/doc polish), m9
  (CLAUDE.md dirtyTrace wording oversells single-scenario wiring). **M-7
  recorded here**: the full soak sweep RAN on this tree 2026-08-08 06:13–
  08:34Z (Fabric 19/19 — dimension-rejoin-warm was one catalog A7
  environmental red, re-run green; Paper 5/5 — unfired-event green after the
  sheep fix; Folia 4/4; every client row carried session_version 20); the
  bandwidth-throttle MARGIN re-check (§9's raw-growth item) has NO dedicated
  evidence beyond the scenario's generic 5%-cap law passing — recorded as an
  accepted gap (the +5% raw growth sits well inside the law's tolerance
  band). **M-8 recorded**: the windowed unfired-event premise is deliberately
  weaker than whole-run flatness (an ambient mark ON the edited chunk outside
  the window would mis-attribute the heal — position-blind counters can't
  close it; the exact per-position fix is noted in the review for a future
  round). **M-6 (merge mechanics, verified by the reviewer)**: the main merge
  commit must atomically change build.yml:5-11 (drop the feat/protocol-20
  entry + its comment) AND ReleaseWorkflowContractTest:185's pinned literal
  to the 2-entry form — both halves red symmetrically if split; it must ride
  INSIDE the PR into main (pull_request filters on the TARGET branch).
  **C-3 stands: cell 3 (the Via guard live pull) awaits the user** — run it
  on the C5 rig, or direct a dated re-assignment to D3 with the mega-plan
  gate-text amendment.

- **2026-08-08 (C6 benchmark arms MEASURED — R-4 discharged; the C1 direct-emit
  follow-up is TRIGGERED)**: three matrices, 4+4 reps × 150 s ABBA each, R=256,
  box exclusive (`profile-results/c6-serve`, `c6-backfill`, `c6-dialect`).
  **Native-v20 serve arm** (base = the B4 merge `e079d30d`, the B5-accepted
  tree): serve lss µs/col 206.2 → 244.3 = **+18.5% rise [13.4, 23.7]**,
  alloc/col 324 KB → 515 KB (+59% — movers are the translate's byte[]/long[]
  buffers + IdentityCodec machinery), hibw ceiling 604.6 → 572.0 col/s (−5.4%).
  This is C0-N20/C1's accepted translate-at-producer intermediate cost, now
  measured. **Backfill arm (the C1-13 save-cost check)**: µs/col 170.8 → 199.6
  = **+16.9% [11.4, 22.6]**, alloc +59% — but deposit throughput UNCHANGED
  (844.2 → 852.1/s): the save-path translate (DirtyContentFilter hashing
  through serializeColumn) rides inside existing thread headroom and does not
  gate the walk; serialize marker +5.3% n.s.; serialize-live tripwire 0/0
  (normal for backfill windows, noted per the tooling rule). **Legacy-dialect
  arm** (same tree, native vs `-Psoak.dialect=19` via the new
  BENCHMARK_CLIENT_GRADLE_ARGS seam): µs/col **−0.5% n.s. [−4.7, +3.5]**,
  ceiling n.s. — C2's memoized enqueue-time translation is pipeline-free for a
  single legacy client; XVER §12's translation-cost concern is retired for
  this shape. **Consequence: C1's recorded conditional fires** ("if the arms
  show the translate cost matters, the direct emit lands before D2") — both
  ref-vs-ref CIs exclude zero. The direct transcode-path emit (ids are already
  memo-resolved; emit v20 directly, no native parse+re-emit; byte-identical
  outputs so every golden is untouched) is now a SCHEDULED work item: its own
  small PR after C6's merge, before D2, review-gated, targeting the +18.5%
  serve rise. Release-note framing for D1: perf-round wins are quoted vs the
  same-release baseline; any end-to-end v0.9.x→v0.10.0 claim cites the
  native-v20 arm per R-4 (serve CPU/col regresses ~18% for the cross-version
  capability; backfill throughput unaffected; wire +~10%).

- **2026-08-08 (USER DIRECTIVE — D0: timestamp-cache tile redesign worked into
  the mega plan; plan-text pair applied)**: `timestamp-cache-tile-redesign.md`
  (authored 2026-08-08, status PLANNED, reviewed) becomes stage **D0**, slotted
  between C6 and D1: per-region flat tiles (`int[1024]` u32 epoch-offset stamps
  from `TS_EPOCH_SECONDS` 2025-01-01, slot 0 = absent, every clamp rounds UP —
  the safe direction per its §2.2), the parallel `insertionTimes` map and the
  O(n log n) `evictOldest` machinery deleted, tile-granular oldest-`lastTouch`
  eviction under a byte cap, persistence v2 with in-place v1 migration, AUTO
  sizing re-pointed to spend part of the ~13x per-column win on COVERAGE
  (`TIMESTAMP_CACHE_AUTO_COVERAGE_FACTOR = 8.0` — ~3x less heap while tracking
  ~5.3x more columns at default distance). Sequencing constraints (the
  directive's point): fresh `feat/tscache-tiles` off main only AFTER C6's
  integration merge; MUST land BEFORE the pre-D3 whole-program 3-Fable review
  (its diff is in that review's scope) AND before the D3 re-ports (the backport
  lines carry it from their first v0.10.0 build). No kill switch (design §10 —
  differential-fuzz-proven router equivalence; the failure direction that
  matters is covered by the clamp rules + dirty-* soaks; lost stamps self-heal).
  Gates = the design's §8; D1 release notes carry its §9 Performance item.
  Mega-plan edits: stage-table D0 row + the "D0 sequencing" landing-order block
  + D1 row amendments.

- **2026-08-08 (D0 IMPLEMENTED + gated, on `feat/tscache-tiles`; commits
  `7b7bc7a2` impl, `f3ab1a25` fix-forward, `6b71b17b` emptied-save fix)**: the
  full §8 ladder is green. The impl commit's ten downstream test casualties were
  all the same clamp-drift class (pre-epoch fixture stamps reading back as
  epoch+1): router-test fixtures shifted to a `TS_BASE` helper (+ a NEW
  router-level §2.2 pin — a pre-epoch seeded stamp must RE-SERVE an equal
  pre-epoch claim), TimestampSaveScheduler helpers shift both ways,
  `COLUMN_TS` bumped past the epoch in both OffThreadProcessor suites, the
  exporter eviction case reshaped to tile granularity, the config AUTO pin
  re-derived exactly (12 MB at 256). Two REAL finds rode the red suite: (1) the
  first skip-empty `save()` would have left an EMPTIED cache's stale file to
  resurrect invalidated stamps on reboot — caught by
  `invalidationTriggersADebouncedSave…`; save() now separates never-touched
  (no file) from emptied (clobbering live-tiles-only write), belt-pinned both
  directions; (2) `MemoryLodStore.awaitQuiesceForTest`'s queue-empty + 20 ms
  beat missed a polled-but-mid-apply deposit (transiently visible inside
  applyDeposit's put→recheck window) — replaced with exact offered/retired
  accounting. §8 completions the impl commit lacked: the §5 constants coherence
  pin, v1-golden 0/negative-stamp ABSENT arms, the no-empty-tile invariant pin.
  §9 doc sweep done (CLAUDE.md bullet/test-phrase/AUTO/#62 figure, config-review
  §7.3 erratum, miss-memo mbToEntries note). Gates: Fabric T1+T2 + Paper T1
  green (3 full-gauntlet runs); soaks 7/7 PASS — warm-rejoin on the PRESERVED
  v1 base (Fabric migration arm: `Migrated timestamp cache v1 → v2 (2144
  entries → 4 tiles)` + 0 violations), fresh-backfill (base regenerated v2),
  warm-rejoin on v2 (no migration line — native load), cold-restart-resync,
  dirty-broadcast, dirty-while-offline, and `SOAK_PLATFORM=paper warm-rejoin`
  against the still-v1 base-paper (the Paper migration arm — same INFO line,
  0 violations). §8's manual `test-server.sh` v1-world arm REMAINS OPEN as a
  user-optional smoke (the automated arms exercise the same load path). Next:
  grouped review round, single PR to main.

- **2026-08-08 (D3 COMPLETE as PREP — both fresh re-ports GREEN; no tags
  pushed, per the governing directive)**: `support/mc26.1-v0.10` (tip c435396e)
  and `support/mc1.21.11-v0.10` (tip 9cf05332), both cut fresh from main per
  XVER §11.2, both CI-green twice INCLUDING Tier 3, both release-gate green
  (`0.10.0+26.1.2` / `0.10.0+1.21.11` jar sets, release_check OK). Highlights:
  the 26.1 line needed only the publishServer single-overload flavor + the
  xray-masked regeneration (R-7's three surfaces verified IDENTICAL — tracer
  fully sound); the 1.21.11 line carried the big catalogued deltas (Java 21
  toolchain + loom-remap, accesswidener named-namespace, ScopedValue shim
  pass-through, the ONE-count-short wire divergence flavored through
  WireSectionCursor/serializers with v20 keeping both shorts per §2.1,
  per-line-AND-per-module nbt-corpus regeneration via new RegenTool twins,
  corrupt-region triage label divergence) — and the tracer ships FULLY
  FUNCTIONAL there too, R-7's degrade allowance unused. **The automated
  issue-#85 proof holds on both lines**: the committed 26.2-captured
  xver-live-corpus decodes STRICTLY (zero identity fallbacks) on 26.1.2 AND
  1.21.11. The C6-recorded M-3/M-4/M-5 follow-ups landed on main (PR #112:
  the drift arm + identity manifest + whole-body toNative pins). The old
  support branches are UNTOUCHED (archival/rename is release-time branch
  surgery, the user's call). Deferred user-assisted set (recorded in the
  checklist): matrix cells 1a/1b/2a/2b/3 (GUI joins), the Prism profile half
  of the rig refresh, the reverse-arm native corpus captures per line, the
  Modrinth live deploy (archon token), the tri-release tag pushes + branch
  archival. One repeat observation, not yet cataloged: MemoryLodStoreTest's
  tombstone test failed ONCE mid-run on each port (passed all reruns) — a
  third sighting should get a catalog entry.

- **2026-08-08 (D3 STARTED — re-ports prep; NO tags will be pushed)**: the
  26.1 re-port is running as a delegated build on branch `support/mc26.1-v0.10`
  (fresh from main per XVER §11.2 — the old support branches are reference
  only, frozen at v0.8.0). The R-8 verification-matrix checklist (manual smoke,
  per the support-line effort budget — the automated load rides each line's CI):

  **D3 verification matrix (checklist)**
  - [ ] cell 1a: 26.1 line same-MC native smoke (join → cold arc → warm rejoin
        w/ store hits → `Dialects: v20=1` → store status clean; Fabric AND Paper)
  - [ ] cell 1b: 1.21.11 line same-MC native smoke (same ladder)
  - [ ] cell 2a: cross-MC v20 — 1.21.11 client (its line's v0.10 build) → Via'd
        26.2 server (terrain eyeball, `/lss trace` fallback counters bounded,
        no decode loops) — THE issue-#85 end-to-end proof, needs the backported
        client + a GUI: user-assisted
  - [ ] cell 2b: cross-MC v20 — 26.1 client → Via'd 26.2 server (same ladder):
        user-assisted
  - [ ] cell 3 (re-assigned from C6): the Via mismatch-guard live pull — old-LSS
        client via Via → denial messaging + `LSS_VIA_GUARD=0` kill-switch A/B:
        user-assisted (needs a GUI join)
  - [x] lss-multi-test refresh (automatable half DONE — header retargeted, Via staging added, all SIX v0.10.0 RC jars installed: 26.2 pair from main@84096313, 26.1 pair from c435396e, 1.21.11 pair from 9cf05332; the Prism client-profile half is user-side): SERVER_DEFS → v0.10.0 RC jars from main + the
        two fresh branches, Via dimension on the 26.2 pair, per-line Prism
        profiles gain the backported clients, keep one v0.9.1 legacy profile;
        close every session with ./run.sh stop + the pgrep check
  - [x] per-line pre-flights (26.1: gate OK, CI 2x green incl. local T3; 1.21.11: gate OK 3x, CI 2x green incl. T3): CI=true build + release_check.py --version 0.10.0
        green on BOTH fresh branches
  - [x] tag texts prepared for all three lines (NO pushes)
  - [x] issue #85 answer drafted (posted only at the real tri-release)

- **2026-08-08 (pre-D3 whole-program 3-Fable review EXECUTED + fix round
  MERGED — the user-directed gate is DISCHARGED)**: three Fable lenses over
  `1f6e8a33..main` (the full program, 245 files). Verdicts: wire/protocol and
  pipeline/perf lenses clean of release blockers; the gates/docs lens found 5
  MAJORs, all fixed same day on PR #109 (headline: the store-migration gate's
  overlap premise was timing-vacuous — now a held-walk + HELD/join/RESUMING
  ordering assertion, LIVE-PROVEN; the legacy-FNV chain was self-anchored —
  now pinned to published FNV-1a-64 vectors). Full findings + dispositions +
  the deferred queue: docs/planning/pre-d3-review-findings-2026-08-08.md.
  PROCESS NOTE, recorded honestly: the #109 merge was batched in the same
  command as the CI check read, so it landed while Client GameTests showed
  red — the red proved to be the CATALOGED Tier-3 silent-hang flake (mute log
  stop after session-config, empty crash-reports, 35-min kill; catalog entry
  updated with the recurrence), and BOTH the PR rerun and the merge commit's
  main run came back fully green. No harm done, but future merges must read
  checks in a separate command first. REMAINING: D3 re-ports prep only (26.1 +
  1.21.11; no tags pushed).

- **2026-08-08 (D2 release PREP executed — NO TAG PUSHED, per the governing
  directive)**: pre-flight green on main @ `29c3f267` (D1 merge): `CI=true
  :fabric:build -x runClientGameTest :paper:test :paper:shadowJar
  -Pmod_version=0.10.0` BUILD SUCCESSFUL, then `release_check.py --version
  0.10.0` → **OK** after the stale-jar guard correctly flagged and we removed
  four leftover v0.9.1 jars in build/libs (the guard's designed catch).
  Tag TEXT prepared at `docs/planning/release-tag-v0.10.0.txt` (cut from the
  D1 draft; headers intact — the eventual command is `git tag -a v0.10.0 -F
  docs/planning/release-tag-v0.10.0.txt --cleanup=verbatim`, to be run ONLY on
  the user's release decision, after the pre-D3 3-Fable review and D3's
  matrix-cells gate). Tier 3 on the release commit: PR #107's run was green;
  the main-push run on `29c3f267` was in_progress at record time — confirm
  green before any tag. Remaining D-work: the pre-D3 whole-program 3-Fable
  review (user directive; findings gate D3), then D3 re-ports prep (26.1 +
  1.21.11, no tags pushed).

- **2026-08-08 (task #8 — direct v20 transcode-path emit MEASURED, on
  `feat/v20-direct-emit` commit `20951f13`)**: all-transcoded disk columns now
  build the v20 `WireColumn` straight from the transcode descriptors
  (`NativeToV20Translator.convertIndexed` + `WireSectionCursor.emit`), skipping
  the native emit + whole-column translator re-parse; mixed/fallback/masked
  columns and the live path keep the translate route (recorded scope). Routing
  telemetry `DIRECT_V20_EMITS` + pins on both platforms (byte-identical outputs
  make a silent re-route invisible to every golden — only a counter catches
  it); the transcode-vs-object fuzz now proves direct-vs-translate byte
  identity across its corpus, all goldens untouched. **Gate matrix**
  (`profile-results/20260808-092305`, 4+4 × 150 s ABBA, R=256, base = main
  `e9f83232` translate route, 8/8 arms valid): serve lss µs/col **246.0 →
  231.3 (−6.0%)**, alloc/col 512 KB → 453 KB (−12%), disk-reader-thread LSS
  cut **+11.8% [+7.2, +16.2]**, `lss_attributed` +5.9% [+1.9, +9.8],
  `lss-other` (the translator's band) +24.9% [+16.8, +32.3]. Reading vs C6:
  the intermediate's REMOVABLE cost (~15 µs/col) is recovered; the residual
  vs the B5 baseline is the v20 capability's inherent price (dictionary +
  identity emit — R-4's framing stands for release notes). Tripwires:
  `nbt-other` n.s.; `serialize-live` ROSE (109→159 samples) — explained and
  benign: `in_memory` probe serves +7% (1320→1414 across reps) with
  `disk_read` identical (45,204), the faster pipeline shifting a handful of
  columns to the untouched live path; not a live-path code change (the diff
  never touches it).

- **2026-08-08 (task #8 review round — one grouped Opus lens, 9 findings, no
  correctness defect; all actionable ones fixed same day)**: byte identity,
  dictionary determinism, the allTranscoded gate and the mask pre-gate all held
  analytically and empirically (the reviewer measured with temporary probes).
  Fixed: (1) MAJOR — `exactPreSizingNeverFallsBackAcrossTheCorpusShapes` had
  gone VACUOUS on both platforms (its all-transcoded columns now route direct,
  so the sizing arithmetic it pins never ran — proven `directEmits=2`); both
  twins now serialize a MIXED column (transcoded + Global-fallback section)
  with a routing premise assert; (2) the one demonstrated byte-identity gap —
  an out-of-byte-range sectionY truncates through the native route's writeByte
  but THREW on the direct route — closed by truncating identically ((byte)
  cast, both platforms; production gates Y, but the range-free corpus/tool
  overload serializes garbage Y by contract); (3) `convertIndexed` now
  ENFORCES the native indexed-width thresholds (blocks 8 / biomes 3) instead
  of assuming the transcoder's fallback gate — a relaxed gate would have
  diverged the routes silently; (4) the new public API got its own suite pins
  (direct-vs-translate byte equality across single/duplicate-entry/boundary
  shapes + the width-guard throws); (5) the fuzz asserts a NONZERO direct
  fraction (its comment was true but unverified); (6) the routing pin's
  negative arms got servable-bytes premise asserts; (7) `direct_v20=` joined
  `sel_fallbacks=` on the Fabric read_path diag line (the live liveness
  receipt — the counter was write-only in production); (8) Paper counter
  placement aligned to its pair. Import-style twin drift recorded as accepted
  (Fabric imports WireSectionCursor, Paper fully qualifies — logic verified
  line-identical by the reviewer).

- **2026-08-08 (D0 grouped review round — 2 Opus lenses, 33 findings, all
  verified-real ones fixed same day)**: lens A (production semantics, 13
  findings) + lens B (§8/§9 compliance + fixture fidelity + blast radius, 20
  findings) converged on three shared MAJOR roots, each confirmed inline before
  fixing: (1) **the §2.2 pre-epoch warn-once was never implemented** — the
  design accepted the skewed-clock regression ONLY on visibility; now a latched
  WARN at the first pre-epoch clamp + `preEpochWarnedForTest` pin; (2) **a
  mid-tile EOF on a truncated v2 file left loaded tiles invisible to the
  liveSizes mirror** (batched end-of-dim bump; the next invalidation drove the
  exporter gauge negative for the session) — the mirror now bumps per-tile
  inside the loop, with a deterministic hand-written-v2 truncation test pinning
  survivors + mirror + never-negative; (3) **eviction was O(tiles²)** on the
  bulk paths the loader deliberately admits (config shrink 1024→256 ≈ 38k
  evictions ≈ 8×10⁸ map traversals on the processing thread) — one sorted pass
  now, snapshotting to plain values first (fastutil entries are live views that
  null on removal — caught by the fix's own red tests). Also fixed: the §2
  writer-side save belt (a leaked liveCount-0 tile would make the loader
  discard the REST of the file); v1-migration ts<=0 records now SKIPPED per §6
  (put()-streaming cleared live stamps on additive-merge loads); v2 load merges
  at SLOT granularity (a whole-tile put deleted in-memory stamps sharing a
  region; the merge pin was passing by accident — now same-region-strengthened);
  summed-liveCount eviction pinned (return value + cumulative counter + mirror
  — previously every eviction test used 1-entry tiles, so `evicted += 1` would
  have passed the whole tree); the re-put pin rewritten to STATE the §5
  tile-granularity weakening (a cold tile-mate survives while an older tile's
  younger column evicts); exact trim-to-byte-budget (was `>0`/`<6` — the
  store-cap vacuous-pin lesson); clearMiss-fires-on-ts<=0 pin (memo-enabled
  cache); the fuzz now proves sizesPerDimension parity (the mirror is a second,
  independently-maintained accounting path); MemoryLodStore quiesce gauge-write
  moved inside the HB edge + shutdown-break added; CLAUDE.md's wrong numbers
  (64 B/entry not ~136, ~13× not ~32×, ~1.6 MB disc not ~4.5, ~2.5× RAM not
  ~3×) and the LSSConstants 75%-fill claim corrected to §5's arithmetic; the
  two remaining pre-epoch fixture constants (OffThreadProcessorStoreTest,
  StoreFrameDeliveryTest) bumped for convention. Deliberately NOT per lens B:
  `maxPlausibleTiles` keeps its +1 slop against the exact-§6 reading, now with
  a comment — an exact bound discards a truncated dimension at the count rung
  before the tile loop can keep its complete tiles (the new truncation test
  proved it live). Recorded, no action: B16 (the router 113-pin is one-sided;
  adequate combined with the unit truth table), B20 (blast radius clean — no
  Paper twin, no checker change).

- **2026-08-08 (C6 slices 1–4 + the §9 coverage-gap round LANDED, on
  `feat/protocol-20-c6`; commits `0024eed2`, `3f19783b`, `a35a9d69`)**: before
  slice 2, a dedicated coverage audit mapped §9 against the actual suites and
  found FIVE items flat missing — headlined by `ClientIdentityResolver` (the
  entire §3 fallback ladder, the component the cross-version feature exists to
  provide) having ZERO tests, the cross-registry simulation (the cross-MC
  proof) not existing, and §9's bijection fuzz having been silently swapped
  for a 14-golden corpus walk at C2. All landed now: (slice 1) client-snapshot
  `session_version` + `--expect-session-version` in check_soak (5 selftest
  cases, 196 total) wired into EVERY soak.sh invocation — a dialect-lever soak
  that silently degrades to another rung now reds instead of passing
  format-blind laws; (slice 2) T3 C10 ingest-fallback pin (lying-lookup corpus
  body through the production receive glue → wall-to-wall stone at a fresh
  consumer; first version polled the main recorder past its 256-record cap —
  fixed); (gap round) `CrossRegistrySimulationTest` (permuted/missing/added
  registry pairs, identity-level equality, fallback-exactly-on-the-missing-set,
  duplicate-resolve collapse), `ClientIdentityResolverTest` (13 tests: full
  ladder, coercions, memo, warn-ceiling counter half), the seeded bijection
  fuzz (200 rounds, every packing mode incl. DIRECT synthesis, coverage
  counters; duplicate-palette rounds pinned content-identical per the collapse
  carve-out), client-config validation (fallback keys' healing + real-load
  malformation tolerance); the §9 migration golden (the walk with the REAL
  translator persists a row byte-equal to the committed v20 golden, CRC hashes
  recomputed); (slice 4) the store-migration soak variant LIVE-PASSED:
  `SoakStoreDowngrade` (dev-only) rewrites a warmed v20 store to the released
  v0.9.x shape at SERVER_STARTING, `store-migration-join` + named check +
  `store_migration_gate.sh` — downgrade ran, lazy 3→4 upgrade + warm 19-row
  serves (≥800 through the inverse translator) + walk completed with zero
  anomalies, 34 windows 0 violations. **C4's accepted no-live-19-row gap is
  CLOSED.** C1-14 (out-of-range-sectionY parse-and-discard) stays DEFERRED to
  the D3 cross-line fixture suites — re-recorded here per the audit's note.
  REMAINING C6: cross-line fixture-corpus capture tool + 26.2 decode test
  (slice 3), full soak passes (19 Fabric + 5 Paper + 4 Folia + bandwidth-
  throttle margin re-check), benchmark arms (native-v20 + legacy-dialect +
  C1-13 save cost), matrix cells 3–4, grouped review round, merge
  `feat/protocol-20` → main + build.yml filter drop + its contract-test pin.

- **2026-08-08 (C6 implementation plan — hardening, XVER Phase 6 / §9
  leftovers + the integration-branch merge)**: slices, in execution order.
  **(1) Negotiated-protocol observability** (C3's deferred m8/m11 — without it
  a `SOAK_DIALECT=19` soak that silently degraded to v16 still PASSES): the
  client snapshot gains a `session_version` field (the gate's established
  version; 0 pre-config), `check_soak.py` learns an optional per-scenario
  `expect_session_version` (asserted on every post-config client row when set)
  + selftest cases, and the dialect-19 soak invocations pin 19. **(2) T3
  ingest-fallback assertion** (§9 Tier 3): a synthetic unknown identity in a
  served column decodes to the fallback state at the registered consumer.
  **(3) Cross-line fixture corpus** (§9 goldens, review MAJOR): capture a v20
  frame corpus from REAL 26.2 terrain (the settling tool's corpus source —
  `test-server/paper` world), check it in under `xver-live-corpus/` with a
  26.2-side decode test (identity-level content + fallback count 0 + §2.1
  count-shorts recompute); the 26.1/1.21.11 decode suites are D3 phase-7 work
  against these same fixtures. **(4) Store-migration soak variant** (§9): a
  `store-second-join` variant staging a schema-3/v19-format store fixture
  (built by the migration test's builder logic exported as a tool) — warm hits
  from minute zero through the inverse translator, migration completion, laws
  clean during the walk; closes C4's accepted no-live-19-row gap. **(5) Full
  soak passes**: all 19 Fabric + 5-scenario Paper + 4-scenario Folia, box-
  exclusive, serialized; `bandwidth-throttle` margins re-checked against the
  ~+5% raw growth. **(6) Benchmark arms** (R-4, PERF Phase 0 ref-vs-ref
  tooling): native-v20 arm vs the B5 tree (the shipped serve cost — licenses
  any end-to-end v0.9.x→v0.10.0 claim) + the legacy-dialect arm (XVER §12
  translation cost) + the C1-13 save-cost check (DirtyContentFilter now pays
  the v20 translate per chunk save); markers optionally extended with
  IdentityCodec/translator classes (re-check classifier tripwires for that run
  only). **(7) Live-matrix cells 3–4** (R-8): cell 3 = the mismatch-guard live
  pull (LSS_VIA=1 rig + the v0.8.0-era 1.21.11 lss-multi-test client jars →
  denial INFO; LSS_VIA_GUARD=0 → garbage behavior returns) — needs a real
  client join, flagged for the user if headless drive proves impractical;
  cell 4 = optional same-MC legacy-rung eyeball (crafted-frame gametests carry
  it). **(8) Grouped-Opus review round over the C6 diff, then merge
  `feat/protocol-20` → main** (PR, `--merge`), drop the `feat/protocol-20`
  entry from build.yml's branch filter AND update its
  `ReleaseWorkflowContractTest` pin in the same commit. Deviations from §9 not
  re-decided here: dialect-16 lever stays EXCLUDED (review CRITICAL — A1 has
  no terms for the shim's sheds), v16 live soak stays its own future sub-task.

- **2026-08-08 (C5 IMPLEMENTED + review round + fix round, on
  `feat/protocol-20-c5`)**: shipped per the plan entry below. The grouped-Opus
  round verified the correctness core EMPIRICALLY — the real ViaVersion API
  fetched from upstream (`getAPI()` raw-typed static, `getPlayerVersion(UUID)` a
  non-deprecated default returning int, -1 for unknown players; ViaFabric
  Jar-in-Jars without relocation so the single class name serves both
  platforms), `SharedConstants.getProtocolVersion()` javap'd on the loom jar,
  all four Modrinth facets queried live, the snapshot-bit encodings matched —
  and found NO path where the deny leaks a reply or registration. Verdict: 0
  CRITICALs, 4 MAJORs (all about the guard's own observability), 15 minors.
  Fixed: **MAJOR-1** the rig's kill-switch A/B was impossible (the staging
  rewrites the config every run) → `LSS_VIA_GUARD=0` knob writes
  `enableViaMismatchGuard=false` into the staged config; **MAJOR-2** Fabric's
  production denial path had zero executable coverage → seam overload
  (mirroring Paper's) + a `ServiceLifecycleGameTests` case driving a forced
  mismatch through the PRODUCTION ladder (silent + unregistered) with its
  fail-open twin; **MAJOR-3** both wiring pins were unanchored
  `String.contains` pairs a broken composition satisfies → whitespace-
  normalized regex anchoring the config-flag→ternary→probe RELATIONSHIP, the
  Fabric native-protocol pin (LSSConstants there would deny every legacy
  client), and a denial-precedes-reply order pin; **MAJOR-4** the production
  Holder was never exercised → a stub-driven `ViaProbe.playerProtocol` case (a
  typo'd FQN is a permanently dead guard). Minors taken: m1/m2 (registration-
  survives documented at both call sites, false test comment fixed), m3 (Via's
  not-initialized throw is IllegalArgumentException, javadoc corrected), m4
  (non-initializing forName + catch-all belt in `playerProtocol` — an escaped
  Holder-init Throwable would NoClassDefFoundError the network handler
  forever), m5 ("Minecraft protocol" wording — the line above prints an LSS
  protocol number), m6 (the no-signal overload's native slot is 0, never an
  LSS constant), m7 (`softdepend: [ViaVersion]` + PluginYmlContractTest pin),
  m8 (.part+mv atomic downloads), m9 (non-fatal loud-warn Via staging), m10
  (usage line, CLAUDE.md knob block), m13 (stub made shape-faithful: generic
  `ViaAPI<T>` + default UUID overload, pinning the erasure-collision and
  default-method-dispatch properties the reviewer verified by hand), m14
  (re-attach-prompt window comment), m15 (dead array data). Recorded-not-done:
  m12 (probe consulted on v20 handshakes too — KEPT with rationale in the call
  site comment: future diagnostics, one cached invoke per join), the m5
  per-player INFO dedupe (a v19 client walking 19→16 logs two denials per
  join — accepted, it is the guard's only observable), `run-paper-via`
  convenience case (LSS_VIA=1 run-paper covers it), Folia Via staging (nothing
  ever stages Via into Folia's plugins dir, so there is nothing to park).
  **D1/D2 OBLIGATION (m11, carried here so it cannot be lost): the v0.10.0
  release notes MUST document the Velocity/Bungee proxy hole — Via running on
  a proxy is invisible to the backend, so legacy LSS clients behind such a
  proxy stay undetected and get the pre-guard garbage behavior until they
  update (XVER §7 "Release notes say so").** Review judged both documented
  decisions sound: silence as the denial shape (walks the C3 ladder to
  terminal disarm; a disabled-advertisement would need a reply shaped for a
  dialect just judged unservable — worse invariant), and the proxy hole as
  release-notes-only. Gates: T1 both + T2 + checker selftest 191 + dialect-19
  fresh-backfill PASS (33 windows, 0 violations — the v19 rung through the
  real gate with the guard composition present, probe no-signal provably
  unchanged); the live Via pull is the rig variant + D3 matrix cell 3 per R-8.

- **2026-08-08 (C5 implementation plan — Via mismatch guard, XVER §7 / Phase 5 +
  R-8's rig variant)**: (1) `common/compat/ViaProbe` — reflective bridge to
  `com.viaversion.viaversion.api.Via.getAPI().getPlayerVersion(UUID)` (ONE class
  serves both platforms: ViaFabric bundles viaversion-common, ViaVersion is the
  Paper plugin; Via classes are not MC classes so no remap concern), the
  MoonriseReadCompat shape — handles resolved once per JVM off `getAPI`'s DECLARED
  return type (no instance needed at resolve time), instance-scoped `build(lookup,
  warn)` for order-independent tests, class-absent = silent no-signal,
  present-but-unresolvable = warn-once, every invoke failure (incl. Via's
  not-yet-initialized IllegalStateException) = no-signal per call. Guard rule
  FAIL-OPEN: mismatch only when the probe returns `> 0` AND `!= nativeProtocol`
  (Via answers -1/native for non-Via'd players; no-signal must never deny).
  (2) `HandshakeGate` gains an 8th input `viaMismatch` (plain boolean — call
  sites compose probe × `enableViaMismatchGuard` × the platform's
  `SharedConstants.getProtocolVersion()` and log the INFO line with the real
  numbers) and a new outcome `VIA_MISMATCH`: applied AFTER the version rung
  selects a LEGACY dialect (V19/V18/V16 — a v20 client carries mcDataVersion and
  is always servable, so CURRENT ignores the flag), BEFORE any register/reply
  outcome; NO reply (silence is each legacy client's native "no LSS here"
  signal — the v19/v18 ladders fall through to their next rung and terminal
  disarm, v16 stays dark). The 7-arg overload delegates `false` so every
  existing gate pin stays byte-identical. (3) `enableViaMismatchGuard` default
  true in `ServerConfigBase` (§2.2's operator override — the guard denies
  registration off a third-party API) + compat-defaults pins both platforms +
  the `check_soak.py` allowlist entry (PIN-1 obligation). (4) R-8 rig variant:
  `LSS_VIA=1` stages ViaFabric+ViaBackwards (Fabric) / ViaVersion+ViaBackwards
  (Paper) via Modrinth API downloads; `run-fabric-via` convenience target — the
  guard is dead code on a rig without Via, and the D3 matrix needs the live
  pull. (5) Tests: `ViaProbeTest` against real-package-name stubs under
  `com/viaversion/` (resolve ladder, throwing getAPI, -1 no-signal, warn-once),
  gate matrix additions (mismatch × each legacy dialect → silent no-register;
  mismatch × CURRENT → unaffected; false → 7-arg-identical), config pins, and a
  source-regex wiring pin that both platform call sites compose the probe behind
  the config gate. Gates: T1 both + T2 + one dialect-19 fresh-backfill (the v19
  rung through the real gate with the guard composition present, probe absent =
  provably unchanged). Live Via pull deferred to the rig variant (manual) + D3
  matrix cell 3 per R-8.

- **2026-08-08 (C4 IMPLEMENTED + review round + fix round; commits `b3dce7cc`,
  `8212e3d7`, `abb71814` + the fix commit on `feat/protocol-20-c4`)**: the lazy
  3→4 migration shipped per the plan entry below, with two recorded deviations —
  (a) §5.3's legacy-session double-translation skip is SUPERSEDED: `raw() == v20`
  is a C2 pipeline invariant (the legacy egress translates FROM v20 at enqueue,
  and a dedup group can hold mixed-dialect recipients), so a 19-row ALWAYS
  translates to v20 at the rung; (b) the walk is idle/floor-gated on the batcher
  instead of MSPT-gated (the batcher thread never holds the server tick). The
  grouped-Opus round empirically verified the highest-stakes claims (one-txn
  upgrade atomicity against pinned sqlite-jdbc 3.49.1.0; from-state matrix
  completeness incl. the `store_layout` guard killing the C1..C3-era 4∧20 latch-
  dead hole; WAL snapshot safety; watermark monotonicity) and returned **1
  CRITICAL + 8 MAJORs + 12 minors**. CRITICAL-1 (fixed): the walk decompressed
  from the row's own `usize` with NO bound, caught only `Exception`, and its
  anomaly DELETE could itself fail — one bit-rotted row = multi-GB allocation
  attempt → Error escapes → same-batch retry every 200 ms → WRITE_FAILURE_LATCH →
  a RESTART-SURVIVING dead store. Fix: the R1 bound `[1, MAX_ROW_USIZE]` on the
  walk too, per-row `catch (Throwable)`, and a guarded DELETE whose own failure
  leaves the row 19-tagged BEHIND the watermark (served translated forever,
  bounded) — forward progress is absolute; poison-usize row pinned. MAJORs
  (all fixed): #2 successful walk batches now reset `writerFailures` (the latch
  was a lifetime budget of 20 for the walk — it ran only on idle iterations,
  which never reset the streak); #3 a busy FLOOR (one batch per 32 applied ops)
  replaced strict idle-gating, which starved the walk to ZERO under any steady
  deposit traffic (a 500 cps backfill never leaves a 200 ms idle window) —
  §5.5's ordering intent holds as "walk keeps deposit pace", recorded with
  deviation (b); #4 anomaly counters surfaced (` migrate_anomalies=` in the
  status token + counts in the completion INFO — row deletion is irreversible
  and was write-only); #5 the serve rung's per-19-hit `zstdOrNull()` (a full
  compress/decompress self-test PER SERVE) became a once-resolved reader field.
  Tests added (#6–#9): the raw-rung translation twins (translated body + unwired
  errored-miss, with m17 hit-count pins), the ON-CONFLICT `wirefmt` flip
  (deposit-over-19 retags in one upsert; ts-loser leaves the row 19 — a
  half-applied retag would CRC-validate FNV hashes), the faulted-batch
  watermark-txn coupling (injected fault between UPDATEs and commit: rows AND
  watermark retract together, every row translates exactly once, no
  double-count), and `StoreLegacyRowCompositionTest` — the one place the REAL
  pieces meet (genuine SQLite 19-row forged to the lazy-upgrade shape → getFrame
  → rung → REAL `toV20` → byte-equals the committed v20 corpus golden →
  `fromV20` round-trips to the native golden; no soak can produce a live
  19-row). Minors taken: 10/11 (placement/javadoc), 12 (`WIREFMT_*` promoted to
  `LodStoreService`), 14 (latched store shows NO migration token — must look
  dead), 15 (`commitTxn` in finishMigration), 16 (DropAll clears walk meta AFTER
  the drop loop — a shutdown mid-drop now re-arms a smaller walk instead of
  stranding 19-rows walkless), 17 (failed translation books errored-miss only,
  never also a hit), 18 (fixture carries v0.9.x page_size/auto_vacuum), 19
  (indent + twin comment). Recorded-not-done: m13 (`migrateRemaining` can
  under-report after anomaly deletes — cosmetic, self-corrects at completion),
  m20 (byte-bounded walk batches — 64 rows ≈ 400 KB worst case, acceptable),
  m21 (D2 release-notes line: ~+10% store growth at migration vs an armed
  `lodStoreMaxMB` cap). Reviewer-sound-as-shipped: initMigrationState
  publication (Thread.start HB edge), query-plan bounding, sweep/eviction
  wirefmt-blindness, both deviations' implementations.

- **2026-08-08 (USER DIRECTIVE — pre-D3 whole-program review round)**: immediately
  BEFORE the D3 re-ports to the older MC lines, run one additional review round
  with **three Fable subagents** over ALL changes made as part of this plan (the
  full v0.10.0 program diff: A1/A2, B0–B5, C0–C6 as merged). This is in addition
  to the per-stage grouped-Opus rounds already run; findings gate D3.

- **2026-08-08 (C4 implementation plan — store schema 3→4, XVER §5 / Phase 4;
  ENDS the R-1 deploy hold when merged)**: implement §5 as specified, no
  deviations planned: (1) `SCHEMA_VERSION=4` + the upgrade block between
  `openWriter()` and `metaMatches` — fires ONLY from the exact state
  `schema_version=3 ∧ wire_format_version=19` ∧ fingerprint match ∧ codec
  match (any other from-state drops as today); `pragma table_info` probe before
  the ALTER (`ADD COLUMN wirefmt INTEGER NOT NULL DEFAULT 19`); meta stamped
  `4 ∧ 20` INSIDE the upgrade txn immediately (the §5.1 review MAJOR —
  completion is tracked separately, never via the version keys); any upgrade
  failure falls through to drop-and-rebuild. (2) Deposits write `wirefmt=20`.
  (3) `getFrame` hit record carries `wirefmt`; a 19-row hit decompresses →
  id→identity inverse translation (a NativeToV20 translation with the server's
  emit tables — the C1 `toV20` path reused on the reader pool) → re-encode,
  recomputed usize feeding the raw-denominated charge; a LEGACY-session serve
  of a 19-row skips the double translation. (4) The background migration walk
  rides the backfill restraint pattern: per-dim rowid watermark in meta
  (`migrate_progress_<dim>`), batches through the single-writer batcher with
  the watermark riding the SAME txn as its batch, per-row parse anomalies
  delete the row, DropAll resets watermarks, WRITE_FAILURE_LATCH shared,
  start/end INFO + `migrating=<n>/<total>` in store status; migration walk
  runs BEFORE the backfill in the shared pacing budget. (5) Interplay per §5.5
  (eviction/sweeps wirefmt-blind — verified in the spec against the current
  selects). (6) Fingerprint drop policy unchanged (§5.6). Tests:
  SqliteLodStoreTest + Paper-twin additions (exact-from-state upgrade, the
  half-applied-ALTER catch path, dev-era metas drop, 19-row translated serve
  byte-equality vs a native golden, legacy-session skip, walk watermark
  txn-coupling, DropAll reset, anomaly row-delete, eviction age-order across
  mixed wirefmt); gate = store soaks (`store-second-join`, `store_gate.sh
  warm`) + a dialect-19 store soak leg + the §0 A/B if timing allows.

- **2026-08-08 (C3 IMPLEMENTED + review round; commits `7587faf6`, `787d18e3` on
  `feat/protocol-20-c3`)**: the ladder shipped per the plan entry below (rung
  machinery, V16ClientWire generalization, enableV19ServerCompat, the lever as
  initial rung; ladder Tier 1 suite + T2 + T3 green; dialect-19 fresh-backfill
  PASS through the real ladder). The grouped-Opus round then found — and
  empirically verified — a **CRITICAL** the suite could not see: the ladder's
  own 16 rung clobbered the single last-announce memory, so a slow v19 echo fell
  to the SessionConfig FOREIGN arm as (19, enabled=false, lod=0) — accepted,
  latched, LOD silently off, with NO heal on a server running
  `enableV16Compat=false`. Fix: sticky per-connection announce SET in
  V16ClientWire (bits per legacy rung; retired with their flags by a
  higher-version re-assert — the guard's heal, preserving the R2-3
  prompt-hazard defense per rung — or by the new retractAnnounce on a thrown
  send). **MAJOR-2**: the drain read the live dialect flag on a third thread
  out of frame order — fixed with a per-payload native-body STAMP captured at
  netty decode (`VoxelColumnS2CPayload.read`), the only frame-ordered
  authority; the server-side residual (stale queue across a flip → bounded
  ingest-failure re-serve) stays the accepted C2 window. **MAJOR-3**: the
  promised codec-level pins landed (`V16ClientWireDecodeTest`, the CRITICAL
  case red-first) and the two gate race pins were reordered to the real
  netty→main sequence (they had pinned an inverted ordering). Minors: rung
  state commits after send success, per-connection primaryAnnounce, config
  comment, truth-table 19-row. Recorded-not-done: negotiated-protocol
  observability on the client snapshot + a dialect assertion in check_soak
  (m8/m11 — C6 hardening candidates; today a lever soak that silently degraded
  to v16 would still PASS), the hostile-server re-announce ping-pong (m10,
  pre-existing shape). Post-fix gates: T1+T2+T3 green, dialect-19
  fresh-backfill PASS.

- **2026-08-08 (C3 implementation plan — client-side ladder 20→19→16, XVER §6 /
  Phase 3)**: (1) `V16ClientWire` generalizes IN PLACE (name kept — renaming
  breaks source pins and a dozen doc references): `announced16` →
  `announcedVersion` int, `observeSessionConfigVersion` arms BOTH
  `columnSourceless` (announced 16 × observed 16, unchanged semantics) and the
  new session-19 flag (announced 19 × observed 19); new accessors
  `lastAnnouncedVersion()` (the SessionConfig codec's 19-arm gate — same
  mark-before-send wire-causality argument as announced16) and
  `isNativeBodySession()` (sourceless OR session-19 — the drain's
  translation-skip gate, replacing the lever's static read). (2) The
  `SoakDialectOverride` clauses leave the CODEC and the DRAIN — the lever
  becomes purely the ladder's INITIAL rung (19 instead of 20), riding the real
  per-connection state; suppressing client_info keeps the property read.
  (3) `ClientSessionGate` rung ladder: initial announce → 100-tick silence →
  19 (gated `enableV19ServerCompat`, NEW client config default true) →
  100-tick silence → 16 (gated `enableV16ServerCompat`, unchanged) → disarm;
  any SessionConfig disarms; send-failure/disconnect/reset semantics preserved
  per rung. (4) Acceptance generalizes to {the initial version} ∪ {19 if this
  connection announced it} ∪ {16 via the existing v16 branch}; the SLOW-ECHO
  RACE is the load-bearing case — a late 20-echo after the ladder advanced must
  still be ACCEPTED (rejecting would disable LOD against a healthy v20 server
  on a slow join), and the server-side dialect flip our 19-announce caused
  heals via the guard's re-announce. (5) The downgrade guard generalizes per
  §6: a KNOWN lower-rung config (16 OR 19) on an established higher session
  re-announces the ESTABLISHED version (today's v16-on-current logic, per
  rung); a 20-echo on an established 19 session is the UPGRADE path and rides
  the normal re-sent-config manager replace. (6) No v18 rung (§6 decision,
  recorded — v0.7.x–v0.8.x servers all carry the server-side v16 rung).
  (7) Tests: the ladder Tier 1 suite in ClientSessionGateTest (rung
  sequence/timing, per-rung config gates, disarm-on-any-config, BOTH
  downgrade-guard flavors + the slow-echo upgrade, reset family), V16ClientWire
  arming pins for the 19 flag (announced-gated, unsolicited-19 inert — the
  poisoning story's 19 twin), the codec 19-arm pin via marked state, config
  default pin; gate = the suite + one dialect-19 fresh-backfill (the lever now
  exercises the real ladder path end to end).

- **2026-08-08 (C2 review round, single grouped Opus reviewer — 3 MAJORs + 9
  minors, all addressed or recorded; commit `101410a6`)**: **MAJOR-1** — the
  enqueue's dialect read (the single decision selecting the translate branch) was
  pinned by NOTHING and an unattached tracker fails toward the C1-1 CRITICAL
  (v20 bodies to legacy clients) with every tier green. Fixed: non-null
  empty-tracker default, attach-call source pins on both platforms, and the
  discriminating Paper Tier-1 pin (the REAL enqueue end to end — service-attached
  tracker → dialect read → translate → the flushed frame byte-equals a
  natively-built frame). **MAJOR-2** — the legacy branch bypassed the ColumnBytes
  holder, costing one translate+compress PER RECIPIENT on what becomes the
  default path at release; fixed with a two-slot memo on the shared holder
  (LegacyColumnBuild moved to common), compute-once pinned. **MAJOR-3** — the
  enqueue admission guard checked the V20 size but native can be LARGER
  (wide palettes repack from ≤12-bit dictionary indices to ~15-16-bit native
  DIRECT), and an over-cap body kills the legacy connection at readByteArray;
  fixed: buildLegacyColumn refuses over-cap translations (→ the up_to_date
  containment), pinned by a synthetic 220-section wide-palette column on both
  platforms. Minors taken: min-bytes term on the legacy compress, clear-path
  containment comment, probe-hash denomination comment, corpus-dir lockstep +
  collapse-width guards, serializer-suite independence notes, lever warn text,
  null-tolerant dialectOf. Recorded-not-done: m11 (defensive queue purge on a
  mid-session dialect flip — no reachable path) and m4 (T2 succeedWhen teardown
  leak — matches the file's existing convention). **Post-fix soak validation
  became an A7 investigation**: two consecutive dialect-19 fresh-backfill reds
  (+10 timeout-triaged disk.errors, the EXACT catalog signature — 0 non-timeout
  errors, A5 exact, runs still converged) settled per the catalog's same-day
  branch-A/B rule, extended to a 4-arm matrix: C1-base no-lever PASS, C2
  no-lever PASS (the native path — the whole production population — clean),
  then the SAME lever arm PASS with 0 errors minutes later on byte-identical
  code. Verdict: the red pair was one transient WSL2 IO window; the lever's
  extra per-column processing-thread work (translate + recompress) makes
  dialect-19 soaks a mild intensifier of the documented A7 mechanism on a
  constrained box — added to the CLAUDE.md flake catalog.

- **2026-08-08 (C2 IMPLEMENTED — legacy egress translators + soak lever; PLAN
  DEVIATION: translation moved from the §4.2 flush seam to the per-recipient
  ENQUEUE choke point — plan-text pair: the AMENDED block in
  cross-version-identity-encoding-plan.md §4.2)**: commits `4766f78f` (flush-seam
  first cut per the plan), `a9cfedea` (lever decode fix), `862f734c` (the enqueue
  move), `591a7111` (docs) on `feat/protocol-20-c2`. The two live-run discoveries,
  both caught by the dialect-19 soak gate and neither by the green gauntlet:
  **(1)** the emulated v19 client came up DEAD — the v20 codec's SessionConfig
  decode routed the version-19 echo to the FOREIGN arm (drain + enabled=false),
  which a real v0.9.x client never hits; the lever now makes the CURRENT arm
  accept 19 (`SoakDialectOverride` moved to the payloads package beside
  V16ClientWire), and the T2 dialect gametests gained enabled/lodDistance echo
  pins (the encode-side flavor of the same hole). **(2)** law **A2 redded by
  exactly the v20-vs-native rawSize delta** (server booked 16,554,236 raw bytes,
  client received 16,292,136): flush-time translation ran AFTER the QueuedPayload
  sizes were booked, so bandwidth budget, queue gauges, diag books, and A2 all
  counted v20 bodies while the client decoded native ones. Fix = the deviation:
  translation at `buildAndEnqueueColumnPayload` (both platforms) via
  `fromV20` (the exact-inverse twin of `toV20`, riding the same scopedFor memo
  tables) + `buildLegacyColumn` (v19 recompress shrink-gated off the attached
  `wireCodec()`, v18/v16 forced-RAW, rawSize re-derived) — every queued size now
  derives from the bytes the legacy client decodes, translation CPU lands on the
  processing thread, the flush seams keep ONLY the header splices (v19 ships
  verbatim), and translation failure takes the established oversized-column
  `up_to_date` semantics (a retry loop on a persistent table bug would re-read
  disk forever). Store hits need no separate wiring (every producer funnels
  through the same enqueue). **Gates**: Tier 1 both platforms green (fabric
  ~1256, paper ~340), Tier 2 green incl. the two dialect DELIVERY legs
  (sectionsSent + surviving done-bit through the real egress), and the plan's
  soak gate under the new `SOAK_DIALECT=19` lever — **fresh-backfill PASS** (32
  windows, 0 violations; `cols_zstd=2619, cols_raw=0` — the entire backfill rode
  the v19 translate+recompress path; A2 balanced at 16,279,704 bytes both sides)
  and **warm-rejoin PASS** (52 windows, 0 violations — resync stamps against
  translated bodies). Tests: the §9 corpus translation chains on BOTH platforms
  drive `fromV20` through the PRODUCTION registry tables (one documented
  palette-collapse carve-out — `duplicate-air`'s duplicate palette entries
  de-dupe through the identity dictionary, pinned content-identical);
  build-level codec pins (recompress round-trip, the {0,0}→{0} ghost-clear
  shrink-gate, missing-codec degrade); Paper header-shape ladder pins;
  `CorpusRegistryAccess` extracted on both modules + the C1-15 inverse-builder
  dedupe (5 copies → the production statics). Pending before the C2 PR: the
  grouped-Opus review round.

- **2026-08-07 (C2 implementation plan — legacy egress translators, XVER Phase 2)**:
  (1) SERVER exact resolvers: `IdentityTables.blockIdsByIdentity()` (exists) + a
  biome identity→id inverse added to the NbtSectionSerializer scopedFor memo (invert
  `buildBiomeIdentities`; expose `biomeIdLookup(RegistryAccess)` + the idMap size) —
  unknown identity → -1 → translator throws → egress warn-drop + clearDiskReadDone
  (the C1 latch becomes the FAILURE path, not the normal path). (2) Fabric egress
  (`sendColumnPayload`): v19 = translate the body (decompress→translate→recompress
  when codec ZSTD — v19 supports the codec byte; RAW = translate only), rebuild the
  payload, send at CURRENT shape (v19 header == current); v18/v16 = translate the
  (forced-RAW) body, rebuild, then the EXISTING asV18()/asV16() splices compose on
  top — §4.2's "v18/v16 are v19 minus bytes". (3) Paper: same ladder over byte[]
  frames via a new PaperPayloadHandler decode-column-frame + re-encode helper,
  composing with rewriteColumnToV18/V16. (4) Tests: the translation-chain goldens =
  for every frozen nbt-corpus native golden, egressTranslate(toV20(x)) must
  BYTE-EQUAL x (the §9 bijection on one registry, corpus-driven), plus zstd
  round-trip, splice composition against the existing v18/v16 goldens, failure
  containment, both platforms. (5) The `-Psoak.dialect=19` lever: a dev-only
  legacy-client EMULATION mode on the soak client (`lss.soak.dialect` property):
  announce 19, accept the 19 echo in ClientSessionGate's version check, and skip
  the client-side v20 translation (bodies arrive native) — then fresh-backfill +
  warm-rejoin under the lever are the C2 soak gate.

- **2026-08-07 (C1 review round, single grouped Opus reviewer — all three lenses;
  2 CRITICALs + 2 MAJORs + 12 minors, all addressed or recorded)**: **C1-1 CRITICAL**
  — the recorded all-three-dialects egress-drop decision was only half-implemented:
  v18/v16 branches still header-spliced v20 BODIES (a legacy client would decode
  dictionary strings as section data — the exact issue-#85 failure mode). Fixed:
  one shared drop latch for ALL legacy dialects on both platforms, done-bit cleared
  so the state never claims delivery, honest bounded-loop comment (the old
  "healthy-idle" claim was wrong — review C1-4), v18 T2 pin annotated with the
  C2-restore marker. **C1-2 CRITICAL** — the client translated v16-session NATIVE
  bodies (breaking the shipped enableV16ServerCompat: every column of every legacy
  server would fail translation into a permanent re-serve loop). Fixed: the
  per-application `V16ClientWire.isColumnSourceless()` gate at the drain seam —
  also the eventual §6 shape. **C1-3 MAJOR**: the identity memo + fallback warns
  were unbounded by wire content — memo capped at 65,536 (resolution still works
  past it), a 256-warn global ceiling atop the per-identity latch, logging moved
  outside the CHM lock. **C1-4 MAJOR**: folded into C1-1's done-bit clearing.
  Minors: fallbacks surfaced as a session-teardown summary line (C1-5), the
  production-drain source pin (C1-6 — the identity-overload downgrade hazard),
  production biome table now validates + fails loudly at build (C1-7), Paper
  v19-echo byte pin (C1-8), sidecar maps cleared at SERVER_STOPPING/onDisable
  (C1-9), stale clear-size comment (C1-10), legacy session-start log restored in
  the tracker (C1-11), the v20 `xray-masked` sibling golden on both modules with
  regen-ordering fix (C1-12), native-palette-at-DIRECT-width emit guard (C1-16).
  Recorded, not fixed: **C1-13** — the translate-at-producer cost is also per
  chunk SAVE (DirtyContentFilter hashes through serializeColumn), added to the C6
  benchmark-arm scope; **C1-14** — §2.3's out-of-range-sectionY parse-and-discard
  (vs today's count clamp) is DEFERRED to C3/C7 (same-version heights are safe at
  C1; the cross-line fixture suites hit it first); **C1-15** — four copies of the
  test inverse-biome builder (cleanup note, C2). The reviewer's cleared list
  verified: gate + both 7-arg call sites, tracker lifecycle equivalence incl. the
  §4.3 single-lifetime resolution, ALL byte-producing paths hooked, the clear
  invariant end-to-end, DIRECT width matching vanilla's read exactly, the
  version-gated data-version append, resolver threading, tracer same-commit rule,
  R-5 layering, and store rebuild-on-wire-drift riding Environment.wireVersion.

- **2026-08-07 (B5 round-acceptance deviation — the serve-arm µs/col gate, plan-text
  pair: the VERDICT block in the plan's Round acceptance section)**: the closing
  A/B's serve-arm lss-attributed µs/col came in at **−4.3% (a rise), n.s. vs zero
  [−9.1, +0.4]**, missing the ≥5%-cut gate as written. ACCEPTED as a documented
  deviation, no phase reverted or flag-disabled, for four reasons. (1) The gate's
  premise — "R1 moves work rather than cutting it", i.e. µs/col-neutral — was
  falsified by B3's own accepted measurement BEFORE the closing ran: R1 alone
  measured +10.3% [+5.5, +15.4] serve-arm rise (b3-serve pair; part real pool-side
  overhead, part attribution — post-split the moved parse is fully LSS-attributed,
  and the band split shifts, see the cross-split analysis note), with B3 accepted
  on its own per-thread/ceiling gates (ceiling IMPROVED +3.8%). (2) R2's clean
  same-tree kill-switch claw-back is −10.5% [−14.4, −6.5] (b4 pair); composing the
  two measured factors predicts ≈−1.3% net vs the −4.3% measured — consistent
  within the calibrated cross-session noise floor. (3) At the CPU-bound hibw
  ceiling — the arm where µs/col actually binds — the same metric is a **+5.9% cut
  [+1.4, +10.1]**, passing the gate's number there, and the ceiling itself improved
  +10.7%. (4) Reverting/flag-disabling the "offending" phase (R1) to win a
  bandwidth-bound serve-arm metric would resurrect the A7 IOWorker-starvation
  mechanism the round was largely FOR; `useBackgroundReadSplit=false` remains the
  shipped per-deployment rollback for any box that prefers the old profile. All
  other round gates passed with margin (backfill three-thread +21.3% vs ≥15%,
  four-class alloc +49.2%/+52.4% vs ≥30%, ceiling +10.7% vs ±5%, store gate
  outright). Analysis rule recorded for future sessions: `nbt`/`serialize` BAND
  splits are NOT comparable across the R1 boundary (pre-split base serialize 859
  vs split-tree same-workload 1300 — post-split parse nests under the transcoder's
  frames and band classification is exclusive); `lss_attributed`, per-thread sums,
  and any-frame markers are the cross-split quantities.

## Measurement ledger

- **2026-08-07 — B3 (R1 background-read split) gate session: ALL GATES PASS. Box
  exclusive** (`pgrep` clean; tree frozen through all matrices). Protocol: A7
  branch-A/B triage + serve/hibw/backfill ref-vs-ref, 4+4 reps ABBA each,
  base=`96cf1f9` (B2-merge main, worktree) change=`174ab32` (B3 + review fixes).
  **A7 triage (gate 6, directional)**: the earlier fresh-backfill red (+5 =
  diskReaderThreads, zero non-timeout errors, A5 quiet — the documented signature)
  did NOT reproduce: base AND change both passed same-day, 0 violations — box-state
  variance, branch exonerated per the flakes-catalog protocol. **Gate 1 (serve,
  ≥75% cut on LSS-frame IO-Worker samples): PASS — 1494 → 9, cut +99.4%
  [+98.8, +99.7]**; vanilla residual flat (337→336) exactly as the plan said to
  check; column parity 0.02%. **Gate 2 (conservation)**: moved work reappears on
  LSS Disk Reader (1161→2925) + processing thread; serve tripwires quiet
  (nbt-other +2.6% n.s.; serialize-live within its dispersion). **Gate 3 (hibw
  ceiling, ±5%)**: PASS AND IMPROVED — pooled 1689.9 → 1754.3 col/s (+3.8%) with
  IO-Worker cut +99.1% [+98.5, +99.5]. The hibw serialize-live tripwire FIRED
  (23→152, rep-consistent [4,4,10,5]→[30,26,57,39]) — investigated, NOT classifier
  drift: at the CPU-bound ceiling the freed IOWorker lets vanilla's save pipeline
  (and the save-hook's DirtyContentFilter hashes = serialize-live's content) drain
  denser inside the measured window, the exact residual-rise direction the plan
  predicted for the vanilla share; the gates read markers/per-thread splits, which
  are unaffected. **Gate 5 (backfill, ≥50%)**: PASS — IO-Worker LSS 1658 → 16,
  cut +99.0% [+98.4, +99.4], and walk time IDENTICAL (216 s, 844.2 deposits/s both
  arms — the pace-bound "walk flat" requirement holds exactly). lss_attributed:
  serve −10.3% (within the ~12% A/A floor; the moved parse is fully visible under
  LSS frames), backfill +0.7% flat. Caveat recorded: the plan's "±5% validated
  against the A/A hibw spread first" leg has no hibw A/A on file (the B0 A/A ran
  the standard arm only) — the +3.8% improvement direction with a +99% mechanism
  receipt makes the gate reading safe regardless. Correctness legs: fresh-backfill,
  disk-saturation (`disk.saturated == 0`, superseded churn present), warm-rejoin
  all PASS on the change code. VERDICT: Phase 3 accepted — the serve path's top
  structural constraint (the single-threaded IOWorker carrying inflate+parse) is
  eliminated; artifacts `profile-results/b3-{serve,hibw,backfill}`.

- **2026-08-07 — B4 (R2 selective NBT parse) gate session: ALL GATES PASS. Box
  exclusive** (`pgrep` clean; tree frozen). Protocol: kill-switch A/B on the ONE
  review-fixed commit `bae0952` — same jar, `PROFILE_SELECTIVE_PARSE=false|true`,
  manually ABBA-interleaved, 4+4 reps × 150 s per arm, serve
  (`b4-off`/`b4-on`, vanilla arms) + backfill (`b4-bf-off`/`b4-bf-on`, walk
  window); the earlier in-flight session on pre-fix code was stopped and
  discarded (decisions log). **Gate 1 (alloc/col ≥30% cut on the four churn
  classes String/HashMap$Node/StringTag/CompoundTag): PASS both arms — serve
  80172 → 40058 B/col (+50.0%), backfill 78523 → 40742 B/col (+48.1%)**; biggest
  single movers HashMap$Node −20319 B/col and String −16018 B/col (serve).
  All-classes alloc/col: serve 416319 → 330200 B (−20.7%), backfill −23.8%.
  **Gate 2 (nbt band minus MemoizedNbtCodec ≥25% cut): PASS both arms — serve
  1097 → 546 (+50.2%; raw band 1465→904 = +38.3% [+33.0, +43.2], memo marker flat
  368→358), backfill 903 → 374 (+58.6%; raw band 1128→610 = +45.9%
  [+40.3, +51.0], memo flat 225→236)**. **Gate 3 (column parity ±2%): PASS —
  serve 182164 vs 182113 (0.03%), backfill exact (182356 both, fixed walk)**.
  **Gate 4 (backfill arm, same metrics): PASS** — walk pace unchanged (848.2 vs
  844.2 deposits/s, CPS-bound), zip −2.2% n.s., store −3.2% n.s.; nbt-other and
  serialize-live are 0-in-both there (normal for backfill windows — classifier
  drift undetectable in that arm, noted per the tool's own warning; the SERVE arm
  carries those tripwires: nbt-other −8.1% n.s., serialize-live +17.2% n.s.).
  Headline timing: serve lss µs/col 232.8 → 208.3 (+10.5% [+6.5, +14.4]),
  backfill 199.5 → 170.1 (+14.7% [+10.5, +18.7]); hibw-style ceiling from the
  serve pools flat (594.6 → 590.7 col/s, −0.7% n.s. — serve arms are not
  ceiling-bound, the ±5% leg belongs to B5's ref-vs-ref). Mechanism receipts:
  `SelectiveChunkNbtLoader` marker APPEARED (0 → 825 serve on LSS Disk Reader,
  0 → 1193 backfill on LSS Store Backfill) and **fallback warns = 0 across all 8
  change reps** (first fallback always logs — the selective path served every
  column of the healthy world without one full-parse retry). VERDICT: Phase 4
  accepted — the structure-building slice of the parse (junk-key String/HashMap
  churn for data LSS never reads) is gone from both serve and backfill paths;
  artifacts `profile-results/b4-{off,on,bf-off,bf-on}` +
  `profile-results/b4-serve-compare.txt`.

- **2026-08-07 — B5 (round-acceptance closing A/B) session: ROUND ACCEPTED — 4/5
  gates PASS, serve-µs/col gate missed as written and accepted as a documented
  deviation (decisions log). Box exclusive** (`pgrep` clean before launch; tree
  frozen 13:58–15:26 EDT). Protocol: ref-vs-ref base=`834e4d7` (B0 merge — pre-round
  code + tooling/echo) vs change=`41f8fae` (B4-merge `e079d30` + one docs commit),
  worktree arms, three matrices 4+4 reps ABBA each — serve (150 s, 20 MiB/player),
  hibw (100 s, 100 MiB/player), backfill (150 s, cps=1000) — then
  `store_gate.sh warm 3 120`. All legs RC=0, all arms echo-valid. **Noise audit
  (user-prompted, cpu.jsonl host-vs-JVM decomposition per rep): foreign CPU
  balanced across arms — serve 0.26–0.34 cores avg (arm means 0.28 vs 0.30), hibw
  0.35–0.40 flat, backfill 0.06–0.12 with the one mild outlier (+0.05 cores, one
  rep) in the CHANGE arm — conservative direction for every passed gate, and ~0.3%
  of benchmark load, an order of magnitude below the serve-arm miss it could be
  suspected of.** Gate results: **Backfill (headline) PASS** — three-thread sum
  (LSS Store Backfill + LSS LOD Store SQLite + IO-Worker LSS-frames) 3938 → 3099 =
  **+21.3%** vs ≥15%, cross-checked by lss µs/col 218.0 → 171.8 (+21.2%
  [+17.4, +24.8]); components: IO-Worker −98.8%, SQLite thread +35.5% cut (R4's
  store band +39.7%), nbt band +42.4% (R2); walk pace flat (844.2 vs 840.4
  deposits/s). **Serve alloc PASS** — four-class 76701 → 38937 B/col (+49.2%);
  backfill twin 81056 → 38591 (+52.4%) vs ≥30%. **Throughput PASS** — hibw
  ceiling 1585.5 → 1755.3 col/s (**+10.7% improved**, "or better" arm of ±5%),
  with hibw lss µs/col a +5.9% CUT [+1.4, +10.1] (the CPU-bound arm passes the
  serve gate's own number); parity serve 0.05% / hibw 0.03%; backfill col/s flat.
  **Store gate PASS outright** — median hit ratio 0.993 (floor 0.95),
  disk.submitted 0.65% (ceil 2%), addressable-CPU cut 96.6% (floor 85%), LSS-band
  cut 79.3% (floor 50%, and the plan-as-written 70% ALSO passed — the pre-agreed
  metric-2 re-baseline clause was NOT needed), hit-p95 speedups 25–37×.
  **Serve µs/col MISS as written**: 198.7 → 207.2 (−4.3%), n.s. vs zero
  [−9.1, +0.4] — the deviation entry has the full decomposition (R1 +10.3%
  measured at B3 × R2 −10.5% measured at B4 ≈ net −1.3% predicted). Tripwires:
  serve nbt-other +8.9% n.s.; serialize-live fired Poisson-significant in
  OPPOSITE directions across sessions (B3 serve +41.4% cut, B5 serve −39.0% rise,
  B5 hibw +65.4% cut) at counts ≤152 — the A/A-calibrated bursty window-edge band,
  no gate reads it, recorded not chased. VERDICT: the PERF round closes accepted;
  B5 is the C1 merge gate — C-phase unblocked. Artifacts
  `profile-results/b5-{serve,hibw,backfill}` + `b5-{serve,hibw,backfill}-compare.txt`
  + the store-gate section of the session log.

- **2026-08-07 — B2 (R4 CRC32C) gate session: ALL GATES PASS. Box exclusive**
  (`pgrep` clean before each block; tree frozen during the ref-vs-ref A/B).
  **Backfill ref-vs-ref A/B** (`profile-results/b2-backfill`, 4+4 reps × 150 s ABBA,
  base=`b43610d` change=`9abb75b`, all arms valid): **`LodStoreService.contentHash`
  marker on the batcher thread 354 → 0, cut +100.0% [+99.2, +100.0]** vs the ≥80%
  gate — total elimination below sampling resolution (the CRC32C intrinsic);
  independently corroborated by the `store` band's +41.6% [+35.0, +47.4] cut.
  Deposits/s flat (844.2 → 840.4, −0.5%): the walk is pace/IO-bound, the freed CPU
  shows in the band — the non-regression leg holds. Tripwires quiet. (The A/B ran
  against `9abb75b`; the review-fix commit `4ecd501` adds only an uncalled legacy
  method + tests + comments — measured-path-identical, the B0-A/A precedent.)
  **store_gate warm** (`b2-warm`, 2 reps): GATE PASS — hit ratio 0.99, addressable
  cut ~100%, LSS-band cut 82.5% (floor 50), §0-as-written 70% floor passed at 82.5%.
  **store_gate cold**: first run at n=2 FAILED the per-rep majority (median +8.7%
  inside the 10% ceiling, reps +6.0%/+11.5%) — re-run at n=4 on `4ecd501`:
  **GATE PASS, median +6.3%, 4/4 reps within ceiling** (the n=2 red was the
  majority-rule artifact at a tight margin, not a regression; R4 does not touch the
  cold gate's comparison — both arms run the change ref). **store_save_storm.sh**:
  PASS — storm-window CPU off 4.88 s → on 5.24 s (delta +0.36 s, bound +1.22 s),
  cadence held 50.00 ms both arms. **Store soaks** (all on `4ecd501`):
  store-second-join PASS, store-save-storm scenario PASS (+ its off arm), and
  store_offline_edit's three chained phases PASS — 0 violations, 0 warnings across
  every run. VERDICT: Phase 2 accepted; CRC32C + interim schema v4 sound.

(one entry per A/B session: date, box-state confirmation (§2 exclusivity +
`pgrep` clean), arms/refs, pooled numbers, verdict)

- **2026-08-07 — B0 A/A control (Phase 0 item 6's gate for B1) + backfill smoke.
  Box: exclusive** (`pgrep -af 'lss.multitest|test-server|benchmark.duration'` clean
  before start; nothing else ran during the runs). **Backfill smoke**
  (`profile-results/b0-smoke`, 1 rep, 150 s, cps=1000): reproduced the ad-hoc F3
  numbers exactly — 45,589 deposited in 55 s = 828.9/s (F3: ~830/s); `--window walk`
  reproduces the F3 gate counters (IO-Worker 407 under ChunkDiskReader, memo 80 on
  the backfill thread, contentHash 87). **A/A** (`profile-results/b0-aa-control`,
  3+3 reps × 150 s ABBA no-cache R=256, base=change=357782f via the worktree path;
  change-rep3 meta honestly flags `worktree_dirty` — script-only edits landed
  mid-matrix, not compiled into the jar, arms jar-identical): all 6 arms valid,
  column parity 0.03%, hibw ceiling 555.6 vs 545.2 col/s (1.9%). Steady serve-path
  metrics are QUIET as required: nbt -2.1% [-11.9,+6.9], serialize -3.7%
  [-15.9,+7.1], ChunkDiskReader marker -1.0% [-7.5,+5.1], IO-Worker LSS +1.8%
  [-6.6,+9.6], MemoizedNbtCodec -16.1% [-43.4,+5.9] (wide, small counts — Phase 1's
  ≥30%-cut gate has headroom but NOT much CI margin at 150 s×3; prefer the backfill
  arm's larger counts for the memo gate). **Calibration finding: the save/probe-
  correlated bands are BURSTY window-edge work, 5-20x over-dispersed vs Poisson** —
  serialize-live per-rep spread 6..164 on code-identical arms (pooled "cut" -135.7%
  with a CI excluding 0!), nbt-other 31..74, zip -9.4% "significant", and
  lss_attributed inherits it (-11.5% on identical code → the µs/col metric has an
  ~±12% noise floor at this protocol). Consequence applied to the tooling: the
  classifier tripwire now tests per-rep RATES (pooled t-test, T_CRIT table) instead
  of pooled Poisson counts, and is quiet on this A/A while still firing on a tight
  10x drift (selftest-pinned with this A/A's real rep data). Consequence for phase
  gates: read the steady bands/markers and per-thread LSS splits; never gate on
  serialize-live/nbt-other/zip absolutes or on µs/col deltas under ~12% without more
  reps. VERDICT: harness sound, A/A PASS, B1 may proceed.

- **2026-08-07 — B1 (R3 memo key) gate session: BOTH GATES FAILED → phase reverted.
  Box exclusive** (`pgrep` clean; tree frozen through both matrices). Protocol:
  backfill + serve ref-vs-ref, 4+4 reps × 150 s ABBA each (even reps per the A/A
  lesson), base=`834e4d7` (B0-merge main, worktree) vs change=`aa7e657` (B1 +
  review fixes); all 16 arms `arm_valid`, clean trees both sides, column parity
  0.03%/0.07%. **Gate 1 (backfill, ≥30% cut on the backfill-thread memo marker):
  FAIL — 249→286, cut −14.9% [−36.1, +3.1]** (regression direction; CI includes 0).
  **Gate 2 (serve, ≥20%): FAIL — 258→290, cut −12.4% [−32.9, +5.0].** The
  F6-added `$Key` marker made the mechanism unambiguous: 278 (backfill) / 239
  (serve) samples under the Key walk — the one-pass forEach hash costs statistically
  the same as the AbstractMap.hashCode recursion it replaced. Gate 3 (allocation):
  the EntryIterator claim was REAL (−2225 B/col HashMap$EntrySet, −1035 HashMap,
  −1060 Node[]) but fully offset (+2884 B/col Key, +2697 Node) — net wash, as both
  review lenses' recount predicted. Everything else flat: lss_attributed −0.8%
  backfill / +1.5% serve, deposits/s 832.7→828.9, µs/col ±1%, hibw-window steady;
  serve serialize-live "moved" −37% pooled but is the A/A-calibrated bursty band
  (its dispersion-aware tripwire verdict is what counts, and rep spread covers it).
  CONCLUSION: the walk is the irreducible cost of structural keying; R3's premise
  (cost lives in iterator machinery) is falsified by measurement. Phase reverted
  same-day; negative result recorded in the plan's Phase 1 verdict block; B5's
  closing A/B drops R3. Artifacts: `profile-results/b1-backfill`,
  `profile-results/b1-serve` (+ jfr-report.md each).

- **2026-08-07 — C0 settling measurement (XVER §8's gate on published size claims):
  REAL-ENCODER numbers land ABOVE the wire envelope — release notes must cite ~+10%
  wire/store growth, not §8's +4–6%.** Method: the §8 corpus (`test-server/paper`
  overworld, genuine played terrain), EVERY full column (2,684 — §8 sampled 400),
  production `serializeChunkNbt` (native v19) vs `NativeToV20Translator` over the
  real registry tables, actual zstd-1 frames, size-only (no box-exclusivity needed).
  Results: **raw 35,829 → 37,719 B/col = +1,891 B/col (+5.28%)** — inside §8's
  +3.5–6% raw envelope (upper half; this corpus is richer than §8's sample: 51 dict
  entries/col vs 38, 11 served sections vs 9.6); **zstd-1 frame 5,785 → 6,346 B/col
  = +561 B/col (+9.70%)** — §8's ≈+4–6% wire figure UNDERSTATED it, exactly the
  cross-corpus/zlib-proxy bias §8 itself flagged (the proxy omitted index bytes and
  the current palette's own compressed cost). Store rows grow with the wire figure
  ⇒ plan on ~+10% store growth at C4 migration. Robustness receipts: 0 translate
  failures, 0 reparse failures across all 2,684 columns (the cursor + tables +
  translator handled every real column); p95 frame delta +860 B, max +1,432 B —
  no pathological outliers. Artifacts: `profile-results/v20-settling/settling.json`;
  tool `V20SettlingExperimentTool` (re-runnable on any corpus).

## Live-deploy log

(every Modrinth-server deploy: build SHA, date, stage-era of the jar, store outcome)

- **2026-08-07 — A1 tracer build STAGED, restart PENDING (archon token expired).**
  Jar built from main @ 7282011 (A1 merge, PR #87), 7,718,211 bytes, uploaded over
  `mods/lod-server-support-fabric.jar` (byte-size verified; the stale
  `lod-server-support-fabric-0.9.1+26.2.jar` was REMOVED — two jars = duplicate mod
  id, boot failure). `config/lss-move-trace.enable` marker staged. The archon power
  API returned **401 — the panel session token in `~/.bot.env` expired** (the
  CLAUDE.local.md-predicted failure); an RCON `stop` was deliberately NOT attempted
  (auto-restart on stop is unverified — risking a down server is worse than a
  delayed deploy). **Needs user: a fresh HAR of any panel request to refresh
  `ARCHON_TOKEN`, then Restart + the TRACER §4.3 verification ladder** (`Move desync
  tracer ACTIVE` in latest.log; `rcon.py "lsslod diag"` → `MoveTrace: rung=moonrise`;
  JSONL over SFTP after the first flight). The staged jar is wire/store/config-
  compatible with v0.9.1, so a delayed restart is safe; store expected to open clean.
  Standing note: R-1 deploy hold applies between B2 and C4 merges.
- **2026-08-07 — A2 inert build STAGED over the A1 jar** (main @ 4c0cc35, PR #88,
  7,720,695 bytes, size-verified). Same pending restart as above; when the panel
  token is refreshed one restart brings up the A2-era build (tracer active via the
  staged marker + yield present-but-unarmed), satisfying R-3's "deploy the inert A2
  build pre-B2" in one restart. Verification ladder unchanged (TRACER §4.3), plus:
  `/lsslod diag` must show NO `Yield:` line (unarmed default).

## Flake/anomaly notes

- none yet.
