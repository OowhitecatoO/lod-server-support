# The fabric lss.accesswidener twin (headless section serialization — the palette
# histogram that replaces LevelChunkSection's recalcBlockCounts needs the container's
# palette + storage pair; vanilla keeps them behind a private volatile field on a
# private record).
public net.minecraft.world.level.chunk.PalettedContainer$Data
public net.minecraft.world.level.chunk.PalettedContainer data
