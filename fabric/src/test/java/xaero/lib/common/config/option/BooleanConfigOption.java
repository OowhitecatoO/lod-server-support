package xaero.lib.common.config.option;

/** Tier-1 stub (xaerolib) — the value is the effective one the manager answers. */
public class BooleanConfigOption extends ConfigOption {
    public boolean value = true;
}
