package xaero.lib.common.config.channel;

import xaero.lib.client.config.ClientConfigManager;

/** Tier-1 stub (xaerolib — nested per line in the real jars). */
public class ConfigChannel {
    public final ClientConfigManager manager = new ClientConfigManager();

    public ClientConfigManager getClientConfigManager() { return this.manager; }
}
