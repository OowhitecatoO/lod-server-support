package net.caffeinemc.mods.sodium.client.gui;

/**
 * Real-package-name STUB (the Voxy/Moonrise/Xaero test discipline) — exists ONLY so the
 * {@code .class} resource is on the Tier 1 classpath: {@code SodiumGeneration}'s resource
 * probe then answers LEGACY under fabric-loader-junit, which lets {@code LegacySodiumPage.build()}
 * exercise its production path against the option stubs below. Never instantiated.
 */
public final class SodiumOptionsGUI {
    private SodiumOptionsGUI() {
    }
}
