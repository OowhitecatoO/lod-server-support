package net.caffeinemc.mods.sodium.client.gui.options.storage;

/** Stub: the storage interface LegacySodiumPage implements via java.lang.reflect.Proxy. */
public interface OptionStorage<T> {
    T getData();
    void save();
}
